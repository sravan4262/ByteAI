{
  "tech_stack": "jenkins",
  "subdomain": "cicd",
  "domain": "devops",
  "bytes": [
    {
      "topic": "Pipeline Design — lint → test → build → deploy stages, fail fast principle",
      "status": "done",
      "title": "Jenkins Pipeline Stages — Enforcing Fail Fast with lint → test → build → deploy",
      "body": "Jenkins Declarative Pipelines enforce fail-fast by default: if any stage exits non-zero, downstream stages are skipped and the build is marked FAILURE. Structure your stages in ascending cost order — lint first (cheapest), then test, then build artifacts, then deploy — so expensive work never runs on broken code.\n\nUse `failFast: true` inside a `parallel` block to abort sibling branches the moment one fails. For the lint stage, wire in your actual tool (e.g. `sh 'npm run lint -- --max-warnings 0'`) so warnings become hard errors. Gate the deploy stage with a `when { branch 'main' }` condition so feature branches never accidentally push to production.\n\nStash build artifacts between stages with `stash`/`unstash` rather than rebuilding — Jenkins agents don't share a workspace by default. Use `post { always { junit 'reports/**/*.xml' } }` to surface test results even on failure, giving you signal without re-running the pipeline. Keep each stage's `sh` calls focused; fat stages hide the failure point and slow down red-to-fix feedback loops.",
      "code_snippet": "pipeline {\n  agent any\n  stages {\n    stage('Lint') {\n      steps { sh 'npm run lint -- --max-warnings 0' }\n    }\n    stage('Test') {\n      steps { sh 'npm test -- --ci' }\n      post { always { junit 'reports/jest/*.xml' } }\n    }\n    stage('Build') {\n      steps {\n        sh 'npm run build'\n        stash name: 'dist', includes: 'dist/**'\n      }\n    }\n    stage('Deploy') {\n      when { branch 'main' }\n      steps {\n        unstash 'dist'\n        sh './scripts/deploy.sh'\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "GitHub Actions — workflows, jobs, steps, reusable workflows, marketplace actions",
      "status": "done",
      "title": "Jenkins to GitHub Actions — Pipelines, Shared Libraries, and Plugins Remapped",
      "body": "If you live in Jenkinsfile land, GitHub Actions has a 1-to-1 mental model — once you know the vocabulary. A workflow (.github/workflows/*.yml) is your Jenkinsfile. Jobs are your stages, but run in isolated VMs in parallel by default — no parallel {} block needed. Steps inside a job map directly to your sh or bat steps, plus uses: to pull a marketplace action (think plugins, but versioned via Git tags, not a plugin manager). Shared Libraries become reusable workflows: define a workflow in .github/workflows/build-template.yml with on: workflow_call:, then reference it with uses: org/repo/.github/workflows/build-template.yml@main. Inputs replace your library function parameters. One critical difference from Jenkins: there's no built-in agent inheritance. Each job declares its own runs-on, so you can't set a global agent at the pipeline level. Secrets from Jenkins credentials() become secrets.MY_SECRET, scoped per job or the whole workflow. Matrix builds that previously needed Jenkins plugins are native — just drop a matrix: block under strategy:.",
      "code_snippet": "# .github/workflows/ci.yml\nname: CI\non: [push]\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 20\n      - run: npm ci && npm test\n\n  deploy:\n    needs: build\n    uses: my-org/shared-workflows/.github/workflows/deploy.yml@main\n    with:\n      environment: staging\n    secrets: inherit",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 8
    },
    {
      "topic": "GitLab CI — stages, artifacts, cache, runners, include templates",
      "status": "done",
      "title": "Jenkins Dev's Field Guide to GitLab CI: Stages, Artifacts, and Shared Templates",
      "body": "Coming from Jenkins, GitLab CI's `.gitlab-ci.yml` feels familiar but has sharp differences worth knowing upfront. Stages in GitLab CI work like Jenkins `stages {}` blocks — jobs in the same stage run in parallel, stages run sequentially. Unlike Jenkins where `stash`/`unstash` moves files between stages, GitLab uses `artifacts: paths` to persist outputs and `dependencies` to pull them into downstream jobs explicitly. Cache is separate — it's for reusing slow-to-build directories like `node_modules` between pipelines, keyed by branch and a cache key. Runners replace Jenkins agents: you register a runner with a tag (e.g., `docker`, `k8s`), then target it via `tags:` in each job — no `agent { label '' }` syntax. The biggest power unlock is `include:` — GitLab's equivalent of Jenkins Shared Libraries. You can compose pipelines from reusable template files hosted in the same repo or a central project, pulling in `.gitlab-ci.yml` fragments with `include: project:` or `include: local:`. This replaces the ceremony of `@Library` declarations and `vars/` scripts entirely.",
      "code_snippet": "# .gitlab-ci.yml — annotated for Jenkins developers\nstages: [build, test, deploy]\n\nbuild-job:\n  stage: build\n  tags: [docker]          # like: agent { label 'docker' }\n  script:\n    - npm ci\n    - npm run build\n  artifacts:\n    paths: [dist/]        # like: stash includes: 'dist/**'\n    expire_in: 1 hour\n  cache:\n    key: $CI_COMMIT_REF_SLUG\n    paths: [node_modules/]\n\ninclude:\n  - project: 'org/ci-templates'\n    file: '/templates/deploy.yml'  # like: @Library('ci-templates')import deploy",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Jenkins — pipeline as code, Jenkinsfile, shared libraries, agent config",
      "status": "done",
      "title": "Jenkins Shared Libraries — Reuse Pipeline Logic Across Every Repo",
      "body": "When you find yourself copy-pasting the same Jenkinsfile stages across dozens of repos, Jenkins Shared Libraries are the fix. Define a library in a Git repo with the standard layout: `vars/` for global call steps, `src/` for Groovy classes, and `resources/` for static files. Register it in Manage Jenkins → Configure System → Global Pipeline Libraries, then consume it with `@Library('my-lib@main') _` at the top of any Jenkinsfile.\n\nSteps defined in `vars/buildAndPush.groovy` become first-class pipeline steps — call them like `buildAndPush(image: 'myapp', tag: env.GIT_COMMIT)`. Use `src/org/myorg/Docker.groovy` for complex logic that benefits from a proper class structure with CPS-safe methods.\n\nAgent config belongs in the library too. Define a `podTemplate` wrapper in `vars/k8sAgent.groovy` so every team gets the same Kubernetes agent spec without duplicating YAML. Pair this with `Jenkinsfile` containing only business logic — the library owns the how, the repo owns the what.",
      "code_snippet": "// vars/buildAndPush.groovy\ndef call(Map config) {\n    def image = config.image\n    def tag   = config.tag ?: env.BUILD_NUMBER\n\n    docker.withRegistry('https://registry.example.com', 'registry-creds') {\n        def built = docker.build(\"${image}:${tag}\")\n        built.push()\n        built.push('latest')\n    }\n}\n\n// Jenkinsfile in any repo\n@Library('my-lib@main') _\n\npipeline {\n    agent { label 'docker-agent' }\n    stages {\n        stage('Build & Push') {\n            steps { buildAndPush(image: 'myapp', tag: env.GIT_COMMIT) }\n        }\n    }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Secrets in CI — never hardcode, vault integration, environment secrets",
      "status": "done",
      "title": "Jenkins Secrets Done Right — Credentials Plugin, Vault Integration, and What to Never Do",
      "body": "Hardcoding credentials in a Jenkinsfile is a critical misconfig — they end up in SCM history and console logs. Jenkins' built-in Credentials Plugin is your first line of defense. Store secrets as `Secret text`, `Username with password`, or `Secret file` in Manage Jenkins → Credentials, then bind them in-pipeline using `withCredentials`. For external secret management, the HashiCorp Vault Plugin lets Jenkins authenticate via AppRole or Kubernetes service accounts and fetch secrets at runtime — secrets never touch the Jenkins database. Declare vault bindings with `vault secrets: [...]` in your `withVault` block. For environment-scoped secrets, prefer folder-level or pipeline-level credential scoping over global credentials to enforce least privilege. Always set `VAULT_SKIP_VERIFY=false` in production. One often-missed detail: Jenkins masks bound credentials in console output, but only for the exact string — base64-encoded variants will still leak. Use `credentials()` in `environment {}` blocks for implicit masking across all steps, not just inside `withCredentials`.",
      "code_snippet": "pipeline {\n  agent any\n  environment {\n    DEPLOY_TOKEN = credentials('deploy-token-id')\n  }\n  stages {\n    stage('Deploy') {\n      steps {\n        withVault(vaultSecrets: [[path: 'secret/prod/db', secretValues: [\n          [envVar: 'DB_PASS', vaultKey: 'password']\n        ]]]) {\n          sh 'deploy.sh --db-pass \"$DB_PASS\" --token \"$DEPLOY_TOKEN\"'\n        }\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Caching — dependency caching, Docker layer caching, cache invalidation",
      "status": "done",
      "title": "Jenkins Caching — Dependency Caching, Docker Layer Caching, and Cache Invalidation",
      "body": "Jenkins has no built-in caching layer — you implement it explicitly. For Maven/Gradle, mount the local repo as a volume or use the `stash`/`unstash` steps to persist `.m2` or `.gradle` across stages. Better: share a host-level cache directory via `ws()` or a persistent agent volume, so all builds on that node reuse downloaded artifacts without re-fetching from Artifactory.\n\nFor Docker builds, enable BuildKit with `DOCKER_BUILDKIT=1` and ensure your `Dockerfile` orders layers from least- to most-frequently changed — base image, system deps, then app code. On the same agent, the layer cache is on-disk automatically. For distributed agents or ephemeral cloud agents, push intermediate layers to a registry using `--cache-from` and `--cache-to` with `registry` mode.\n\nCache invalidation is the hard part. Tie cache keys to lockfile hashes: compute `sha256sum package-lock.json` and use that as a directory name under your cache root. When the lockfile changes, the path misses and a fresh install runs. Stale caches cause silent failures — add a `post { always { cleanWs() } }` on the branch that owns the cache volume, scheduled weekly.",
      "code_snippet": "pipeline {\n  agent any\n  environment {\n    DOCKER_BUILDKIT = '1'\n    CACHE_KEY = sh(script: 'sha256sum package-lock.json | cut -d\" \" -f1', returnStdout: true).trim()\n  }\n  stages {\n    stage('Restore deps') {\n      steps {\n        sh 'cp -r /cache/node_modules/${CACHE_KEY}/node_modules . 2>/dev/null || npm ci && mkdir -p /cache/node_modules/${CACHE_KEY} && cp -r node_modules /cache/node_modules/${CACHE_KEY}/'\n      }\n    }\n    stage('Docker Build') {\n      steps {\n        sh '''\n          docker build \\\n            --cache-from registry.example.com/myapp:cache \\\n            --build-arg BUILDKIT_INLINE_CACHE=1 \\\n            -t registry.example.com/myapp:${BUILD_NUMBER} \\\n            -t registry.example.com/myapp:cache .\n          docker push registry.example.com/myapp:cache\n        '''\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Parallelization — matrix builds, parallel jobs, test sharding",
      "status": "done",
      "title": "Jenkins Matrix Builds — Parallel Jobs and Test Sharding at Scale",
      "body": "Jenkins' `matrix` directive (Pipeline 2.12+) lets you define a cross-product of axes, spawning isolated agents per combination — no scripted loops required. Define axes for OS, JDK version, or test suite partition, and Jenkins schedules each cell concurrently across available executors.\n\nFor test sharding, pair matrix with `excludes` to drop redundant combinations, then pass the `SHARD_INDEX` and `SHARD_TOTAL` environment variables into your test runner (e.g., pytest-split, Jest --shard). Each cell runs only its slice, then `junit` merges reports post-build.\n\nUse `parallel` blocks inside a stage for ad-hoc fan-out without the combinatorial overhead — ideal when you need three fixed test buckets, not a full matrix. Cap concurrency with `options { throttleJobProperty(...) }` via the Throttle Concurrent Builds plugin to avoid starving other pipelines on shared agents.\n\nCritical: set `failFast true` inside `matrix` so a failing cell kills siblings immediately, preventing wasted executor time on a build you'll re-run anyway.",
      "code_snippet": "pipeline {\n  agent none\n  stages {\n    stage('Matrix Test') {\n      matrix {\n        axes {\n          axis { name 'SHARD_INDEX'; values '1','2','3' }\n          axis { name 'JDK'; values '17','21' }\n        }\n        agent { label \"jdk-${JDK}\" }\n        options { timeout(time: 20, unit: 'MINUTES') }\n        failFast true\n        stages {\n          stage('Test') {\n            steps {\n              sh \"./gradlew test -Pshard=${SHARD_INDEX} -PshardTotal=3\"\n            }\n            post { always { junit 'build/test-results/**/*.xml' } }\n          }\n        }\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Build Artifacts — versioning, storing, promoting across environments",
      "status": "done",
      "title": "Jenkins Build Artifacts — Versioning, Storing, and Promoting Across Environments",
      "body": "Jenkins treats artifacts as first-class citizens via `archiveArtifacts` and the `copyArtifacts` plugin. Version your builds deterministically using `BUILD_NUMBER` combined with a Git short SHA: `${BUILD_NUMBER}-${GIT_COMMIT[0..7]}`. Archive with `archiveArtifacts artifacts: 'build/libs/*.jar', fingerprint: true` — fingerprinting enables `fingerprintArtifacts` tracking across downstream jobs.\n\nFor promotion, define a pipeline per environment (dev → staging → prod). Downstream jobs pull artifacts using `copyArtifacts(projectName: 'my-app', selector: lastSuccessful(), filter: '**/*.jar')`. Combine this with the `Promoted Builds` plugin to gate promotions behind manual approval steps using `input message: 'Promote to production?'`.\n\nStore artifacts durably by configuring an S3-backed artifact manager (`ArtifactManagerConfiguration` → AWS S3) instead of the default workspace storage, which evicts on executor cleanup. Use `BuildDiscarder(logRotator(artifactNumToKeepStr: '10'))` to cap retention. Tag promoted artifacts in Artifactory or Nexus via a `curl` post-build step, embedding the Jenkins `BUILD_URL` as provenance metadata.",
      "code_snippet": "pipeline {\n  agent any\n  options { buildDiscarder(logRotator(artifactNumToKeepStr: '10')) }\n  environment { VERSION = \"${BUILD_NUMBER}-${GIT_COMMIT[0..7]}\" }\n  stages {\n    stage('Build') {\n      steps {\n        sh \"./gradlew build -Pversion=${VERSION}\"\n        archiveArtifacts artifacts: 'build/libs/*.jar', fingerprint: true\n      }\n    }\n    stage('Promote to Staging') {\n      steps {\n        copyArtifacts(projectName: env.JOB_NAME, selector: specific(env.BUILD_NUMBER))\n        input message: \"Deploy ${VERSION} to staging?\"\n        sh \"./deploy.sh staging build/libs/app-${VERSION}.jar\"\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Environment Promotion — dev → staging → prod gates, approval workflows",
      "status": "done",
      "title": "Jenkins Environment Promotion — Gating Dev → Staging → Prod with input() and Milestone Steps",
      "body": "Jenkins Declarative Pipelines handle environment promotion via the `input` step, which pauses execution and blocks the executor thread until a user approves or aborts. Pair it with `milestone` steps to cancel any older builds that try to leapfrog a newer one — critical for preventing out-of-order deploys when two PRs land close together.\n\nDefine each environment as a discrete `stage` with a `when` condition tied to branch name or a promotion parameter. Gate staging behind a `timeout`-wrapped `input` so abandoned pipelines don't block executors indefinitely. For prod, restrict approvers using the `submitter` parameter, which accepts a comma-separated list of Jenkins usernames or group names (e.g., `submitter: 'ops-team,release-manager'`).\n\nUse `lock` from the Lockable Resources plugin on the prod deploy stage to serialize concurrent release trains. Combine with `disableConcurrentBuilds()` in the `options` block for staging to avoid environment drift during parallel runs. Store environment-specific credentials in Jenkins Credentials Store and inject via `withCredentials`, never hardcoded in the `environment` block.",
      "code_snippet": "pipeline {\n  agent any\n  options { disableConcurrentBuilds() }\n  stages {\n    stage('Deploy to Staging') {\n      steps {\n        milestone(1)\n        timeout(time: 2, unit: 'HOURS') {\n          input message: 'Promote to staging?', submitter: 'dev-leads'\n        }\n        sh './deploy.sh staging'\n      }\n    }\n    stage('Deploy to Prod') {\n      steps {\n        milestone(2)\n        lock('production-environment') {\n          timeout(time: 1, unit: 'HOURS') {\n            input message: 'Approve prod release?', submitter: 'ops-team,release-manager'\n          }\n          sh './deploy.sh prod'\n        }\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Rollback Strategy — automated rollback on failed deploy",
      "status": "done",
      "title": "Jenkins Pipelines — Automated Rollback on Failed Deploy Using previousSuccessfulBuild",
      "body": "Jenkins' Declarative Pipeline `post { failure { } }` block is the natural hook for automated rollback. Instead of hardcoding a version, use `currentBuild.previousSuccessfulBuild?.number` — a Groovy property on Jenkins' `RunWrapper` object — to dynamically target the last known-good artifact.\n\nThe pattern pairs cleanly with an image-tag-as-build-number convention: on failure, re-issue the `kubectl set image` command pointing at the previous build's tag. For non-Kubernetes targets, the **Copy Artifact** plugin's `lastSuccessful()` selector does the same job for WAR/JAR artifacts.\n\nJenkins-specific gotchas to handle:\n- `previousSuccessfulBuild` is `null` on the first ever pipeline run — always guard with the safe-navigation operator `?.` or the rollback step itself will throw.\n- `post { failure }` fires once after the whole pipeline fails, not per-stage — for stage-level rollback, wrap each `stage` in a `catchError` block instead.\n- Annotate the build with `currentBuild.description = \"ROLLED BACK to #${lastGood}\"` so the Jenkins UI and audit logs make the rollback visible without digging through console output.",
      "code_snippet": "pipeline {\n  agent any\n  environment { IMAGE = 'myrepo/app' }\n  stages {\n    stage('Deploy') {\n      steps {\n        sh \"kubectl set image deployment/app app=${IMAGE}:${BUILD_NUMBER} --record\"\n        sh \"kubectl rollout status deployment/app --timeout=120s\"\n      }\n    }\n  }\n  post {\n    failure {\n      script {\n        def lastGood = currentBuild.previousSuccessfulBuild?.number\n        if (lastGood) {\n          sh \"kubectl set image deployment/app app=${IMAGE}:${lastGood}\"\n          currentBuild.description = \"ROLLED BACK to build #${lastGood}\"\n        } else {\n          echo 'No previous successful build found — manual intervention required.'\n        }\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Preview Environments — ephemeral environments per PR, teardown on merge",
      "status": "done",
      "title": "Jenkins Preview Environments — Ephemeral PRs with Automatic Teardown",
      "body": "Jenkins Multibranch Pipelines expose `env.CHANGE_ID` for every pull request branch, making per-PR preview environments straightforward. Use a `when { changeRequest() }` guard to restrict deploy logic to PRs only, then namespace your environment with `preview-${env.CHANGE_ID}` so multiple PRs never collide.\n\nDeploy in the `Build` or `Deploy` stage using that namespace — whether you're running `kubectl apply -n preview-${env.CHANGE_ID}`, spinning up a Docker Compose stack, or provisioning a Kubernetes namespace via Helm with `--namespace pr-${env.CHANGE_ID}`.\n\nTeardown belongs in a `post { cleanup }` block on the PR branch, not the `always` block — `cleanup` runs regardless of build outcome but only after all other post conditions. When the PR merges, the Multibranch plugin deletes the branch and triggers a final run of the Jenkinsfile with `currentBuild.result` available, letting you conditionally skip teardown on failure. Pair with the GitHub Branch Source plugin's `Discard old builds` and `Prune stale branches` settings so orphaned environments don't persist if Jenkins misses a merge event.",
      "code_snippet": "pipeline {\n  agent any\n  stages {\n    stage('Deploy Preview') {\n      when { changeRequest() }\n      steps {\n        sh \"helm upgrade --install pr-${env.CHANGE_ID} ./chart --namespace pr-${env.CHANGE_ID} --create-namespace --set image.tag=${env.GIT_COMMIT[0..6]}\"\n        echo \"Preview: https://pr-${env.CHANGE_ID}.preview.example.com\"\n      }\n    }\n  }\n  post {\n    cleanup {\n      script {\n        if (env.CHANGE_ID) {\n          sh \"helm uninstall pr-${env.CHANGE_ID} --namespace pr-${env.CHANGE_ID} || true\"\n          sh \"kubectl delete namespace pr-${env.CHANGE_ID} --ignore-not-found\"\n        }\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Security Scanning — SAST, dependency scanning, container scanning in pipeline",
      "status": "done",
      "title": "Jenkins Security Scanning — SAST, Dependency, and Container Checks in One Pipeline",
      "body": "Jenkins pipelines can integrate three distinct scanning layers using purpose-built plugins and CLI tools. For SAST, the Warnings Next Generation plugin (`recordIssues`) ingests reports from tools like Semgrep or SpotBugs — run them as a `sh` step and point `recordIssues` at the output file. Dependency scanning fits naturally in a `stage('SCA')` block using OWASP Dependency-Check plugin (`dependencyCheck` / `dependencyCheckPublisher`) — it downloads the NVD feed on first run and caches it in `$JENKINS_HOME`. Container scanning belongs after the `docker build` step; invoke Trivy via `sh 'trivy image --exit-code 1 --severity HIGH,CRITICAL myapp:${BUILD_NUMBER}'`. The `--exit-code 1` flag fails the build on policy violations. Gate the pipeline by wrapping all three stages in a `parallel` block to cut wall-clock time, then aggregate results in a downstream `post { always { ... } }` block. Store reports as artifacts with `archiveArtifacts` so the Jenkins UI surfaces findings without external dashboards.",
      "code_snippet": "pipeline {\n  agent any\n  stages {\n    stage('Security') {\n      parallel {\n        stage('SAST') {\n          steps {\n            sh 'semgrep --config=auto --json > semgrep-report.json || true'\n            recordIssues tools: [semgrep(pattern: 'semgrep-report.json')]\n          }\n        }\n        stage('SCA') {\n          steps {\n            dependencyCheck additionalArguments: '--scan . --format JSON', odcInstallation: 'OWASP-DC'\n            dependencyCheckPublisher pattern: '**/dependency-check-report.json'\n          }\n        }\n        stage('Container') {\n          steps {\n            sh 'trivy image --exit-code 1 --severity HIGH,CRITICAL myapp:${BUILD_NUMBER}'\n          }\n        }\n      }\n    }\n  }\n  post {\n    always {\n      archiveArtifacts artifacts: '**/semgrep-report.json, **/dependency-check-report.json'\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "GitOps — declarative deployments, git as single source of truth",
      "status": "done",
      "title": "Jenkins GitOps — Git as the Single Source of Truth for Kubernetes Deployments",
      "body": "GitOps flips the traditional Jenkins deploy model: instead of pushing artifacts and running imperative kubectl commands, your Kubernetes manifests live in git and Jenkins reconciles cluster state to match. The Jenkinsfile itself is the IaC — checked in, reviewed via PRs, fully auditable.\n\nThe `changeset` condition gates the deploy stage so only commits touching `k8s/**` trigger a reconciliation. `kubectl apply --server-side` uses Server-Side Apply, which tracks field ownership and surfaces conflicts that client-side apply silently ignores. Adding `--prune -l app=myservice` enforces the declarative contract: resources deleted from git are deleted from the cluster. No manual cleanup, no drift.\n\nFor webhook-driven GitOps (preferred over polling), configure a GitHub webhook to POST to `<jenkins-url>/github-webhook/` and replace `pollSCM` with an empty `triggers {}` block — Jenkins reacts instantly to push events instead of lagging up to 2 minutes.\n\nStore your kubeconfig in Jenkins Credentials as a secret file, never a plain-text string, and bind it with `withCredentials([file(...)])` so it is masked in console output and wiped from disk after the step completes.",
      "code_snippet": "pipeline {\n  agent any\n  triggers { pollSCM('H/2 * * * *') }\n  stages {\n    stage('Reconcile') {\n      when { changeset 'k8s/**' }\n      steps {\n        withCredentials([file(credentialsId: 'kubeconfig', variable: 'KUBECONFIG')]) {\n          sh 'kubectl apply --server-side -f k8s/ --prune -l app=myservice'\n        }\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Self-hosted Runners — when to use, maintenance overhead, security",
      "status": "done",
      "title": "Jenkins Self-Hosted Agents — When to Use Them and What They Cost You",
      "body": "Jenkins' built-in controller can run builds directly, but `agent { label 'linux' }` points to a dedicated node registered via JNLP or SSH. Self-hosted agents make sense when builds need specific hardware (GPU, ARM), proprietary tools, or access to air-gapped internal services that Jenkins SaaS agents can't reach.\n\nThe hidden cost is maintenance: every agent node requires OS patching, JDK version alignment with the controller, and plugin-compatible remoting JARs. Drift between nodes causes `NoSuchMethodError` at runtime — pin `remoting.jar` versions via the `jenkins.remoting.version` system property and lock JDK versions with a shared Ansible role or Docker image.\n\nFor security, agents run as a dedicated `jenkins` OS user with no sudo. Enable `Agent → Controller Security` in `Manage Jenkins › Security` to prevent agent code from reading controller secrets. Use credential binding via the `withCredentials` step rather than environment variables in `Jenkinsfile`, so secrets never appear in `env` dumps. Isolate untrusted PR builds to ephemeral Docker agents (`docker.image('node:22').inside`) to prevent workspace poisoning across jobs.",
      "code_snippet": "pipeline {\n  agent { label 'linux-docker' }\n  stages {\n    stage('Build') {\n      steps {\n        withCredentials([string(credentialsId: 'NPM_TOKEN', variable: 'NPM_TOKEN')]) {\n          sh 'npm ci'\n        }\n      }\n    }\n    stage('Untrusted PR') {\n      when { changeRequest() }\n      agent { docker { image 'node:22-alpine'; reuseNode false } }\n      steps {\n        sh 'npm test'\n      }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Pipeline Performance — measuring and optimizing slow pipelines",
      "status": "done",
      "title": "Jenkins Pipeline Performance — Measuring and Eliminating Bottlenecks",
      "body": "Jenkins exposes per-stage timing natively via the Pipeline Stage View and Blue Ocean, but raw numbers hide the real culprits. Use the `currentBuild.duration` global and stage-level timestamps with `System.currentTimeMillis()` to instrument granular timings programmatically. For deeper analysis, enable the `pipeline-graph-analysis` plugin and query the REST API at `/job/<name>/<build>/wfapi/describe` — it returns each stage's `durationMillis` and `pauseDurationMillis`, separating execution time from queue wait.\n\nCommon bottlenecks: (1) Serial stages that could be `parallel {}` blocks — Maven module builds and independent test suites are frequent offenders. (2) Workspace checkout on every agent when SCM content hasn't changed — mitigate with `ws()` reuse or `skipDefaultCheckout(true)` plus selective `checkout scm`. (3) Stash/unstash of large artifacts — prefer a shared NFS mount or S3 via the Artifact Manager S3 plugin. (4) Excessive `sh` subprocess spawning inside loops — batch into a single shell heredoc. Profile first with `wfapi`, then parallelize or cache.",
      "code_snippet": "pipeline {\n  agent any\n  options { skipDefaultCheckout(true) }\n  stages {\n    stage('Checkout') {\n      steps {\n        checkout scm\n      }\n    }\n    stage('Build & Test') {\n      parallel {\n        stage('Unit Tests') {\n          steps { sh 'mvn -pl module-a test -o' }\n        }\n        stage('Integration Tests') {\n          steps { sh 'mvn -pl module-b verify -o' }\n        }\n      }\n    }\n  }\n  post {\n    always {\n      echo \"Total duration: ${currentBuild.durationString}\"\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Monorepo CI — affected detection, selective runs, turborepo/nx integration",
      "status": "done",
      "title": "Jenkins Monorepo CI — Affected Detection with Turborepo and Selective Stage Runs",
      "body": "Running your full Jenkins pipeline on every commit in a monorepo burns agent minutes and developer patience. The fix: compute the affected package set first, then conditionally execute downstream stages.\n\nIn a `Jenkinsfile`, use `sh 'npx turbo run build --filter=...[HEAD^1]'` to let Turborepo's affected graph determine what changed since the previous commit. For Nx, swap in `npx nx affected --target=build --base=HEAD~1`. Capture the output, parse it, and set pipeline-level environment variables to gate stages.\n\nPair this with Jenkins' `when { expression }` directive on each stage. A `lint` stage only runs if `env.AFFECTED_LINT == 'true'`, a `deploy` stage only fires if the `api` package is in the affected set. No custom shell scripts sprawling across the repo — the logic lives in the `Jenkinsfile` and is auditable.\n\nFor caching, mount a persistent Jenkins agent workspace volume and set `TURBO_CACHE_DIR` or `NX_CACHE_DIRECTORY` to a path that survives between builds. This gives you local task-level caching without a remote cache server.",
      "code_snippet": "pipeline {\n  agent any\n  environment {\n    AFFECTED = sh(script: 'npx turbo run build --dry=json --filter=...[HEAD^1] | jq -r \".packages[]\"', returnStdout: true).trim()\n  }\n  stages {\n    stage('Test') {\n      when { expression { env.AFFECTED.contains('packages/api') } }\n      steps { sh 'npx turbo run test --filter=api' }\n    }\n    stage('Deploy') {\n      when { expression { env.AFFECTED.contains('packages/api') } }\n      steps { sh './scripts/deploy.sh api' }\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Notifications — failure alerts, status badges, incident escalation",
      "status": "done",
      "title": "Jenkins Notifications — Failure Alerts, Status Badges, and Incident Escalation",
      "body": "Jenkins' built-in post block in Declarative Pipelines gives you granular notification hooks: always, failure, unstable, fixed, and regression. Pair this with the Email Extension Plugin (emailext) or Slack Notification Plugin to fire targeted alerts. For status badges on your repo, the Embeddable Build Status Plugin exposes a URL like /buildStatus/icon?job=my-pipeline&build=last that returns a real-time SVG — embed it directly in your README. For incident escalation, chain notification steps using currentBuild.previousBuild.result: if the previous build also failed, escalate to PagerDuty via an HTTP POST using the httpRequest step rather than a simple Slack ping. The Jenkins Monitoring Plugin (JavaMelody) and the Prometheus Metrics Plugin expose /prometheus for scraping — wire build failure rates into Grafana alerts for team-wide SLO visibility. Always include BUILD_URL, JOB_NAME, and BUILD_NUMBER in notification payloads so on-call engineers can jump directly to the console log without hunting through the UI.",
      "code_snippet": "pipeline {\n  agent any\n  post {\n    failure {\n      script {\n        def isRecurring = currentBuild.previousBuild?.result == 'FAILURE'\n        if (isRecurring) {\n          httpRequest(\n            url: 'https://events.pagerduty.com/v2/enqueue',\n            httpMode: 'POST',\n            contentType: 'APPLICATION_JSON',\n            requestBody: \"\"\"{\"routing_key\":\"${PAGERDUTY_KEY}\",\"event_action\":\"trigger\",\"payload\":{\"summary\":\"${JOB_NAME} failing repeatedly\",\"severity\":\"critical\",\"source\":\"${BUILD_URL}\"}}\"\"\"\n          )\n        } else {\n          slackSend channel: '#alerts', color: 'danger',\n            message: \"FAILED: ${JOB_NAME} #${BUILD_NUMBER} — ${BUILD_URL}\"\n        }\n      }\n    }\n    fixed {\n      slackSend channel: '#alerts', color: 'good',\n        message: \"RECOVERED: ${JOB_NAME} #${BUILD_NUMBER}\"\n    }\n  }\n}",
      "language": "bash",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    }
  ]
}