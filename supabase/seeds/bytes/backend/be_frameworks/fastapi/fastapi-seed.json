{
  "tech_stack": "fastapi",
  "subdomain": "be_frameworks",
  "domain": "backend",
  "bytes": [
    {
      "topic": "Routing — defining routes, route parameters, grouping and prefixing",
      "status": "done",
      "title": "FastAPI Routing — Route Parameters, Groups, and APIRouter Prefixes",
      "body": "FastAPI routes are defined by decorating async (or sync) functions with `@app.get`, `@app.post`, etc. Path parameters are declared with curly braces and typed in the function signature — FastAPI validates and coerces them automatically. Query parameters are any function parameters not in the path.\n\nFor larger apps, `APIRouter` is the core grouping primitive. You instantiate a router per domain, define routes on it, then register it on the main `FastAPI` instance via `app.include_router()`. The `prefix` argument on `include_router` prepends a path segment to every route in that router — no need to repeat `/users` on every handler. `tags` groups endpoints in the auto-generated OpenAPI docs.\n\nNested routers are supported: a router can `include_router` another router, composing prefixes. Dependency injection works identically on `APIRouter` as on `app`, so you can attach auth middleware at the router level rather than per-route. Path parameter types (`int`, `UUID`, `Enum`) are enforced by Pydantic before the handler runs.",
      "code_snippet": "from fastapi import APIRouter, FastAPI\nfrom uuid import UUID\n\nrouter = APIRouter(prefix=\"/users\", tags=[\"users\"])\n\n@router.get(\"/{user_id}\")\nasync def get_user(user_id: UUID):\n    return {\"user_id\": user_id}\n\n@router.get(\"/{user_id}/posts\")\nasync def get_user_posts(user_id: UUID, limit: int = 10):\n    return {\"user_id\": user_id, \"limit\": limit}\n\napp = FastAPI()\napp.include_router(router)",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Middleware — request pipeline, global vs route-specific, ordering matters",
      "status": "done",
      "title": "FastAPI Middleware — Pipeline Order, Global vs Route-Specific",
      "body": "FastAPI middleware wraps every request/response cycle via `app.add_middleware()`. Execution order is LIFO — the last middleware added runs first on the way in, last on the way out. This means if you add logging, then CORS, then GZip, GZip processes the request first. Global middleware added via `add_middleware()` fires on every route including static files and mounted sub-apps. For route-specific logic, prefer dependencies with `Depends()` instead — they respect router scope and won't pollute unrelated endpoints. Use `BaseHTTPMiddleware` for simple cases, but be aware it buffers the full response body, which breaks streaming. For streaming compatibility, implement the ASGI middleware interface directly using `__call__(scope, receive, send)`. Starlette's `add_middleware()` also accepts any pure ASGI middleware class, not just `BaseHTTPMiddleware` subclasses. A common mistake: placing `add_cors_middleware()` after `add_gzip_middleware()` — CORS headers get compressed before the browser reads them.",
      "code_snippet": "from fastapi import FastAPI, Request\nfrom starlette.middleware.base import BaseHTTPMiddleware\nimport time\n\napp = FastAPI()\n\nclass TimingMiddleware(BaseHTTPMiddleware):\n    async def dispatch(self, request: Request, call_next):\n        start = time.perf_counter()\n        response = await call_next(request)\n        duration = time.perf_counter() - start\n        response.headers[\"X-Process-Time\"] = str(duration)\n        return response\n\n# Added last = runs first on ingress\napp.add_middleware(TimingMiddleware)\n# CORS must be added AFTER timing so it wraps outermost\nfrom starlette.middleware.cors import CORSMiddleware\napp.add_middleware(CORSMiddleware, allow_origins=[\"*\"])",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Dependency Injection — built-in DI containers vs manual wiring, testability impact",
      "status": "done",
      "title": "FastAPI Dependency Injection — Depends() vs Manual Wiring",
      "body": "FastAPI's `Depends()` system is a first-class DI container built into the request lifecycle. Every dependency declared via `Depends()` is resolved per-request, cached within that request's scope by default, and automatically injected into path operation functions — no boilerplate service locator needed.\n\nManual wiring (instantiating services directly inside route handlers or at module level) breaks testability. You can't override a hardcoded `db = SessionLocal()` without monkey-patching. With `Depends()`, you override any dependency in tests using `app.dependency_overrides` — a dict that swaps real implementations for fakes without touching production code.\n\nThe `use_cache=True` default means a single dependency called by multiple sub-dependencies is only executed once per request, preventing redundant DB connections. Set `use_cache=False` when you need a fresh instance each call.\n\nFor async codebases, `async def` dependencies integrate seamlessly — FastAPI awaits them in the same event loop as the handler. Manual async wiring requires explicit `await` chains that leak infrastructure concerns into business logic.",
      "code_snippet": "from fastapi import FastAPI, Depends\nfrom sqlalchemy.orm import Session\nfrom app.database import SessionLocal\n\ndef get_db() -> Session:\n    db = SessionLocal()\n    try:\n        yield db\n    finally:\n        db.close()\n\napp = FastAPI()\n\n@app.get(\"/users/{user_id}\")\ndef read_user(user_id: int, db: Session = Depends(get_db)):\n    return db.query(User).filter(User.id == user_id).first()\n\n# In tests — swap real DB for fake with zero prod-code changes:\n# app.dependency_overrides[get_db] = lambda: fake_db_session",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Project Structure — MVC vs vertical slice, feature folders, layering conventions",
      "status": "done",
      "title": "FastAPI Project Structure — MVC Is a Trap, Use Vertical Slices",
      "body": "Most FastAPI tutorials push you toward an MVC-style layout: a top-level `routers/`, `models/`, `schemas/`, `services/` split. It looks tidy until your app grows — then changing one feature means touching four directories. Vertical slice architecture groups everything by domain instead. A `features/users/` folder holds its own `router.py`, `schemas.py`, `service.py`, and `models.py`. Your `main.py` simply includes each feature's `APIRouter` via `app.include_router(users.router, prefix='/users', tags=['users'])`. FastAPI's `APIRouter` is purpose-built for this: it supports prefix, tags, dependencies, and response models at the router level, so each slice is fully self-contained. Shared infrastructure — `db.py`, `auth.py`, `exceptions.py` — lives in a top-level `core/` module, imported by slices as needed. This maps directly to FastAPI's Dependency Injection system: slice-level `Depends()` chains stay local, cross-cutting concerns (e.g. `get_current_user`) live in `core/deps.py`. When a slice grows large enough, it becomes a mounted sub-application via `app.mount()` with zero refactoring.",
      "code_snippet": "# app/features/users/router.py\nfrom fastapi import APIRouter, Depends\nfrom app.core.deps import get_current_user\nfrom .schemas import UserOut\nfrom .service import UserService\n\nrouter = APIRouter()\n\n@router.get('/me', response_model=UserOut)\nasync def get_me(\n    current_user=Depends(get_current_user),\n    svc: UserService = Depends()\n):\n    return await svc.get_by_id(current_user.id)\n\n# app/main.py\nfrom app.features.users.router import router as users_router\napp.include_router(users_router, prefix='/users', tags=['users'])",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Input Validation — schema validation at the boundary, sanitization, error responses",
      "status": "done",
      "title": "FastAPI Input Validation — Schema Enforcement, Sanitization, and Error Responses",
      "body": "FastAPI uses Pydantic v2 models as the validation layer at every boundary — request bodies, query params, path params, and headers. Define a `BaseModel` subclass and FastAPI automatically parses, coerces, and validates before your handler runs. For stricter constraints, use `Annotated` with `Field()`: set `min_length`, `max_length`, `pattern` (regex), `ge`/`le` for numerics. For sanitization, add a `@field_validator` with `mode='before'` to strip whitespace or normalize input before type coercion. When validation fails, FastAPI returns a `422 Unprocessable Entity` with a structured `detail` list — each entry identifies the failing field, location, and error type. You can override this entirely by catching `RequestValidationError` via `app.exception_handler(RequestValidationError)` and returning a custom `JSONResponse`. For query params, annotate directly in the function signature using `Query(min_length=1, max_length=50)` — Pydantic's machinery handles it identically to body models. Never trust raw input; always declare constraints at the schema level, not inside handler logic.",
      "code_snippet": "from fastapi import FastAPI, Query\nfrom fastapi.exceptions import RequestValidationError\nfrom fastapi.responses import JSONResponse\nfrom pydantic import BaseModel, Field, field_validator\nfrom typing import Annotated\n\napp = FastAPI()\n\nclass CreateUserRequest(BaseModel):\n    username: Annotated[str, Field(min_length=3, max_length=30, pattern=r'^[a-z0-9_]+$')]\n    email: Annotated[str, Field(max_length=255)]\n\n    @field_validator('username', mode='before')\n    @classmethod\n    def normalize(cls, v: str) -> str:\n        return v.strip().lower()\n\n@app.exception_handler(RequestValidationError)\nasync def validation_error_handler(request, exc: RequestValidationError):\n    return JSONResponse(status_code=400, content={\"errors\": exc.errors()})\n\n@app.post(\"/users\")\nasync def create_user(body: CreateUserRequest, q: Annotated[str | None, Query(max_length=50)] = None):\n    return {\"username\": body.username}",
      "language": "python",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Authentication — JWT validation, session handling, auth middleware patterns",
      "status": "done",
      "title": "FastAPI JWT Auth — Dependency Injection for Clean Middleware Patterns",
      "body": "FastAPI's dependency injection system is the idiomatic way to handle JWT validation — not middleware. Use `Depends()` to declare auth requirements per route, keeping concerns separated and testable. Define a `get_current_user` dependency that pulls the `Authorization` header via `HTTPBearer`, decodes the JWT with `python-jose`, and raises `HTTPException(401)` on failure. Inject it into routes with `Depends(get_current_user)`. For session handling, FastAPI has no built-in session layer — pair it with `itsdangerous` or `starsessions` for server-side sessions, or stay stateless with JWT refresh tokens. If you genuinely need global auth logic (e.g., rate limiting by user), use `app.middleware('http')` or a `BaseHTTPMiddleware` subclass, but note these can't use `Depends()` — extract shared logic into plain functions instead. For role-based access, chain dependencies: `Depends(require_role('admin'))` calls `get_current_user` internally. This composes cleanly, documents itself in the OpenAPI schema, and lets you mock auth trivially in tests using `app.dependency_overrides`.",
      "code_snippet": "from fastapi import Depends, HTTPException, status\nfrom fastapi.security import HTTPBearer, HTTPAuthorizationCredentials\nfrom jose import JWTError, jwt\n\nbearer = HTTPBearer()\nSECRET, ALGORITHM = \"secret\", \"HS256\"\n\ndef get_current_user(creds: HTTPAuthorizationCredentials = Depends(bearer)):\n    try:\n        payload = jwt.decode(creds.credentials, SECRET, algorithms=[ALGORITHM])\n        return payload[\"sub\"]\n    except JWTError:\n        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail=\"Invalid token\")\n\n@app.get(\"/me\")\ndef me(user_id: str = Depends(get_current_user)):\n    return {\"user\": user_id}",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Testing — unit vs integration, mocking HTTP, test database strategy",
      "status": "done",
      "title": "FastAPI Testing — Unit vs Integration, Mocking HTTP, and Test DB Strategy",
      "body": "FastAPI's `TestClient` (built on `httpx`) blurs the unit/integration line — use it for integration tests that exercise your full request/response cycle including middleware and dependency injection. For pure unit tests, test business logic functions directly without spinning up the app. Mock external HTTP calls using `respx` (httpx-native) rather than `unittest.mock`, since FastAPI's async client uses httpx under the hood. For test databases, override the `get_db` dependency via `app.dependency_overrides` to inject a SQLite or isolated PostgreSQL session — never mutate your real DB fixture. Use `pytest` fixtures with `scope=\"function\"` for DB teardown between tests. Pair `AsyncClient` with `pytest-asyncio` for async route testing. Avoid `TestClient` for async endpoints in production-like scenarios — it runs a sync wrapper that can mask event loop issues. Keep your `conftest.py` lean: one `engine` fixture, one `session` fixture, one `client` fixture that wires them together via `dependency_overrides`.",
      "code_snippet": "import pytest\nfrom httpx import AsyncClient, ASGITransport\nfrom sqlalchemy.ext.asyncio import create_async_engine, AsyncSession\nfrom sqlalchemy.orm import sessionmaker\nfrom app.main import app\nfrom app.database import get_db\n\nTEST_DB_URL = \"sqlite+aiosqlite:///:memory:\"\nengine = create_async_engine(TEST_DB_URL)\nTestingSession = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)\n\n@pytest.fixture\nasync def client():\n    async def override_get_db():\n        async with TestingSession() as session:\n            yield session\n    app.dependency_overrides[get_db] = override_get_db\n    async with AsyncClient(transport=ASGITransport(app=app), base_url=\"http://test\") as ac:\n        yield ac\n    app.dependency_overrides.clear()",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Error Handling — global error handlers, consistent error response format",
      "status": "done",
      "title": "FastAPI Global Error Handling — Consistent Error Responses with Exception Handlers",
      "body": "FastAPI lets you register app-wide exception handlers via `@app.exception_handler()` or `app.add_exception_handler()`, giving you one place to normalize every error response. Without this, unhandled exceptions return inconsistent shapes — some return `{\"detail\": ...}`, others raw 500s.\n\nDefine a `ProblemDetail` schema and wire it to both `RequestValidationError` (Pydantic validation failures) and `HTTPException`. For `RequestValidationError`, FastAPI normally returns a 422 with a nested `errors` array — override it to match your envelope. For custom domain errors, subclass `HTTPException` and add fields like `error_code`.\n\nUse `StarletteHTTPException` (not FastAPI's re-export) when overriding the base HTTP handler, otherwise your handler silently won't fire. Register order matters: more specific exception types must be registered after generic ones since FastAPI matches last-registered first.\n\nFor unhandled exceptions, add a bare `Exception` handler that logs the traceback via `logging` or your OTel logger, then returns a sanitized 500 — never leak stack traces to clients.",
      "code_snippet": "from fastapi import FastAPI, Request, HTTPException\nfrom fastapi.exceptions import RequestValidationError\nfrom fastapi.responses import JSONResponse\nfrom starlette.exceptions import HTTPException as StarletteHTTPException\n\napp = FastAPI()\n\ndef error_envelope(status: int, code: str, message: str) -> dict:\n    return {\"status\": status, \"error_code\": code, \"message\": message}\n\n@app.exception_handler(StarletteHTTPException)\nasync def http_handler(request: Request, exc: StarletteHTTPException):\n    return JSONResponse(status_code=exc.status_code,\n        content=error_envelope(exc.status_code, \"HTTP_ERROR\", exc.detail))\n\n@app.exception_handler(RequestValidationError)\nasync def validation_handler(request: Request, exc: RequestValidationError):\n    return JSONResponse(status_code=422,\n        content=error_envelope(422, \"VALIDATION_ERROR\", str(exc.errors())))\n\n@app.exception_handler(Exception)\nasync def unhandled_handler(request: Request, exc: Exception):\n    return JSONResponse(status_code=500,\n        content=error_envelope(500, \"INTERNAL_ERROR\", \"Unexpected error\"))",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "ORM & Database — query builders vs full ORMs, N+1 prevention, migrations",
      "status": "done",
      "title": "FastAPI + SQLAlchemy — N+1 Prevention, ORMs vs Core, and Alembic Migrations",
      "body": "FastAPI pairs most naturally with SQLAlchemy — either its ORM layer or Core (query builder style). The ORM handles relationship mapping, but lazy loading is disabled by default in async SQLAlchemy's `AsyncSession`. That's intentional: it forces explicit loading and eliminates N+1 at the source.\n\nWhen loading `Post` objects with their `comments`, pass `selectinload(Post.comments)` to your `select()`. Skip it and accessing `post.comments` in an async context raises `MissingGreenlet` — SQLAlchemy refuses silent lazy loads under `asyncio`. `joinedload` works too but produces a single JOIN; prefer `selectinload` for one-to-many to avoid row duplication.\n\nSQLAlchemy Core fits complex reporting or bulk operations where you want raw SQL control without ORM overhead — use `text()` or `select()` directly against table objects.\n\nFor migrations, Alembic hooks into your `Base.metadata`. Run `alembic revision --autogenerate -m \"description\"` after model changes, but always audit the output — autogenerate misses `server_default` changes, constraint renames, and enum alterations.\n\nSQLModel (SQLAlchemy + Pydantic in one class) cuts boilerplate but obscures what SQLAlchemy is doing underneath. Worth knowing the layer you're abstracting.",
      "code_snippet": "from sqlalchemy.ext.asyncio import AsyncSession\nfrom sqlalchemy.orm import selectinload\nfrom sqlalchemy import select\nfrom app.models import Post\n\nasync def get_posts_with_comments(db: AsyncSession) -> list[Post]:\n    result = await db.execute(\n        select(Post)\n        .options(selectinload(Post.comments))\n        .where(Post.is_published == True)\n        .order_by(Post.created_at.desc())\n    )\n    return result.scalars().all()",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Security — CORS, rate limiting, security headers, injection prevention",
      "status": "done",
      "title": "FastAPI Security — CORS, Rate Limiting, Headers, and Injection Defense",
      "body": "FastAPI's `CORSMiddleware` handles origin whitelisting, but misconfiguring `allow_origins=[\"*\"]` with `allow_credentials=True` is a spec violation and silently breaks in browsers — use explicit origins instead. For rate limiting, `slowapi` integrates cleanly via a `Limiter` attached to `app.state`, with `@limiter.limit(\"10/minute\")` on individual routes. Security headers (X-Content-Type-Options, X-Frame-Options, HSTS) aren't built-in — add them via a custom `BaseHTTPMiddleware` or use `secure` library's `SecureHeaders` class. For SQL injection, FastAPI's SQLAlchemy integration with parameterized queries via `text()` + bound params is safe, but raw f-string queries into `db.execute()` are not — always use `:param` syntax. For NoSQL/Redis injection, sanitize keys before use. Pydantic models at request boundaries are your best injection defense: strict typing rejects malformed payloads before they reach business logic. Enable `model_config = ConfigDict(strict=True)` on sensitive schemas to disallow type coercion.",
      "code_snippet": "from fastapi import FastAPI, Request\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom slowapi import Limiter, _rate_limit_exceeded_handler\nfrom slowapi.util import get_remote_address\nfrom slowapi.errors import RateLimitExceeded\nfrom starlette.middleware.base import BaseHTTPMiddleware\nfrom starlette.responses import Response\n\nlimiter = Limiter(key_func=get_remote_address)\napp = FastAPI()\napp.state.limiter = limiter\napp.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)\n\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"https://app.yourdomain.com\"],\n    allow_credentials=True,\n    allow_methods=[\"GET\", \"POST\"],\n    allow_headers=[\"Authorization\", \"Content-Type\"],\n)\n\nclass SecurityHeadersMiddleware(BaseHTTPMiddleware):\n    async def dispatch(self, request: Request, call_next):\n        response: Response = await call_next(request)\n        response.headers[\"X-Content-Type-Options\"] = \"nosniff\"\n        response.headers[\"X-Frame-Options\"] = \"DENY\"\n        response.headers[\"Strict-Transport-Security\"] = \"max-age=63072000; includeSubDomains\"\n        return response\n\napp.add_middleware(SecurityHeadersMiddleware)\n\n@app.get(\"/items\")\n@limiter.limit(\"10/minute\")\nasync def list_items(request: Request):\n    return {\"items\": []}",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Configuration — env vars, settings classes, secrets management",
      "status": "done",
      "title": "FastAPI Settings Management with pydantic-settings",
      "body": "FastAPI pairs naturally with `pydantic-settings` for type-safe configuration. Define a `Settings` class inheriting from `BaseSettings` — Pydantic automatically reads values from environment variables, `.env` files, and secrets directories, with full type coercion and validation.\n\nUse `@lru_cache` on your `get_settings()` dependency so the settings object is instantiated once per process, not on every request. Inject it via `Depends(get_settings)` in your route handlers.\n\nFor secrets, set `model_config = SettingsConfigDict(secrets_dir='/run/secrets')` — Pydantic reads files in that directory as field values, ideal for Docker secrets or Kubernetes mounted secrets. Field names map directly to filenames.\n\nNever instantiate `Settings()` at module level outside the cached factory — doing so breaks environment override in tests. Instead, always go through `get_settings()`. In tests, override the dependency with `app.dependency_overrides[get_settings] = lambda: Settings(database_url='sqlite://:memory:')` for clean isolation without monkeypatching `os.environ`.",
      "code_snippet": "from functools import lru_cache\nfrom pydantic import SecretStr\nfrom pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass Settings(BaseSettings):\n    model_config = SettingsConfigDict(\n        env_file=\".env\", secrets_dir=\"/run/secrets\", env_file_encoding=\"utf-8\"\n    )\n    database_url: str\n    api_key: SecretStr\n    debug: bool = False\n\n@lru_cache\ndef get_settings() -> Settings:\n    return Settings()\n\n# In your route:\n# @app.get(\"/\")\n# def root(settings: Settings = Depends(get_settings)): ...",
      "language": "python",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Performance — connection pooling, async handlers, response compression",
      "status": "done",
      "title": "FastAPI Performance Triad — Connection Pooling, Async Handlers, and Response Compression",
      "body": "FastAPI's performance ceiling is often hit at the I/O boundary, not in Python itself. Three levers matter most.\n\n**Connection Pooling:** Use SQLAlchemy's async engine with `pool_size` and `max_overflow` tuned to your worker count. With `asyncpg` under the hood, each Uvicorn worker maintains its own pool — set `pool_size=10, max_overflow=20` per worker, not globally.\n\n**Async Handlers:** Every route that touches a database, cache, or external HTTP call must be `async def`. Mixing sync `def` routes forces FastAPI to run them in a threadpool executor via `anyio.to_thread.run_sync`, adding latency. Profile with `asyncio.get_event_loop().set_debug(True)` to catch blocking calls leaking into the event loop.\n\n**Response Compression:** Add `GZipMiddleware` from `starlette.middleware.gzip` with `minimum_size=1000` to skip compressing small payloads. For JSON-heavy endpoints returning large arrays, this alone can cut wire bytes by 60–80%. Pair with `ORJSONResponse` as your default response class — it serializes 3–5x faster than stdlib `json`.",
      "code_snippet": "from fastapi import FastAPI\nfrom fastapi.responses import ORJSONResponse\nfrom starlette.middleware.gzip import GZipMiddleware\nfrom sqlalchemy.ext.asyncio import create_async_engine, AsyncSession\nfrom sqlalchemy.orm import sessionmaker\n\nengine = create_async_engine(\n    \"postgresql+asyncpg://user:pass@host/db\",\n    pool_size=10,\n    max_overflow=20,\n    pool_pre_ping=True,\n)\nAsyncSessionLocal = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)\n\napp = FastAPI(default_response_class=ORJSONResponse)\napp.add_middleware(GZipMiddleware, minimum_size=1000)\n\n@app.get(\"/items\")\nasync def list_items():\n    async with AsyncSessionLocal() as session:\n        result = await session.execute(select(Item))\n        return result.scalars().all()",
      "language": "python",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "API Design — REST conventions, versioning strategy, OpenAPI generation",
      "status": "done",
      "title": "FastAPI — REST Versioning, Router Structure, and OpenAPI Without the Mess",
      "body": "FastAPI's `APIRouter` with prefix-based versioning is the cleanest way to maintain multiple API versions. Mount each version as a sub-application or use `include_router` with a `/v1` prefix — both approaches expose isolated OpenAPI schemas at `/v1/openapi.json`. For true schema isolation, mount versioned `FastAPI()` instances directly: `app.mount(\"/v1\", app_v1)`. Each sub-app gets its own `/docs` and `/openapi.json`.\n\nFor REST conventions, leverage FastAPI's response model system — `response_model=UserOut` strips internal fields and documents the schema automatically. Pair this with `status_code=status.HTTP_201_CREATED` on POST endpoints so your OpenAPI spec reflects accurate HTTP semantics.\n\nUse `tags` on routers (not individual routes) for clean Swagger grouping. Customize `openapi_tags` at the app level to add descriptions and external docs links. For deprecating endpoints, FastAPI's `@router.get(\"/old\", deprecated=True)` marks them in Swagger without removal. Combine `generate_unique_id_function` to control operationId naming for SDK generation — essential if you're auto-generating TypeScript or Python clients from the spec.",
      "code_snippet": "from fastapi import FastAPI, APIRouter, status\nfrom pydantic import BaseModel\n\napp_v1 = FastAPI(title=\"ByteAI API\", version=\"1.0.0\", openapi_tags=[\n    {\"name\": \"users\", \"description\": \"User operations\"}\n])\n\nrouter = APIRouter(prefix=\"/users\", tags=[\"users\"])\n\nclass UserOut(BaseModel):\n    id: int\n    username: str\n\n@router.post(\"/\", response_model=UserOut, status_code=status.HTTP_201_CREATED)\nasync def create_user(username: str) -> UserOut:\n    return UserOut(id=1, username=username)\n\n@router.get(\"/legacy\", deprecated=True, response_model=UserOut)\nasync def legacy_user() -> UserOut:\n    return UserOut(id=0, username=\"legacy\")\n\napp_v1.include_router(router)\napp = FastAPI()\napp.mount(\"/v1\", app_v1)",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Serialization — request/response DTOs, camelCase vs snake_case, null handling",
      "status": "done",
      "title": "FastAPI Serialization — DTOs, camelCase, and Null Handling Done Right",
      "body": "FastAPI uses Pydantic models as your request/response DTOs. By default, field names serialize as-is (snake_case), but most frontend clients expect camelCase. Use `model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True)` on your Pydantic model to handle this automatically — `populate_by_name=True` lets FastAPI still accept snake_case on inbound requests. For response serialization, pass `by_alias=True` to `response_model_by_alias=True` on your route decorator so the alias is used in the JSON output. For null handling, Pydantic v2 distinguishes between a field being absent and explicitly `None`. Use `Optional[str] = None` for nullable fields, but if you want to exclude unset fields from responses entirely, call `.model_dump(exclude_unset=True)` before returning. Pair this with `response_model_exclude_none=True` on the route to strip null fields from the serialized response automatically — useful for sparse update payloads and keeping API responses clean without manual filtering.",
      "code_snippet": "from pydantic import BaseModel, ConfigDict\nfrom pydantic.alias_generators import to_camel\nfrom fastapi import FastAPI\n\napp = FastAPI()\n\nclass UserResponse(BaseModel):\n    model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True)\n\n    user_id: int\n    display_name: str\n    bio: str | None = None\n\n@app.get(\"/users/{user_id}\", response_model=UserResponse, response_model_by_alias=True, response_model_exclude_none=True)\nasync def get_user(user_id: int) -> UserResponse:\n    return UserResponse(user_id=user_id, display_name=\"sravan\", bio=None)",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Background Jobs — task queues, scheduled jobs, worker process patterns",
      "status": "done",
      "title": "FastAPI Background Jobs — BackgroundTasks vs. Celery vs. ARQ",
      "body": "FastAPI ships with `BackgroundTasks`, injected directly into path operation functions. It's fire-and-forget within the same process — fine for sending emails after a response, but it shares the Uvicorn worker's memory and dies if the process restarts. For durable, distributed work, reach for ARQ (asyncio-native, backed by Redis) or Celery with `celery[redis]`. ARQ fits naturally into async FastAPI apps: define `async def` task functions, register them in `WorkerSettings`, and enqueue with `await redis.enqueue_job('send_report', user_id)`. Celery requires a separate worker process (`celery -A tasks worker`) and is better when you need beat scheduling, task chaining, or canvas primitives. For scheduled jobs without a full queue, `APScheduler` with `AsyncIOScheduler` integrates cleanly via a FastAPI `lifespan` context manager — start the scheduler on startup, shut it down on teardown. Never use `BackgroundTasks` for anything that must survive a crash or needs retry logic; that's what worker processes exist for.",
      "code_snippet": "from contextlib import asynccontextmanager\nfrom apscheduler.schedulers.asyncio import AsyncIOScheduler\nfrom fastapi import FastAPI\n\nscheduler = AsyncIOScheduler()\n\n@scheduler.scheduled_job(\"cron\", hour=0, minute=0)\nasync def nightly_digest():\n    await send_digest_emails()\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    scheduler.start()\n    yield\n    scheduler.shutdown()\n\napp = FastAPI(lifespan=lifespan)",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Logging — structured logs, correlation IDs, request logging middleware",
      "status": "done",
      "title": "FastAPI Structured Logging — Correlation IDs and Request Middleware",
      "body": "FastAPI has no built-in request logging, so you wire it yourself via `app.middleware('http')`. The key is attaching a correlation ID per request using Python's `contextvars.ContextVar` — this lets structured log entries share a trace ID across async call stacks without threading hacks.\n\nUse `structlog` with `structlog.contextvars.bind_contextvars()` to stamp every log call in a request's lifetime with the same `request_id`. Generate the ID from the incoming `X-Request-ID` header (for upstream propagation) or mint a new `uuid4`. Bind it at the top of your middleware, then clear it on the way out.\n\nFor the log format, configure `structlog` with `JSONRenderer` in production and `ConsoleRenderer` locally. Emit at minimum: `method`, `path`, `status_code`, `duration_ms`, and `request_id`. Avoid logging request bodies by default — they bloat logs and risk leaking PII. Use `uvicorn`'s `--no-access-log` flag to suppress its duplicate access logs once your middleware is in place.",
      "code_snippet": "import uuid, time, structlog\nfrom contextvars import ContextVar\nfrom fastapi import FastAPI, Request\nfrom structlog.contextvars import bind_contextvars, clear_contextvars\n\nrequest_id_var: ContextVar[str] = ContextVar(\"request_id\", default=\"\")\nlog = structlog.get_logger()\napp = FastAPI()\n\n@app.middleware(\"http\")\nasync def logging_middleware(request: Request, call_next):\n    clear_contextvars()\n    req_id = request.headers.get(\"X-Request-ID\", str(uuid.uuid4()))\n    bind_contextvars(request_id=req_id)\n    start = time.perf_counter()\n    response = await call_next(request)\n    duration_ms = round((time.perf_counter() - start) * 1000, 2)\n    log.info(\"request\", method=request.method, path=request.url.path,\n             status_code=response.status_code, duration_ms=duration_ms)\n    response.headers[\"X-Request-ID\"] = req_id\n    return response",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Framework-specific — Django batteries-included, FastAPI auto-docs, NestJS decorators, Phoenix LiveView",
      "status": "done",
      "title": "FastAPI Auto-Docs — What /docs Actually Gives You and How to Control It",
      "body": "FastAPI generates interactive OpenAPI documentation at /docs (Swagger UI) and /redoc automatically — zero configuration required. Every route, path parameter, query parameter, request body, and response model is introspected at startup via Python type hints and Pydantic models.\n\nThe docs aren't just cosmetic. FastAPI reads your `response_model`, `status_code`, and `responses` kwargs to build accurate schema definitions. Declare `response_model=UserOut` and the schema reflects exactly what clients receive — not what's stored internally.\n\nYou can tag routes with `tags=[\"users\"]` for grouping, add `summary` and `description` per endpoint, and attach `openapi_extra` for anything FastAPI doesn't natively support (e.g. custom security schemes).\n\nTo exclude sensitive internal routes from the docs entirely, set `include_in_schema=False` on the route decorator. To disable the docs UI in production while keeping the OpenAPI JSON available, pass `docs_url=None` to `FastAPI()` and serve `/openapi.json` on a restricted path.\n\nThe schema is generated once at startup from `app.openapi()` — you can override that method to inject custom metadata, merge external specs, or mutate the schema dict directly.",
      "code_snippet": "from fastapi import FastAPI\nfrom pydantic import BaseModel\n\napp = FastAPI(docs_url=\"/docs\", redoc_url=None)\n\nclass UserOut(BaseModel):\n    id: int\n    username: str\n\n@app.get(\n    \"/users/{user_id}\",\n    response_model=UserOut,\n    tags=[\"users\"],\n    summary=\"Fetch a user by ID\",\n    responses={404: {\"description\": \"User not found\"}},\n)\nasync def get_user(user_id: int):\n    return {\"id\": user_id, \"username\": \"alice\"}\n\n@app.get(\"/internal/health\", include_in_schema=False)\nasync def health():\n    return {\"status\": \"ok\"}",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Monitoring — health check endpoints, metrics exposure, distributed tracing",
      "status": "done",
      "title": "FastAPI Monitoring — Health Checks, Prometheus Metrics, and OpenTelemetry Tracing",
      "body": "FastAPI pairs naturally with three monitoring layers. For health checks, define a `/health` route returning dependency statuses — database ping, Redis connectivity, external API reachability — not just `{\"status\": \"ok\"}`. Use `prometheus-fastapi-instrumentator` to expose `/metrics` with zero boilerplate: one `Instrumentator().instrument(app).expose(app)` call gives you request count, latency histograms, and in-flight gauges per route automatically. For distributed tracing, `opentelemetry-instrumentation-fastapi` auto-instruments every request, propagating W3C `traceparent` headers across service boundaries. Configure an OTLP exporter to send spans to Jaeger, Tempo, or any OpenTelemetry-compatible backend. The key integration point is FastAPI's `lifespan` context — initialize your `TracerProvider` and flush it on shutdown there, not in a plain `@app.on_event` (deprecated). Combine all three layers: health for alerting, metrics for dashboards, traces for root-cause analysis. Route `/health` and `/metrics` to a separate internal port using `uvicorn`'s `--port` alongside a secondary `Server` instance to avoid exposing them publicly.",
      "code_snippet": "from fastapi import FastAPI\nfrom contextlib import asynccontextmanager\nfrom prometheus_fastapi_instrumentator import Instrumentator\nfrom opentelemetry import trace\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import BatchSpanProcessor\nfrom opentelemetry.exporter.otlp.proto.grpc.trace_exporter import OTLPSpanExporter\nfrom opentelemetry.instrumentation.fastapi import FastAPIInstrumentor\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    provider = TracerProvider()\n    provider.add_span_processor(BatchSpanProcessor(OTLPSpanExporter(endpoint=\"http://otel-collector:4317\")))\n    trace.set_tracer_provider(provider)\n    FastAPIInstrumentor.instrument_app(app)\n    yield\n    provider.shutdown()\n\napp = FastAPI(lifespan=lifespan)\nInstrumentator().instrument(app).expose(app)\n\n@app.get(\"/health\")\nasync def health(db: AsyncSession = Depends(get_db)):\n    await db.execute(text(\"SELECT 1\"))\n    return {\"status\": \"healthy\", \"db\": \"ok\"}",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Dev Tooling — hot reload, code generation, scaffolding CLI",
      "status": "done",
      "title": "FastAPI Dev Tooling — Hot Reload, CLI Scaffolding, and Code Generation",
      "body": "FastAPI's development loop centers on Uvicorn's `--reload` flag, but most developers don't configure it beyond the default. Pass `--reload-dir` to watch only your app package, not the entire working directory — this prevents reloads triggered by log files or migration artifacts. Pair it with `--reload-delay 0.1` to debounce rapid saves.\n\nFor scaffolding, `fastapi-cli` (shipped with FastAPI 0.111+) provides `fastapi dev` and `fastapi run` commands that wrap Uvicorn with sensible defaults: `fastapi dev main.py` enables reload automatically and binds to `127.0.0.1` only, while `fastapi run` targets production with `0.0.0.0`.\n\nFor code generation, `datamodel-code-generator` converts OpenAPI schemas or JSON Schema directly into Pydantic v2 models — critical when integrating third-party APIs. Run it as a pre-commit step or Makefile target to keep models in sync with upstream specs. Combine with `openapi-python-client` to auto-generate a fully typed async client from your own FastAPI app's `/openapi.json` endpoint.",
      "code_snippet": "# Install\n# pip install fastapi[standard] datamodel-code-generator openapi-python-client\n\n# Hot reload — watch only app dir, debounce saves\nuvicorn app.main:app --reload --reload-dir app --reload-delay 0.1\n\n# fastapi-cli equivalent (FastAPI 0.111+)\nfastapi dev app/main.py\n\n# Generate Pydantic v2 models from a remote OpenAPI spec\ndatamodel-codegen \\\n  --url https://api.example.com/openapi.json \\\n  --input-file-type openapi \\\n  --output models/external.py \\\n  --output-model-type pydantic_v2.BaseModel\n\n# Generate a typed async client from your own app\nopenapi-python-client generate --url http://localhost:8000/openapi.json",
      "language": "bash",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    }
  ]
}