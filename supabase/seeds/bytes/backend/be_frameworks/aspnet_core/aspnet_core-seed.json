{
  "tech_stack": "aspnet_core",
  "subdomain": "be_frameworks",
  "domain": "backend",
  "bytes": [
    {
      "topic": "Project Structure — MVC vs vertical slice, feature folders, layering conventions",
      "status": "done",
      "title": "ASP.NET Core Project Structure — MVC Layers vs. Vertical Slices",
      "body": "Traditional ASP.NET Core projects mirror MVC's horizontal layers: `Controllers/`, `Services/`, `Repositories/`, `Models/`. This feels organized until a feature spans six folders and a simple change requires touching half the solution. Vertical slice architecture flips this: organize by feature, not layer. Each feature folder owns its controller (or minimal API endpoint group), command/query handlers (via MediatR), validators, and DTOs. Changes stay localized. A `Features/Articles/CreateArticle/` folder contains `CreateArticleCommand.cs`, `CreateArticleHandler.cs`, `CreateArticleValidator.cs`, and `CreateArticleResponse.cs` — everything needed, nothing else. In `Program.cs`, register handlers with `builder.Services.AddMediatR(cfg => cfg.RegisterServicesFromAssembly(Assembly.GetExecutingAssembly()))` and scan validators via FluentValidation's `AddValidatorsFromAssembly`. Shared infrastructure (DbContext, auth, logging) lives in `Infrastructure/` — not per-feature. The result: feature cohesion without sacrificing cross-cutting concerns. For greenfield ASP.NET Core 8 projects, vertical slices scale far better than horizontal layering as team size and feature count grow.",
      "code_snippet": "// Features/Articles/CreateArticle/CreateArticleHandler.cs\npublic record CreateArticleCommand(string Title, string Body) : IRequest<ArticleDto>;\n\npublic class CreateArticleHandler(AppDbContext db) : IRequestHandler<CreateArticleCommand, ArticleDto>\n{\n    public async Task<ArticleDto> Handle(CreateArticleCommand cmd, CancellationToken ct)\n    {\n        var article = new Article { Title = cmd.Title, Body = cmd.Body };\n        db.Articles.Add(article);\n        await db.SaveChangesAsync(ct);\n        return new ArticleDto(article.Id, article.Title);\n    }\n}",
      "language": "csharp",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Routing — defining routes, route parameters, grouping and prefixing",
      "status": "done",
      "title": "ASP.NET Core Routing — Parameters, Groups, and Prefix Patterns",
      "body": "ASP.NET Core's minimal API routing maps HTTP verbs directly to delegates via `MapGet`, `MapPost`, etc. Route parameters are declared with `{braces}` and bound automatically to handler parameters by name. Constraints like `{id:int}` or `{slug:regex(^[a-z-]+$)}` are applied inline — no attribute decoration needed.\n\nRoute groups (`MapGroup`) let you share a common prefix and middleware across related endpoints without repetition. Apply `.RequireAuthorization()`, `.WithTags()`, or rate limiting to the group once and every member inherits it. Groups nest cleanly: define a `/api/v1` outer group, then an `/articles` inner group — resulting paths compose automatically.\n\nController-based routing still uses `[Route]` and `[ApiController]` attributes, but minimal APIs are preferred for new vertical slices. Use `WithName()` to assign route names for `TypedResults.Created()` link generation. Catch-all segments use `{**remainder}` syntax. Always prefer constrained parameters over manual parsing inside handlers — the framework rejects non-matching requests before your code runs.",
      "code_snippet": "var api = app.MapGroup(\"/api/v1\").RequireAuthorization();\nvar articles = api.MapGroup(\"/articles\");\n\narticles.MapGet(\"/\", ArticleHandlers.GetAll);\n\narticles.MapGet(\"/{id:guid}\", ArticleHandlers.GetById)\n    .WithName(\"GetArticle\");\n\narticles.MapPost(\"/\", ArticleHandlers.Create)\n    .WithName(\"CreateArticle\")\n    .Produces<ArticleDto>(StatusCodes.Status201Created);\n\narticles.MapGet(\"/by-slug/{slug:regex(^[[a-z0-9-]]+$)}\", ArticleHandlers.GetBySlug);\n\napi.MapGet(\"/users/{userId:int}/feed\", FeedHandlers.GetUserFeed);",
      "language": "csharp",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Dependency Injection — built-in DI containers vs manual wiring, testability impact",
      "status": "done",
      "title": "ASP.NET Core DI — Built-in Container vs Manual Wiring and What It Costs You in Tests",
      "body": "ASP.NET Core's built-in `IServiceCollection` handles constructor injection, lifetime management (Transient/Scoped/Singleton), and scope validation at startup. For most apps, it's enough — and avoiding a third-party container like Autofac removes one moving part from your stack.\n\nThe real testability payoff comes from how you register: prefer `AddScoped<IOrderService, OrderService>()` over `AddScoped<OrderService>()`. Binding to the abstraction means xUnit tests swap in a `Mock<IOrderService>` with zero friction — no `HttpContext`, no `WebApplicationFactory` needed for unit tests.\n\nLifetime mismatches are a silent killer. Injecting a Scoped service into a Singleton causes `ValidateScopes` to throw at startup (enabled by default in Development). Production silently captures stale scope state. Always call `builder.Services.BuildServiceProvider(new ServiceProviderOptions { ValidateScopes = true, ValidateOnBuild = true })` in CI to catch these early.\n\nFor integration tests, `WebApplicationFactory<Program>` lets you call `.WithWebHostBuilder(b => b.ConfigureServices(s => s.AddSingleton<IEmailSender, FakeEmailSender>()))` to override any registration — no reflection hacks required.",
      "code_snippet": "// Program.cs — prefer abstractions, enable validation\nbuilder.Services.AddScoped<IOrderService, OrderService>();\nbuilder.Services.AddSingleton<IEmailSender, SmtpEmailSender>();\n\n// Catch lifetime mismatches in CI\nvar provider = builder.Services.BuildServiceProvider(\n    new ServiceProviderOptions { ValidateScopes = true, ValidateOnBuild = true });\n\n// Integration test override — WebApplicationFactory\nvar factory = new WebApplicationFactory<Program>()\n    .WithWebHostBuilder(b =>\n        b.ConfigureServices(s =>\n            s.AddSingleton<IEmailSender, FakeEmailSender>()));",
      "language": "csharp",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Middleware — request pipeline, global vs route-specific, ordering matters",
      "status": "done",
      "title": "ASP.NET Core Middleware — Pipeline Order, Global vs Route-Specific",
      "body": "ASP.NET Core's request pipeline is a chain of middleware components registered via `app.Use*()` in `Program.cs`. Order is deterministic and critical — each middleware calls `next()` to pass control downstream, then resumes on the way back. Authentication must precede Authorization; logging should wrap everything.\n\nGlobal middleware (registered on `IApplicationBuilder`) runs on every request. For route-scoped behavior, use endpoint filters (`AddEndpointFilter`) or minimal API route groups with their own middleware chain via `MapGroup().Use*()`.\n\nA common mistake: placing `app.UseAuthentication()` after `app.UseAuthorization()` silently breaks auth — claims are never populated when the authorization check runs. Similarly, `app.UseExceptionHandler()` must sit before all other middleware so it can catch downstream exceptions.\n\nUse `app.UseWhen()` for conditional branching without short-circuiting the main pipeline, and `app.Map()` when you need a fully isolated sub-pipeline for a path prefix. Short-circuiting (not calling `next`) terminates the pipeline — intentional in auth failures, catastrophic if done accidentally in logging middleware.",
      "code_snippet": "var app = builder.Build();\n\napp.UseExceptionHandler(\"/error\");       // 1 — outermost\napp.UseHttpsRedirection();               // 2\napp.UseAuthentication();                 // 3 — populate ClaimsPrincipal\napp.UseAuthorization();                  // 4 — evaluate claims\n\n// Route-scoped middleware via MapGroup\nvar api = app.MapGroup(\"/api\")\n    .AddEndpointFilter<RateLimitFilter>(); // only hits /api/**\n\napi.MapGet(\"/bytes\", () => Results.Ok());\n\napp.UseWhen(\n    ctx => ctx.Request.Path.StartsWithSegments(\"/debug\"),\n    branch => branch.UseMiddleware<DiagnosticsMiddleware>()\n);\n\napp.Run();",
      "language": "csharp",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "ORM & Database — query builders vs full ORMs, N+1 prevention, migrations",
      "status": "done",
      "title": "ASP.NET Core EF Core vs Dapper — Picking the Right Tool and Killing N+1",
      "body": "ASP.NET Core gives you two dominant data access paths: EF Core (full ORM) and Dapper (micro-ORM/query builder). EF Core shines for complex domain models, migrations, and change tracking — use it with `DbContext` and model it with owned entities and value objects. Dapper wins for read-heavy endpoints where you want raw SQL performance with `IDbConnection.QueryAsync<T>`.\n\nThe N+1 problem bites hardest with EF Core's lazy loading. Disable it globally (`UseLazyLoadingProxies(false)`) and always eager-load with `.Include()`. For collections across large result sets, swap `.Include()` for `.AsSplitQuery()` to avoid the cartesian explosion that tanks performance on one-to-many joins.\n\nFor migrations, never run `dotnet ef database update` in production CI. Instead, generate idempotent scripts with `dotnet ef migrations script --idempotent` and apply them in your deployment pipeline. Use `MigrationBuilder.Sql()` for custom DDL that EF can't model. Keep migrations small and reversible — each one should have a valid `Down()` method.",
      "code_snippet": "// Eager load with split query to avoid cartesian product\nvar posts = await _db.Posts\n    .Where(p => p.AuthorId == userId)\n    .Include(p => p.Tags)\n    .Include(p => p.Comments)\n    .AsSplitQuery()\n    .AsNoTracking()\n    .ToListAsync();\n\n// Dapper for high-throughput read endpoint\nvar feed = await _conn.QueryAsync<PostDto>(\n    \"SELECT id, title, summary FROM posts WHERE is_published = TRUE ORDER BY created_at DESC LIMIT 20\",\n    commandTimeout: 5);",
      "language": "csharp",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Authentication — JWT validation, session handling, auth middleware patterns",
      "status": "done",
      "title": "ASP.NET Core JWT Validation — Middleware, Claims, and Pitfalls",
      "body": "ASP.NET Core's `AddJwtBearer` extension wires JWT validation into the authentication middleware pipeline. Configure it in `Program.cs` via `AddAuthentication().AddJwtBearer(...)`, setting `TokenValidationParameters` to control what's verified — issuer, audience, lifetime, and signing key. The middleware decodes the token, validates the signature against your `IssuerSigningKey`, and populates `HttpContext.User` with claims automatically before your endpoint logic runs.\n\nUse `[Authorize]` on controllers or map it inline with `.RequireAuthorization()` on minimal API routes. For role-based or policy-based access, define policies in `AddAuthorization(options => options.AddPolicy(...))` and reference them by name.\n\nA common mistake: forgetting to call `app.UseAuthentication()` before `app.UseAuthorization()`. Swapping the order silently breaks claim population — your endpoints see an unauthenticated user even with a valid token.\n\nFor sliding sessions, pair JWT with a refresh token stored in an `HttpOnly` cookie. On expiry, the client hits `/auth/refresh`, which validates the refresh token via a hashed lookup in your DB, then issues a new short-lived JWT. Never extend JWT lifetime as a substitute for proper refresh logic.",
      "code_snippet": "builder.Services\n    .AddAuthentication(JwtBearerDefaults.AuthenticationScheme)\n    .AddJwtBearer(options =>\n    {\n        options.TokenValidationParameters = new TokenValidationParameters\n        {\n            ValidateIssuer = true,\n            ValidateAudience = true,\n            ValidateLifetime = true,\n            ValidateIssuerSigningKey = true,\n            ValidIssuer = builder.Configuration[\"Jwt:Issuer\"],\n            ValidAudience = builder.Configuration[\"Jwt:Audience\"],\n            IssuerSigningKey = new SymmetricSecurityKey(\n                Encoding.UTF8.GetBytes(builder.Configuration[\"Jwt:Key\"]!))\n        };\n    });\n\napp.UseAuthentication();\napp.UseAuthorization();\n\napp.MapGet(\"/me\", (ClaimsPrincipal user) => user.Identity!.Name)\n   .RequireAuthorization();",
      "language": "csharp",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Testing — unit vs integration, mocking HTTP, test database strategy",
      "status": "done",
      "title": "ASP.NET Core Testing — Unit vs Integration, HttpClient Mocking, and Test DB Strategy",
      "body": "ASP.NET Core's WebApplicationFactory<TProgram> is the cornerstone of integration testing — it spins up a real in-memory host with your full middleware pipeline, DI container, and routing, letting you assert end-to-end HTTP behavior without deploying. Unit tests belong to handlers, validators, and pure service logic; keep them fast and dependency-free with Moq or NSubstitute. For outbound HTTP calls, inject IHttpClientFactory and swap in a mock MessageHandler via AddHttpClient().ConfigurePrimaryHttpMessageHandler() in your test — never mock HttpClient directly. For the database, prefer Respawn to reset state between tests against a real PostgreSQL instance rather than EF Core's InMemory provider, which silently ignores relational constraints and gives false confidence. Use a dedicated test connection string via environment variables or appsettings.Testing.json loaded via WebApplicationFactory.WithWebHostBuilder(). Mark integration tests with a custom [Trait(\"Category\", \"Integration\")] and filter them out of your fast CI unit-test run with dotnet test --filter Category!=Integration.",
      "code_snippet": "public class ApiFactory : WebApplicationFactory<Program>\n{\n    protected override void ConfigureWebHost(IWebHostBuilder builder)\n    {\n        builder.ConfigureServices(services =>\n        {\n            services.RemoveAll<DbContextOptions<AppDbContext>>();\n            services.AddDbContext<AppDbContext>(opts =>\n                opts.UseNpgsql(Environment.GetEnvironmentVariable(\"TEST_DB\")));\n\n            services.AddHttpClient<IGitHubClient, GitHubClient>()\n                .ConfigurePrimaryHttpMessageHandler(() => new MockHttpMessageHandler());\n        });\n    }\n}",
      "language": "csharp",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Input Validation — schema validation at the boundary, sanitization, error responses",
      "status": "done",
      "title": "ASP.NET Core Input Validation — Schema Validation at the Boundary, Sanitization, and Error Responses",
      "body": "ASP.NET Core validates request bodies automatically when you annotate your DTOs with Data Annotations and call `builder.Services.AddControllers()` — but that's table stakes. For Minimal APIs, validation doesn't fire automatically; you must invoke it explicitly or wire a filter. Use `IValidator<T>` from FluentValidation for complex rules: it gives you composable, testable validators without polluting your domain models. Register with `builder.Services.AddValidatorsFromAssemblyContaining<Program>()` and inject into endpoint handlers or add a `ValidationFilter<T>` via `app.MapPost(...).AddEndpointFilter<ValidationFilter<CreateByteRequest>>()`. Sanitization is separate from validation — strip HTML at the boundary using `HtmlEncoder.Default.Encode()` or `AngleSharp` for rich content. Never trust `ModelState.IsValid` alone for security-sensitive fields; layer explicit sanitization on top. For error responses, conform to RFC 9457 (Problem Details): ASP.NET Core 8 emits `ProblemDetails` by default when `ApiBehaviorOptions.SuppressModelStateInvalidFilter` is false, giving clients a structured `errors` map with field-level messages. Extend `ValidationProblemDetails` for custom FluentValidation failure projections.",
      "code_snippet": "// Minimal API + FluentValidation + ProblemDetails\napp.MapPost(\"/bytes\", async (CreateByteRequest req, IValidator<CreateByteRequest> validator, IByteService svc) =>\n{\n    var result = await validator.ValidateAsync(req);\n    if (!result.IsValid)\n        return Results.ValidationProblem(result.ToDictionary()); // RFC 9457\n\n    var sanitizedTitle = HtmlEncoder.Default.Encode(req.Title);\n    var created = await svc.CreateAsync(req with { Title = sanitizedTitle });\n    return Results.Created($\"/bytes/{created.Id}\", created);\n});\n\n// FluentValidation rule\npublic class CreateByteRequestValidator : AbstractValidator<CreateByteRequest>\n{\n    public CreateByteRequestValidator()\n    {\n        RuleFor(x => x.Title).NotEmpty().MaximumLength(120);\n        RuleFor(x => x.Body).NotEmpty().Length(50, 300);\n    }\n}",
      "language": "csharp",
      "clarity": 8,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Error Handling — global error handlers, consistent error response format",
      "status": "done",
      "title": "ASP.NET Core Global Error Handling — Consistent ProblemDetails Responses",
      "body": "ASP.NET Core 8 ships with built-in ProblemDetails support (RFC 9457). Wire it up once and every unhandled exception returns a structured JSON error — no per-controller try/catch needed.\n\nRegister `AddProblemDetails()` and `UseExceptionHandler()` in `Program.cs`. The exception handler middleware catches unhandled exceptions, maps them to `ProblemDetails`, and writes the response. For custom mappings, implement `IProblemDetailsWriter` or use `IExceptionHandler` — the cleaner option in .NET 8 — to intercept specific exception types before the fallback kicks in.\n\n`IExceptionHandler` receives the `HttpContext` and the raw exception. Return `true` to short-circuit, `false` to fall through to the next handler. Chain multiple handlers for `ValidationException`, `NotFoundException`, `UnauthorizedAccessException` etc., each setting the appropriate HTTP status and `ProblemDetails.Type` URI.\n\nSet `ASPNETCORE_ENVIRONMENT=Development` to expose `exception` details in the response; in Production they're stripped automatically. Pair with `IProblemDetailsService.WriteAsync()` for manual ProblemDetails responses inside minimal API endpoints or middleware.",
      "code_snippet": "// Program.cs\nbuilder.Services.AddProblemDetails();\nbuilder.Services.AddExceptionHandler<NotFoundExceptionHandler>();\nbuilder.Services.AddExceptionHandler<ValidationExceptionHandler>();\n\napp.UseExceptionHandler();\n\n// NotFoundExceptionHandler.cs\npublic sealed class NotFoundExceptionHandler(IProblemDetailsService pds)\n    : IExceptionHandler\n{\n    public async ValueTask<bool> TryHandleAsync(\n        HttpContext ctx, Exception ex, CancellationToken ct)\n    {\n        if (ex is not NotFoundException) return false;\n        ctx.Response.StatusCode = StatusCodes.Status404NotFound;\n        await pds.WriteAsync(new() { HttpContext = ctx, Exception = ex });\n        return true;\n    }\n}",
      "language": "csharp",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Security — CORS, rate limiting, security headers, injection prevention",
      "status": "done",
      "title": "ASP.NET Core Security — CORS, Rate Limiting, Headers, and Injection in One Pass",
      "body": "Lock down your ASP.NET Core 8 API by layering four defenses. CORS: call `builder.Services.AddCors()` with a named policy, restrict `WithOrigins()` to known domains, and never use `AllowAnyOrigin()` with `AllowCredentials()` — it throws at startup. Rate limiting: use `AddRateLimiter()` with a fixed-window or token-bucket policy and apply `[EnableRateLimiting(\"policy-name\")]` per endpoint or globally via `app.UseRateLimiter()`. Security headers: ASP.NET Core doesn't ship these by default — use `app.Use()` middleware to set `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, and `Content-Security-Policy`. For injection prevention, never interpolate user input into raw SQL — use EF Core's parameterized LINQ or `FromSqlInterpolated()` (safe) over `FromSqlRaw()` with string concat (dangerous). Bind input through model binding with `[ApiController]` to get automatic `ModelState` validation, and annotate DTOs with `[MaxLength]` and `[RegularExpression]` to reject malformed data before it reaches your business logic.",
      "code_snippet": "builder.Services.AddCors(o => o.AddPolicy(\"Api\", p =>\n    p.WithOrigins(\"https://app.example.com\")\n     .WithMethods(\"GET\", \"POST\")\n     .AllowCredentials()));\n\nbuilder.Services.AddRateLimiter(o => o.AddFixedWindowLimiter(\"default\", opts => {\n    opts.Window = TimeSpan.FromMinutes(1);\n    opts.PermitLimit = 100;\n}));\n\napp.Use(async (ctx, next) => {\n    ctx.Response.Headers[\"X-Content-Type-Options\"] = \"nosniff\";\n    ctx.Response.Headers[\"X-Frame-Options\"] = \"DENY\";\n    ctx.Response.Headers[\"Content-Security-Policy\"] = \"default-src 'self'\";\n    await next();\n});\n\napp.UseCors(\"Api\");\napp.UseRateLimiter();",
      "language": "csharp",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Performance — connection pooling, async handlers, response compression",
      "status": "done",
      "title": "ASP.NET Core Performance — Connection Pooling, Async Handlers, and Response Compression",
      "body": "Three settings that meaningfully change throughput in ASP.NET Core apps often go untouched after scaffolding. First, Npgsql's connection pool defaults (`MaxPoolSize=100`) are usually too conservative for high-concurrency APIs — bump it via the connection string and configure `NpgsqlDataSourceBuilder` for named pools per tenant. Second, every Minimal API or controller action that touches I/O must be `async Task<IResult>` — synchronous handlers block Kestrel's thread pool under load; there's no automatic offloading. Third, `app.UseResponseCompression()` is opt-in. Register `AddResponseCompression` with Brotli first, gzip as fallback, and set `EnableForHttps = true` only after confirming your deployment terminates TLS at the app level, not a reverse proxy. Stack all three with `app.UseResponseCaching()` and `OutputCache` middleware for endpoints with stable payloads. Benchmark with `bombardier` or `k6` before and after — the async fix alone typically cuts p99 latency by 30–60% on DB-heavy routes.",
      "code_snippet": "// Program.cs\nbuilder.Services.AddResponseCompression(opts =>\n{\n    opts.EnableForHttps = false; // true only if Kestrel terminates TLS\n    opts.Providers.Add<BrotliCompressionProvider>();\n    opts.Providers.Add<GzipCompressionProvider>();\n});\n\nbuilder.Services.AddNpgsqlDataSource(\n    builder.Configuration.GetConnectionString(\"Postgres\")!,\n    b => b.ConnectionStringBuilder.MaxPoolSize = 200);\n\nvar app = builder.Build();\napp.UseResponseCompression();\n\napp.MapGet(\"/bytes\", async (NpgsqlDataSource ds) =>\n{\n    await using var conn = await ds.OpenConnectionAsync();\n    // async I/O — never block here\n    return Results.Ok();\n});",
      "language": "csharp",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Serialization — request/response DTOs, camelCase vs snake_case, null handling",
      "status": "done",
      "title": "ASP.NET Core Serialization — DTOs, camelCase, and Null Handling Done Right",
      "body": "ASP.NET Core's `System.Text.Json` serializer defaults to preserving property name casing, so a C# `UserId` property serializes as `UserId`, not `userId`. Fix this globally in `Program.cs` via `builder.Services.ConfigureHttpJsonOptions(o => o.SerializerOptions.PropertyNamingPolicy = JsonNamingPolicy.CamelCase)`. For snake_case (common in Python-facing APIs), use `JsonNamingPolicy.SnakeCaseLower` (.NET 8+). Always use dedicated request/response DTOs — never expose your EF Core entities directly. Decorate DTOs with `[JsonPropertyName]` when a single property needs a different name than the global policy. For null handling, set `DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull` to strip null fields from responses, reducing payload size. On input, `[Required]` + nullable reference types (`string?`) let the model binder distinguish missing from explicitly-null. Pair this with `[ApiController]` so missing required fields auto-return `400` without manual `ModelState` checks. For versioned APIs where field presence semantics matter, prefer `JsonIgnoreCondition.Never` and explicit null to avoid ambiguity between absent and null.",
      "code_snippet": "// Program.cs\nbuilder.Services.ConfigureHttpJsonOptions(options =>\n{\n    var json = options.SerializerOptions;\n    json.PropertyNamingPolicy = JsonNamingPolicy.CamelCase;\n    json.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;\n});\n\n// Response DTO\npublic record UserResponse(\n    int Id,\n    string DisplayName,\n    [property: JsonPropertyName(\"avatar_url\")] string? AvatarUrl\n);\n\n// Request DTO\npublic record CreateUserRequest(\n    [Required] string DisplayName,\n    string? Bio\n);",
      "language": "csharp",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Configuration — env vars, settings classes, secrets management",
      "status": "done",
      "title": "ASP.NET Core Configuration — Env Vars, Typed Settings, and Secrets Done Right",
      "body": "ASP.NET Core's configuration system layers sources in priority order: appsettings.json → appsettings.{Environment}.json → environment variables → command-line args. Environment variables override file-based config automatically — no extra code needed. Bind sections to strongly-typed classes using the Options pattern: register with `builder.Services.Configure<MySettings>(builder.Configuration.GetSection(\"MySettings\"))`, then inject `IOptions<MySettings>` or `IOptionsSnapshot<MySettings>` (reloads on change). For secrets, never commit sensitive values. In development, use `dotnet user-secrets init` and `dotnet user-secrets set \"MySettings:ApiKey\" \"abc123\"` — stored in `~/.microsoft/usersecrets/`. In production, prefer Azure Key Vault via `builder.Configuration.AddAzureKeyVault(...)` or environment variables injected by your container orchestrator. Validate options at startup using `ValidateDataAnnotations()` and `ValidateOnStart()` to catch missing secrets before the app serves traffic — not at runtime when a request fails.",
      "code_snippet": "// Program.cs\nbuilder.Services.Configure<StripeSettings>(\n    builder.Configuration.GetSection(\"Stripe\"))\n    .ValidateDataAnnotations()\n    .ValidateOnStart();\n\n// StripeSettings.cs\npublic class StripeSettings\n{\n    [Required] public string SecretKey { get; init; } = default!;\n    [Required] public string WebhookSecret { get; init; } = default!;\n}\n\n// Usage in service\npublic class PaymentService(IOptions<StripeSettings> opts)\n{\n    private readonly StripeSettings _stripe = opts.Value;\n}",
      "language": "csharp",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "API Design — REST conventions, versioning strategy, OpenAPI generation",
      "status": "done",
      "title": "ASP.NET Core API Versioning — REST Conventions, Route Strategy, and OpenAPI in One Setup",
      "body": "ASP.NET Core's `Asp.Versioning.Http` package gives you URL-segment, query-string, and header versioning with minimal config. The idiomatic approach pairs `MapGroup` with `AddApiVersioning` and `AddApiExplorer` to generate per-version OpenAPI documents via Swashbuckle or the built-in `Microsoft.AspNetCore.OpenApi` (introduced in .NET 9). Register versioning once in `Program.cs`, then decorate each `RouteGroupBuilder` with `.HasApiVersion(1, 0)`. For REST conventions: use plural nouns (`/bytes`, not `/getBytes`), map HTTP verbs semantically (`POST` creates, `PUT` full-replace, `PATCH` partial), and return `201 Created` with a `Location` header on resource creation via `TypedResults.Created`. Deprecate old versions by tagging `.HasDeprecatedApiVersion(1, 0)` — the `api-deprecated-version` response header signals consumers automatically. OpenAPI metadata flows through `WithOpenApi()` on each endpoint, letting you annotate summaries and examples inline without XML doc comments. Combine this with `[Produces]`/`[Consumes]` attributes on groups to enforce consistent content negotiation across all routes in a version.",
      "code_snippet": "var builder = WebApplication.CreateBuilder(args);\nbuilder.Services.AddApiVersioning(o => {\n    o.DefaultApiVersion = new ApiVersion(1, 0);\n    o.AssumeDefaultVersionWhenUnspecified = true;\n    o.ReportApiVersions = true;\n}).AddApiExplorer(o => {\n    o.GroupNameFormat = \"'v'VVV\";\n    o.SubstituteApiVersionInUrl = true;\n});\nbuilder.Services.AddOpenApi();\nvar app = builder.Build();\nvar v1 = app.NewVersionedApi(\"Bytes\")\n    .MapGroup(\"/api/v{version:apiVersion}/bytes\")\n    .HasApiVersion(1, 0)\n    .WithOpenApi();\nv1.MapGet(\"/\", () => Results.Ok()).WithSummary(\"List bytes\");\nv1.MapPost(\"/\", (CreateByteRequest req) =>\n    TypedResults.Created($\"/api/v1/bytes/1\", req));\napp.MapOpenApi();\napp.Run();",
      "language": "csharp",
      "clarity": 8,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Logging — structured logs, correlation IDs, request logging middleware",
      "status": "done",
      "title": "ASP.NET Core Logging — Structured Logs, Correlation IDs, and Request Middleware",
      "body": "ASP.NET Core's built-in `ILogger<T>` supports structured logging out of the box. Instead of string interpolation, use message templates so log sinks (Serilog, OpenTelemetry) can index named properties: `_logger.LogInformation(\"Order {OrderId} placed by {UserId}\", orderId, userId)`. This keeps logs queryable in tools like Seq or Grafana Loki.\n\nFor distributed tracing, inject a correlation ID on every request using middleware. Read `X-Correlation-ID` from the incoming header (or generate a new `Guid`), push it into `ILogger`'s ambient scope via `BeginScope`, and echo it back in the response header. Serilog's `LogContext.PushProperty` works identically.\n\nRequest logging middleware — either Serilog's `UseSerilogRequestLogging()` or a custom `IMiddleware` — gives you structured per-request entries (method, path, status, elapsed ms) without ASP.NET Core's noisy default host logs. Configure `appsettings.json` to suppress `Microsoft.AspNetCore` at Warning level while keeping your app namespace at Information, so production logs stay clean and actionable.",
      "code_snippet": "// Program.cs\napp.Use(async (ctx, next) =>\n{\n    var correlationId = ctx.Request.Headers[\"X-Correlation-ID\"].FirstOrDefault()\n                        ?? Guid.NewGuid().ToString();\n    using (logger.BeginScope(new Dictionary<string, object>\n        { [\"CorrelationId\"] = correlationId }))\n    {\n        ctx.Response.Headers[\"X-Correlation-ID\"] = correlationId;\n        await next();\n    }\n});\n\n// Suppress noisy host logs, keep app logs\nbuilder.Logging.AddFilter(\"Microsoft.AspNetCore\", LogLevel.Warning);\nbuilder.Logging.AddFilter(\"YourAppNamespace\", LogLevel.Information);",
      "language": "csharp",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Dev Tooling — hot reload, code generation, scaffolding CLI",
      "status": "done",
      "title": "ASP.NET Core Dev Tooling — Hot Reload, Scaffolding, and dotnet-aspnet-codegenerator",
      "body": "ASP.NET Core's `dotnet watch` enables Hot Reload without restarting the process — file saves trigger a delta recompile and patch the running IL. For most controller/middleware edits, the app state persists. Rude edits (constructor changes, attribute additions) force a full restart, signaled in the console. Tune behavior in `launchSettings.json` via `\"hotReloadEnabled\": true` or pass `--no-hot-reload` when profiling.\n\nFor scaffolding, `dotnet-aspnet-codegenerator` generates controllers, Razor pages, and EF Core DbContexts from existing models. Install it once per machine: `dotnet tool install -g dotnet-aspnet-codegenerator`. Target a model and DbContext to emit a fully wired Minimal API or MVC controller with CRUD actions — saving 40+ lines of boilerplate per resource.\n\nThe `dotnet new` CLI ships first-class templates for Web API, gRPC, SignalR hubs, and Blazor. Custom templates via `dotnet new install <package>` let teams enforce internal conventions — authentication wiring, OpenTelemetry setup, health check endpoints — across every new service from day one.",
      "code_snippet": "# Install scaffolding tool globally\ndotnet tool install -g dotnet-aspnet-codegenerator\n\n# Scaffold an API controller for a Product model using AppDbContext\ndotnet aspnet-codegenerator controller \\\n  -name ProductsController \\\n  -async \\\n  -api \\\n  -m Product \\\n  -dc AppDbContext \\\n  -outDir Controllers\n\n# Run with hot reload enabled (default in dotnet watch)\ndotnet watch run --project ./MyApi/MyApi.csproj",
      "language": "bash",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Monitoring — health check endpoints, metrics exposure, distributed tracing",
      "status": "done",
      "title": "ASP.NET Core Monitoring — Health Checks, Metrics, and Distributed Tracing in One Pass",
      "body": "ASP.NET Core 8 ships three observability primitives out of the box. Health checks via `AddHealthChecks()` let you register named checks — database connectivity, external APIs, disk space — and expose them at `/health` with granular `HealthStatus` responses. Tag filters let k8s liveness and readiness probes hit separate subsets. For metrics, `AddOpenTelemetryMetrics()` wires into the built-in `System.Diagnostics.Metrics` API; the ASP.NET Core instrumentation library auto-emits request duration, active connections, and queue depth histograms — push them to Prometheus via `OpenTelemetry.Exporter.Prometheus.AspNetCore`. Distributed tracing uses `ActivitySource` and propagates W3C `traceparent` headers automatically through `HttpClient` and EF Core when you add `AddAspNetCoreInstrumentation()` and `AddEntityFrameworkCoreInstrumentation()`. All three pillars share one `IServiceCollection` registration pattern and export to the same OpenTelemetry collector, keeping your `Program.cs` composable. Pair with `dotnet-monitor` sidecar for on-demand dumps without redeployment.",
      "code_snippet": "var builder = WebApplication.CreateBuilder(args);\n\nbuilder.Services.AddHealthChecks()\n    .AddNpgsql(builder.Configuration[\"ConnectionStrings:Default\"]!, tags: [\"ready\"]);\n\nbuilder.Services.AddOpenTelemetry()\n    .WithMetrics(m => m\n        .AddAspNetCoreInstrumentation()\n        .AddPrometheusExporter())\n    .WithTracing(t => t\n        .AddAspNetCoreInstrumentation()\n        .AddEntityFrameworkCoreInstrumentation()\n        .AddOtlpExporter());\n\nvar app = builder.Build();\napp.MapHealthChecks(\"/health/live\");\napp.MapHealthChecks(\"/health/ready\", new() { Predicate = c => c.Tags.Contains(\"ready\") });\napp.MapPrometheusScrapingEndpoint();\napp.Run();",
      "language": "csharp",
      "clarity": 8,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Background Jobs — task queues, scheduled jobs, worker process patterns",
      "status": "done",
      "title": "ASP.NET Core Background Jobs — BackgroundService, Channels, and Worker Patterns",
      "body": "ASP.NET Core's `BackgroundService` (via `IHostedService`) is the backbone for long-running work. Inherit it, implement `ExecuteAsync(CancellationToken)`, and register with `builder.Services.AddHostedService<T>()`. For task queues, pair it with `System.Threading.Channels.Channel<T>` — a high-performance, built-in producer/consumer primitive. Your API enqueues work via `ChannelWriter<T>`, and your `BackgroundService` dequeues and processes via `ChannelReader<T>.ReadAllAsync()`. This avoids thread-unsafe `ConcurrentQueue` hacks and integrates naturally with `CancellationToken` propagation.\n\nFor scheduled jobs, ASP.NET Core has no built-in scheduler — use `PeriodicTimer` (introduced in .NET 6) inside `ExecuteAsync` instead of `Task.Delay` loops; it skips missed ticks cleanly. For production-grade scheduling with persistence and retries, Hangfire or Quartz.NET integrate cleanly via their ASP.NET Core packages.\n\nFor heavy workloads, extract the `BackgroundService` into a standalone Worker Service project (`dotnet new worker`) running as an Azure Container App job. It shares the same `IHostedService` model but decouples CPU-bound processing from your web API's request pipeline, preventing resource starvation.",
      "code_snippet": "// Channel-backed task queue with BackgroundService\nvar channel = Channel.CreateBounded<WorkItem>(new BoundedChannelOptions(100) {\n    FullMode = BoundedChannelFullMode.Wait\n});\nbuilder.Services.AddSingleton(channel.Writer);\nbuilder.Services.AddSingleton(channel.Reader);\nbuilder.Services.AddHostedService<TaskQueueProcessor>();\n\npublic class TaskQueueProcessor(ChannelReader<WorkItem> reader) : BackgroundService\n{\n    protected override async Task ExecuteAsync(CancellationToken ct)\n    {\n        await foreach (var item in reader.ReadAllAsync(ct))\n            await ProcessAsync(item, ct);\n    }\n}",
      "language": "csharp",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Framework-specific — Django batteries-included, FastAPI auto-docs, NestJS decorators, Phoenix LiveView",
      "status": "done",
      "title": "ASP.NET Core's Built-In Batteries: What You're Already Shipping Without a Package",
      "body": "Developers switching from Django, FastAPI, or NestJS often reach for third-party packages to replicate familiar features — but ASP.NET Core already ships most of them. FastAPI's auto-docs? Add `builder.Services.AddOpenApi()` and `app.MapOpenApi()` in .NET 9 — zero Swashbuckle required. NestJS-style decorators map directly to ASP.NET Core attributes: `[Authorize]`, `[RequiredScope]`, `[OutputCache]`, `[RateLimiting]` compose cleanly on Minimal API endpoints or controllers. Phoenix LiveView's server-driven reactivity is Blazor Server over SignalR — components re-render server-side, DOM diffs stream to the client. For Django's batteries-included feel, ASP.NET Core ships `IConfiguration` (layered config with env overrides), `IMemoryCache`, built-in health checks via `AddHealthChecks()`, and `IHttpClientFactory` with polly-backed resilience. The trap is reaching for NuGet when the `Microsoft.AspNetCore.*` namespace already has what you need. Run `dotnet add package` only after checking `WebApplication.CreateBuilder(args)` first.",
      "code_snippet": "var builder = WebApplication.CreateBuilder(args);\n\nbuilder.Services.AddOpenApi();                          // FastAPI-style auto-docs\nbuilder.Services.AddOutputCache();                      // Built-in response caching\nbuilder.Services.AddRateLimiter(o => o.AddFixedWindowLimiter(\"api\", c => {\n    c.Window = TimeSpan.FromMinutes(1);\n    c.PermitLimit = 100;\n}));\nbuilder.Services.AddHealthChecks().AddNpgSql(connStr);  // DB health endpoint\n\nvar app = builder.Build();\napp.MapOpenApi();                                       // GET /_openapi/v1.json\napp.MapGet(\"/data\", () => Results.Ok())\n   .RequireAuthorization()\n   .RequireRateLimiting(\"api\")\n   .CacheOutput(p => p.Expire(TimeSpan.FromSeconds(30)));\n\napp.Run();",
      "language": "csharp",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    }
  ]
}