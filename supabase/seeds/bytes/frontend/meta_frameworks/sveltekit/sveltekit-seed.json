{
  "tech_stack": "sveltekit",
  "subdomain": "meta_frameworks",
  "domain": "frontend",
  "bytes": [
    {
      "topic": "Architecture & Structure — file-based routing as architecture, route-level colocation of loader/error/loading",
      "status": "done",
      "title": "SvelteKit File-Based Routing Is Your Architecture — Not Just Your URL Map",
      "body": "Every directory under `src/routes/` in SvelteKit is a self-contained feature unit, not just a URL segment. The folder declares its own data contract (`+page.server.ts`), error boundary (`+error.svelte`), and layout inheritance (`+layout.svelte`) — the filesystem is your architecture.\n\nThe `load` function in `+page.server.ts` runs exclusively on the server, returning a typed `PageData` object inferred from `./$types`. Throw SvelteKit's `error()` helper inside `load` and the nearest `+error.svelte` up the route tree catches it automatically — no try/catch in the component.\n\nSvelteKit has no `+loading.svelte`. Instead, return an unresolved `Promise` from `load` and use `{#await}` in `+page.svelte` for streamed, progressive rendering — the page shell arrives immediately while deferred data resolves.\n\nRoute groups like `(auth)/` let you wrap routes in a shared `+layout.svelte` (e.g., auth guard, different shell) without polluting the URL. The group name is invisible to the router.\n\nThe result: open any route folder and instantly know what data it fetches, how it fails, and how it renders — zero cross-file archaeology required.",
      "code_snippet": "// src/routes/dashboard/+page.server.ts\nimport { error } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ locals }) => {\n  if (!locals.user) throw error(401, 'Unauthorized');\n\n  return {\n    profile: locals.user,\n    stats: fetchStats(locals.user.id), // unresolved Promise → streamed\n  };\n};\n\n// src/routes/dashboard/+page.svelte\n// {#await data.stats}<Skeleton />{:then s}<Stats data={s} />{/await}",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Component Design — Server Components vs Client Components, push use client to the leaves",
      "status": "done",
      "title": "SvelteKit Component Design — Keep the Server at the Root, Push Interactivity to the Leaves",
      "body": "SvelteKit doesn't have a `use client` directive, but the same architectural principle applies: load data on the server, hydrate only where interactivity is needed. Your `+page.server.ts` is your server boundary — it runs exclusively on the server and its return value flows as props into your component tree. The mistake is reaching for `onMount` or reactive stores at the layout or page level when the data could simply arrive from `load()`. Structure your tree so `+page.server.ts` owns all data fetching, `+page.svelte` is a thin shell that distributes props, and only leaf components — a `<LikeButton>`, a `<CommentInput>` — wire up event handlers, `writable` stores, or `onMount` for client-only effects. This lets SvelteKit stream and prerender the outer shell while deferring hydration cost to small, focused components. Guard any truly client-only initialization with `if (browser)` from `$app/environment` rather than sprinkling `onMount` everywhere. The result: faster TTI, smaller hydration surface, and a clear mental model of what runs where.",
      "code_snippet": "// +page.server.ts — server boundary, zero client JS\nexport const load = async ({ locals }) => {\n  const posts = await db.query('SELECT * FROM posts ORDER BY created_at DESC');\n  return { posts };\n};\n\n// +page.svelte — thin shell, no onMount, no stores\n<script lang=\"ts\">\n  import LikeButton from '$lib/LikeButton.svelte'; // leaf: has interactivity\n  const { data } = $props();\n</script>\n\n{#each data.posts as post}\n  <article>\n    <h2>{post.title}</h2>\n    <LikeButton postId={post.id} initialCount={post.likes} />\n  </article>\n{/each}\n\n// LikeButton.svelte — leaf component, client interactivity lives here\n<script lang=\"ts\">\n  import { browser } from '$app/environment';\n  let { postId, initialCount } = $props();\n  let count = $state(initialCount);\n  const like = async () => { count++; await fetch(`/api/posts/${postId}/like`, { method: 'POST' }); };\n</script>\n<button onclick={like}>{count} Likes</button>",
      "language": "typescript",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "State Management — URL as state for filters/pagination, server state in loaders not useState",
      "status": "done",
      "title": "SvelteKit URL State — Filters and Pagination Belong in the URL, Not $state",
      "body": "SvelteKit's `load()` functions are the right home for server-derived data — not `$state` runes or writable stores. For filters and pagination, encode them in `url.searchParams` and let `load()` react automatically. SvelteKit re-runs any `load()` that reads `url.searchParams` whenever those params change, so `goto()` becomes your state setter — no manual invalidation needed.\n\nIn `+page.server.ts`, read `url.searchParams` directly and return typed data. The `data` prop in `+page.svelte` updates reactively on every navigation. When a filter changes, call `goto()` with an updated `URL` object. Pass `noScroll: true` and `keepFocus: true` to prevent jarring resets on filter interactions.\n\nAvoid mirroring `data.results` into a `$state` variable — that creates a second source of truth and silently breaks re-runs. If you need imperative invalidation (e.g., after a mutation), call `invalidate('app:results')` paired with `depends('app:results')` in `load()`, not a local state copy.\n\nThis pattern gives you bookmarkable, shareable URLs, free browser history navigation through filter states, and zero client/server sync bugs — all without a single `$state` declaration for server data.",
      "code_snippet": "// +page.server.ts\nexport const load = async ({ url }) => {\n  const q = url.searchParams.get('q') ?? '';\n  const page = Number(url.searchParams.get('page') ?? 1);\n  const results = await db.search(q, page);\n  return { results, q, page };\n};\n\n// +page.svelte\n<script lang=\"ts\">\n  import { goto } from '$app/navigation';\n  import { page } from '$app/stores';\n  let { data } = $props();\n\n  function filter(q: string) {\n    const url = new URL($page.url);\n    url.searchParams.set('q', q);\n    url.searchParams.set('page', '1');\n    goto(url, { keepFocus: true, noScroll: true });\n  }\n</script>",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Performance — SSG vs SSR vs ISR decision matrix, parallel data fetching in loaders",
      "status": "done",
      "title": "SvelteKit SSG vs SSR vs ISR — Decision Matrix and Parallel Loader Performance",
      "body": "SvelteKit's rendering mode is controlled by exports in `+page.ts` or `+page.server.ts`, not config files. Use `export const prerender = true` for SSG — correct for docs, marketing, or any route with zero per-request variance. Default SSR runs your `load` on every request; keep it for auth-gated, personalized, or real-time pages. ISR isn't native to SvelteKit's core, but `adapter-vercel` exposes `export const config = { isr: { expiration: 60 } }` per-route for edge revalidation. For self-hosted deploys, set `Cache-Control: stale-while-revalidate` in your `handle` hook instead.\n\nThe highest-yield SSR optimization is eliminating waterfall fetches inside `load`. SvelteKit lets you return unawaited promises — the runtime streams them to the client, enabling `{#await}` blocks to resolve independently. For server-only parallel fetches, use `Promise.all` over SvelteKit's injected `fetch`, not `globalThis.fetch`. The injected version forwards cookies automatically, deduplicates identical requests within the same render cycle, and resolves relative URLs against the origin — none of which global fetch does in a Node SSR context.",
      "code_snippet": "// +page.server.ts\nimport type { PageServerLoad } from './$types';\n\n// SSG: export const prerender = true;\n// ISR (Vercel only): export const config = { isr: { expiration: 60 } };\n\nexport const load: PageServerLoad = async ({ fetch }) => {\n  // Return promises unresolved — SvelteKit streams each independently\n  return {\n    posts:    fetch('/api/posts').then(r => r.json()),\n    trending: fetch('/api/trending').then(r => r.json()),\n  };\n};",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Data Fetching — route loaders fetch before render, eliminate waterfall, no fetch inside components",
      "status": "done",
      "title": "SvelteKit Route Loaders — Kill the Waterfall Before the Component Renders",
      "body": "SvelteKit's `load` function in `+page.server.ts` runs on the server before your component mounts — data arrives with the HTML, not after. This eliminates the fetch-on-mount waterfall that plagues component-level data fetching. If a page needs a post and its author, fire both requests in parallel inside a single `load` using Promise.all or by returning multiple awaited calls — SvelteKit resolves them concurrently. The typed `data` prop lands in `+page.svelte` fully resolved, no loading spinners required for initial paint. Nest `+layout.server.ts` loaders for shared data like auth state; child `load` functions run in parallel with their parent layout loaders automatically. SvelteKit's enhanced `fetch` inside `load` is credential-forwarded, SSR-safe, and deduplicated — never reach for `fetch` inside `onMount` or `<script>` blocks for route-critical data. For client-side navigations, loaders re-run transparently via `invalidate()` or `invalidateAll()`, giving you fine-grained cache control without a state library.",
      "code_snippet": "// src/routes/posts/[id]/+page.server.ts\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ params, fetch }) => {\n  const [post, author] = await Promise.all([\n    fetch(`/api/posts/${params.id}`).then(r => r.json()),\n    fetch(`/api/users/${params.id}/author`).then(r => r.json()),\n  ]);\n\n  return { post, author };\n};\n\n// src/routes/posts/[id]/+page.svelte\n<script lang=\"ts\">\n  let { data } = $props();\n</script>\n<h1>{data.post.title}</h1>\n<p>by {data.author.name}</p>",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Forms — Server Actions, progressive enhancement, form validation on server boundary",
      "status": "done",
      "title": "SvelteKit Forms — Server Actions, Progressive Enhancement, and Server-Side Validation",
      "body": "SvelteKit's form actions run exclusively on the server via the `actions` export in `+page.server.ts`. Your `<form>` POSTs to the same route by default — no `fetch`, no API endpoint needed. Validation happens in the action using `request.formData()`, and failures are returned with `fail(422, { errors })`, which SvelteKit sends back as `ActionData`. Bind it in `+page.svelte` via the `form` prop (typed as `ActionData`). This works with zero JavaScript — true progressive enhancement. Add `use:enhance` from `$app/forms` to intercept the submission client-side, skip the full-page reload, and still receive the same `ActionData` back. The `enhance` callback gives you fine-grained control: optimistic UI, custom loading states, or preventing the default update entirely. Crucially, `fail()` sets the HTTP status without throwing, so SvelteKit keeps the form data in scope for re-population. Never use `throw redirect()` on validation failure — that drops the error payload. Server actions also automatically protect against CSRF via SvelteKit's built-in origin check, so form submissions from foreign origins are rejected without any extra middleware.",
      "code_snippet": "// +page.server.ts\nimport { fail } from '@sveltejs/kit';\nimport type { Actions } from './$types';\n\nexport const actions: Actions = {\n  default: async ({ request }) => {\n    const data = await request.formData();\n    const email = data.get('email')?.toString().trim();\n\n    if (!email || !email.includes('@')) {\n      return fail(422, { email, errors: { email: 'Valid email required' } });\n    }\n\n    await subscribeUser(email);\n  }\n};\n\n// +page.svelte\n<script lang=\"ts\">\n  import { enhance } from '$app/forms';\n  import type { ActionData } from './$types';\n  let { form }: { form: ActionData } = $props();\n</script>\n\n<form method=\"POST\" use:enhance>\n  <input name=\"email\" value={form?.email ?? ''} />\n  {#if form?.errors?.email}<p>{form.errors.email}</p>{/if}\n  <button>Subscribe</button>\n</form>",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "TypeScript — typed route params, typed loaders, typed search params with Zod",
      "status": "done",
      "title": "SvelteKit — Typed Route Params, Loaders, and Search Params with Zod",
      "body": "SvelteKit's generated `$types` module gives you `PageLoad`, `PageServerLoad`, and `RouteParams` — but these are structural types, not validated at runtime. Combine them with Zod to get both compile-time safety and runtime validation in a single pass.\n\nIn `+page.server.ts`, destructure `params` typed via `PageServerLoad` and parse with a Zod schema. For search params, SvelteKit passes them through `url.searchParams` — not typed by default. Zod's `z.coerce` handles the string-to-number conversion that `URLSearchParams` always returns.\n\nThe key SvelteKit-specific detail: `load` functions run both on the server and client during SSR hydration. A Zod `safeParse` lets you throw a `redirect` or `error` (from `@sveltejs/kit`) cleanly inside `load` without leaking stack traces to the client. Typed returns from `load` flow into `+page.svelte` via the `data` prop, which `PageData` narrows automatically — no casting needed downstream.",
      "code_snippet": "// src/routes/tools/[slug]/+page.server.ts\nimport { error } from '@sveltejs/kit';\nimport { z } from 'zod';\nimport type { PageServerLoad } from './$types';\n\nconst paramsSchema = z.object({ slug: z.string().min(1) });\nconst searchSchema = z.object({\n  page: z.coerce.number().int().positive().default(1),\n  q: z.string().optional(),\n});\n\nexport const load: PageServerLoad = ({ params, url }) => {\n  const { slug } = paramsSchema.parse(params);\n  const search = searchSchema.safeParse(\n    Object.fromEntries(url.searchParams)\n  );\n  if (!search.success) throw error(400, 'Invalid query params');\n  return { slug, ...search.data };\n};",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Error Handling — error.tsx / +error.svelte per route segment, notFound(), redirect()",
      "status": "done",
      "title": "SvelteKit Error Handling — +error.svelte Boundaries, notFound(), and redirect()",
      "body": "SvelteKit's error model is route-segment-scoped. Drop a `+error.svelte` next to any `+page.svelte` or `+layout.svelte` and it catches errors thrown within that segment — without taking down the surrounding shell. If a segment lacks one, the error bubbles to the nearest ancestor `+error.svelte`, ultimately falling back to `src/routes/+error.svelte`.\n\nThrow structured errors in `load` or form actions using `error(status, message)` from `@sveltejs/kit`. Use `notFound()` as a semantic shorthand for `error(404)`, and `redirect(303, '/path')` for post-auth or post-mutation flows — both are `throw`-based, so SvelteKit intercepts them before your `return` is ever evaluated.\n\nInside `+error.svelte`, `$page.error` exposes the thrown object and `$page.status` gives the HTTP code — no prop drilling, no try/catch in the component. The component still inherits parent layout data via `$page.data`, so your nav shell stays intact on error pages.\n\nOne footgun: errors thrown outside `load` — e.g., in a module-level utility — bypass the boundary entirely and surface as unhandled exceptions. Keep all throwing inside `load`, `actions`, or `hooks.server.ts` `handle`.",
      "code_snippet": "// src/routes/posts/[slug]/+page.server.ts\nimport { error, redirect, notFound } from '@sveltejs/kit';\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = async ({ params, locals }) => {\n  if (!locals.user) redirect(303, '/login');\n\n  const post = await db.posts.findUnique({ where: { slug: params.slug } });\n  if (!post) notFound();\n  if (post.draft && !locals.user.isAdmin) error(403, 'Forbidden');\n\n  return { post };\n};\n\n// src/routes/posts/[slug]/+error.svelte\n// <script>\n//   import { page } from '$app/stores';\n// </script>\n// <h1>{$page.status}</h1>\n// <p>{$page.error?.message}</p>",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Testing — test loaders and actions in isolation, Playwright for full route flows",
      "status": "done",
      "title": "SvelteKit Testing — Isolate Loaders & Actions, Then Verify Full Flows with Playwright",
      "body": "SvelteKit's `load` functions and form actions are pure-ish functions — test them directly without spinning up a server. Import your `+page.server.ts` loader and call it with a mock `RequestEvent`. Assert on the returned data shape. For actions, call the named export (e.g. `actions.default`) with a fake `request` containing `FormData`. No `fetch` mocking ceremony needed for most cases — just construct the event object.\n\nFor full route integration, Playwright's `page.goto('/dashboard')` exercises the actual SvelteKit router, SSR, and hydration. Use `page.waitForSelector` after navigation to assert hydrated state, not just SSR HTML. For authenticated routes, seed cookies via `context.addCookies` before the test — Clerk or any JWT-based session can be injected this way.\n\nCombine both layers: unit tests catch broken loader logic fast; Playwright catches broken routing, redirects (`throw redirect(302, '/login')`), and form action side-effects that only surface end-to-end. Skip testing `+layout.server.ts` in isolation unless it contains non-trivial logic — the E2E layer covers it implicitly.",
      "code_snippet": "// +page.server.test.ts\nimport { load, actions } from './+page.server';\nimport { describe, it, expect } from 'vitest';\n\ndescribe('dashboard loader', () => {\n  it('returns user data for authenticated event', async () => {\n    const event = {\n      locals: { user: { id: '42', role: 'admin' } },\n      url: new URL('http://localhost/dashboard'),\n    } as any;\n\n    const result = await load(event);\n    expect(result.user.role).toBe('admin');\n  });\n\n  it('action rejects empty form submission', async () => {\n    const formData = new FormData();\n    const request = new Request('http://localhost', { method: 'POST', body: formData });\n    const response = await actions.default({ request } as any);\n    expect(response?.status).toBe(400);\n  });\n});",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Security — Server Actions as trust boundary, validate all input, env var separation",
      "status": "done",
      "title": "SvelteKit Server Actions — Every formData Value Is Hostile Until Proven Otherwise",
      "body": "SvelteKit's `actions` export in `+page.server.ts` runs server-side only, but network reachability doesn't equal safety — every `request.formData()` value arrives as an untyped string from the wire. Validate with Zod before any DB write or downstream call. The `fail()` helper from `@sveltejs/kit` returns structured 4xx responses without throwing, keeping your action's happy path readable.\n\nThe env module system is where SvelteKit's trust model really shines. Secrets belong in `$env/static/private` or `$env/dynamic/private` — the bundler raises a hard build error if these are imported inside any client-evaluated module (`+page.ts`, `+layout.ts`, a component). Public configuration lives in `$env/static/public`. Never fall back to `process.env` directly: it bypasses SvelteKit's enforcement entirely and its behavior inside client bundles is undefined.\n\n`load` in `+page.server.ts` carries the same obligations as actions. User-controlled inputs like `params.id` or `url.searchParams.get()` are trust-boundary values too — parse and narrow them with the same rigor before they touch business logic.",
      "code_snippet": "// src/routes/subscribe/+page.server.ts\nimport { fail } from '@sveltejs/kit';\nimport { z } from 'zod';\nimport { SENDGRID_API_KEY } from '$env/static/private';\nimport type { Actions } from './$types';\n\nconst schema = z.object({\n  email: z.string().email().max(254),\n});\n\nexport const actions: Actions = {\n  default: async ({ request }) => {\n    const raw = Object.fromEntries(await request.formData());\n    const result = schema.safeParse(raw);\n    if (!result.success) return fail(422, { errors: result.error.flatten() });\n    await sendEmail(result.data.email, SENDGRID_API_KEY);\n  },\n};",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Code Quality — route segment conventions, consistent loader/action naming",
      "status": "done",
      "title": "SvelteKit Route Conventions — Consistent Naming for Loaders and Actions",
      "body": "SvelteKit's file-based routing gives you `+page.server.ts`, `+layout.server.ts`, and `+server.ts` — but code quality degrades fast when teams name exported functions inconsistently. The convention: always name your `load` function `load` and your form actions descriptively, never default.\n\nIn `+page.server.ts`, export a named `load` function (typed with `PageServerLoad`) and a named `actions` object with verb-first keys like `create`, `update`, or `deletePost`. This makes `$page.form` errors traceable and `use:enhance` bindings explicit.\n\nAvoid exporting anonymous arrow functions as `load` — type inference breaks and stack traces become unreadable. For `+layout.server.ts`, prefix shared data keys with the layout segment name (e.g., `dashboardUser`) to prevent key collisions when layout and page `load` data are merged into `$page.data`.\n\nUse `satisfies` instead of `as` for typing `actions` — it preserves the literal action keys so `ActionResult` generics resolve correctly downstream in your `+page.svelte` `ActionData` type.",
      "code_snippet": "// src/routes/dashboard/posts/+page.server.ts\nimport type { PageServerLoad, Actions } from './$types';\n\nexport const load = (async ({ locals }) => {\n  const posts = await locals.db.post.findMany();\n  return { posts };\n}) satisfies PageServerLoad;\n\nexport const actions = {\n  create: async ({ request, locals }) => {\n    const data = await request.formData();\n    await locals.db.post.create({ data: { title: String(data.get('title')) } });\n  },\n  deletePost: async ({ request, locals }) => {\n    const id = String((await request.formData()).get('id'));\n    await locals.db.post.delete({ where: { id } });\n  }\n} satisfies Actions;",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Routing — file-based routing patterns, dynamic segments, catch-all, intercepting routes, parallel routes",
      "status": "done",
      "title": "SvelteKit Routing Deep Dive — Dynamic Segments, Catch-Alls, and Groups",
      "body": "SvelteKit's filesystem router maps `src/routes` to URLs with zero config. A folder named `[slug]` creates a dynamic segment — accessible via `params.slug` in both `+page.svelte` and `+page.server.ts` load functions. Wrap a segment in `[[optional]]` to make it optional without creating a separate route.\n\nCatch-all routes use `[...rest]` — `src/routes/docs/[...path]` matches `/docs/a/b/c`, giving you `params.path = 'a/b/c'`. Combine with a typed `ParamMatcher` in `src/params/` to validate segments before the route resolves.\n\nRoute groups `(group)` organize routes without affecting the URL — perfect for sharing a layout between `/dashboard` and `/settings` without a common path prefix. Parallel routes don't exist natively in SvelteKit; the idiomatic substitute is named layout slots via `@` resets — e.g., `+page@` breaks out of a specific ancestor layout. For intercepted routes (modal patterns), use shallow routing: `goto(url, { state: { modal: true } })` and read `$page.state` to conditionally render a modal overlay instead of navigating away.",
      "code_snippet": "// src/routes/docs/[...path]/+page.server.ts\nimport type { PageServerLoad } from './$types';\n\nexport const load: PageServerLoad = ({ params }) => {\n  // params.path = 'getting-started/installation'\n  const segments = params.path.split('/');\n  return { segments };\n};\n\n// src/routes/(app)/settings/+page.svelte — grouped, no URL change\n// Shallow routing for modal interception:\n// goto('/photo/42', { state: { modal: true } });\n// Then in +layout.svelte: if ($page.state.modal) show <Modal />",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Internationalization — i18n routing strategies, locale in URL vs cookie, server-side locale detection",
      "status": "done",
      "title": "SvelteKit i18n — URL Locale vs Cookie, and Server-Side Detection",
      "body": "SvelteKit gives you two primary locale strategies: encode it in the URL (`/en/about`, `/fr/about`) or store it in a cookie. URL-based locale is SEO-friendly and stateless — ideal for public content. Cookie-based is cleaner for authenticated apps where locale is a user preference.\n\nFor URL routing, use a `[lang]` param segment and a `+layout.server.ts` to load translations. Read `params.lang` in `load()` and pass it down via `$page.data`.\n\nFor server-side detection without a URL param, use the `handle` hook in `hooks.server.ts`. Parse `event.request.headers.get('accept-language')` to extract the preferred locale, then either redirect to the localized route or set a `locale` cookie via `event.cookies.set()`. Always prefer `event.locals` to propagate the resolved locale to all `load()` functions downstream — avoids redundant header parsing per route.\n\nAvoid mixing strategies: if your `load()` reads from both `params.lang` and a cookie, you'll get hydration mismatches on the client.",
      "code_snippet": "// src/hooks.server.ts\nimport type { Handle } from '@sveltejs/kit';\n\nexport const handle: Handle = async ({ event, resolve }) => {\n  const cookieLocale = event.cookies.get('locale');\n  const headerLocale = event.request.headers.get('accept-language')?.split(',')[0].slice(0, 2);\n  const supported = ['en', 'fr', 'de'];\n\n  const locale = supported.includes(cookieLocale ?? '') \n    ? cookieLocale! \n    : supported.includes(headerLocale ?? '') \n    ? headerLocale! \n    : 'en';\n\n  event.locals.locale = locale;\n  return resolve(event);\n};",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Web Vitals — image optimization with framework Image component, font loading, LCP via priority flag",
      "status": "done",
      "title": "SvelteKit Web Vitals — Image Optimization, Font Loading, and LCP Tuning",
      "body": "SvelteKit doesn't ship a built-in `<Image>` component like Next.js, but `@sveltejs/enhanced-img` fills that gap. Add it via `vite-imagetools` integration and use `<enhanced:img>` in your markup — it automatically generates WebP/AVIF variants, injects width/height to prevent layout shift, and produces a `srcset` without manual configuration.\n\nFor LCP images, pass `fetchpriority=\"high\"` and skip `loading=\"lazy\"` — the browser needs that resource immediately. Pairing this with a `<link rel=\"preload\">` in your `<svelte:head>` block gives the earliest possible fetch signal.\n\nFont loading is handled cleanly in SvelteKit's `app.html` or via a layout's `<svelte:head>`. Use `font-display: optional` for body fonts to eliminate render-blocking; for brand fonts critical to LCP, `font-display: swap` with a preconnect to your font CDN is the safer tradeoff.\n\nMeasure with `$app/environment`'s `browser` guard to run `PerformanceObserver` only client-side, capturing real LCP entries without SSR noise.",
      "code_snippet": "<!-- src/routes/+layout.svelte -->\n<svelte:head>\n  <link rel=\"preconnect\" href=\"https://fonts.googleapis.com\" />\n  <link\n    rel=\"preload\"\n    as=\"image\"\n    href=\"/hero.avif\"\n    fetchpriority=\"high\"\n  />\n</svelte:head>\n\n<!-- Hero component -->\n<script>\n  import { browser } from '$app/environment';\n  import { onMount } from 'svelte';\n\n  onMount(() => {\n    if (!browser) return;\n    const observer = new PerformanceObserver((list) => {\n      for (const entry of list.getEntries()) {\n        console.log('LCP candidate:', entry.startTime, entry.element);\n      }\n    });\n    observer.observe({ type: 'largest-contentful-paint', buffered: true });\n  });\n</script>\n\n<enhanced:img\n  src=\"/hero.png\"\n  alt=\"Hero\"\n  fetchpriority=\"high\"\n  loading=\"eager\"\n/>",
      "language": "javascript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Memory Leaks — streaming with Suspense, abort signals in loaders, cleanup on navigation",
      "status": "done",
      "title": "SvelteKit Loaders Leak Without Abort Signals — Here's the Fix",
      "body": "SvelteKit's `load` functions receive a `signal` — an `AbortSignal` that fires when the client navigates away or the HTTP connection closes. SvelteKit's built-in `fetch` wrapper honours it automatically, but any native `fetch`, `EventSource`, or `ReadableStream` you open yourself does not.\n\nWhen you return an unresolved promise from a server `load` (deferred streaming), SvelteKit pipes chunks to the client over an open HTTP connection. If your async generator never checks `signal.aborted`, it keeps producing chunks server-side long after the user has moved on — leaking both memory and open connections.\n\nOn the client, `onDestroy` is too late for in-flight requests started after mount. Use `beforeNavigate` instead: it fires before the outgoing route tears down, giving you a clean window to `abort()` any active controllers. `afterNavigate` resets the controller for back-navigation into the same page. Pair this with `onDestroy` as a final safety net for tab-close scenarios.\n\nThe pattern: one `AbortController` per page instance, aborted in `beforeNavigate`, reset in `afterNavigate`, and passed as `signal` to every outbound `fetch`.",
      "code_snippet": "<!-- +page.svelte -->\n<script lang=\"ts\">\n  import { beforeNavigate, afterNavigate } from '$app/navigation';\n  import { onDestroy } from 'svelte';\n\n  let controller = new AbortController();\n\n  // beforeNavigate fires before unmount — ideal for aborting in-flight work\n  beforeNavigate(() => controller.abort());\n\n  // Reset after back-navigation so the page is usable again\n  afterNavigate(() => { controller = new AbortController(); });\n\n  // Final safety net for tab close / component destroy\n  onDestroy(() => controller.abort());\n\n  async function loadMore() {\n    const res = await fetch('/api/feed/next', { signal: controller.signal });\n    if (!res.ok || controller.signal.aborted) return;\n    // handle chunks\n  }\n</script>",
      "language": "typescript",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Real-time & WebSockets — server-sent events from route handlers, combining with client state",
      "status": "done",
      "title": "SvelteKit SSE — Streaming Server-Sent Events from Route Handlers into Svelte Stores",
      "body": "SvelteKit's `+server.ts` route handlers can stream SSE by returning a `Response` wrapping a `ReadableStream` with `Content-Type: text/event-stream`. The `start` callback receives a `ReadableStreamDefaultController` — enqueue raw SSE-formatted strings (`data: ...\n\n`) directly. Return a cancel function from `start` to clean up timers or DB subscriptions when the client disconnects.\n\nOn the client, use the browser's native `EventSource` API pointed at your route, then pipe incoming messages into a Svelte `writable` store. Wrap the connection in `onMount` and return `source.close` as the cleanup function — Svelte's lifecycle handles disconnection on component destroy automatically.\n\nFor server-side load data coexisting with the stream, keep concerns separated: use `+page.server.ts` for initial hydration and SSE for deltas only. This avoids race conditions where the initial `load` and the first SSE event overwrite each other. If you need auth, validate the JWT inside the `GET` handler via `request.headers.get('cookie')` before opening the stream — `EventSource` sends cookies but ignores `credentials: 'include'` since it predates Fetch.",
      "code_snippet": "// src/routes/stream/+server.ts\nimport type { RequestHandler } from './$types';\n\nexport const GET: RequestHandler = () => {\n  const stream = new ReadableStream({\n    start(controller) {\n      const tick = setInterval(\n        () => controller.enqueue(`data: ${JSON.stringify({ ts: Date.now() })}\\n\\n`),\n        2000\n      );\n      return () => clearInterval(tick);\n    }\n  });\n  return new Response(stream, {\n    headers: { 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache' }\n  });\n};",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Environment & Config — NEXT_PUBLIC_ vs private vars, validate at startup, never expose secrets",
      "status": "done",
      "title": "SvelteKit Environment Variables — PUBLIC_ Boundaries, Startup Validation, and Secret Safety",
      "body": "SvelteKit splits env vars into two enforced namespaces. Variables prefixed with `PUBLIC_` are accessible from `$env/static/public` or `$env/dynamic/public` and can be imported in components, pages, and client-side `+page.ts` files. Everything else lives behind `$env/static/private` or `$env/dynamic/private` — importable only in server-side files like `+page.server.ts`, `+server.ts`, and `hooks.server.ts`. This isn't a convention — SvelteKit's module graph analysis throws a build error if you import a private env module from a shared or client-accessible file.\n\nValidate required vars at startup inside `hooks.server.ts`. It runs once on server boot, surfaces misconfigurations in CI before requests are served, and avoids null-reference crashes buried inside request handlers. Prefer `$env/dynamic/private` over `$env/static/private` when you need runtime flexibility (e.g. container deployments). Never reach for `process.env` directly — SvelteKit's env modules are tree-shaken, statically typed, and enforce the public/private boundary at the compiler level.",
      "code_snippet": "// src/hooks.server.ts\nimport { env } from '$env/dynamic/private';\nimport type { Handle } from '@sveltejs/kit';\n\nconst REQUIRED = ['DATABASE_URL', 'GROQ_API_KEY', 'CLERK_SECRET_KEY'] as const;\n\nfor (const key of REQUIRED) {\n  if (!env[key]) {\n    throw new Error(`Missing required server env var: ${key}`);\n  }\n}\n\nexport const handle: Handle = ({ event, resolve }) => resolve(event);",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "API Layer — route handlers for public APIs, Server Actions for mutations, middleware for auth",
      "status": "done",
      "title": "SvelteKit API Layer — Route Handlers, Server Actions, and Hooks for Auth",
      "body": "SvelteKit gives you three distinct tools for server-side logic, and conflating them causes pain.\n\n**`+server.ts` route handlers** are for public API endpoints — think REST or webhook receivers. They export named HTTP method functions (`GET`, `POST`, etc.) and return a `Response`. Use these when external clients need to hit your API.\n\n**Server Actions** (form `actions` in `+page.server.ts`) are for mutations triggered from your own UI. They run on `POST`, integrate with `enhance`, and return typed data back to the page via `ActionData`. No `fetch` needed — SvelteKit wires it automatically.\n\n**`hooks.server.ts`** is where auth lives. The `handle` hook intercepts every request before it reaches a route. Set `event.locals.user` here after validating your session cookie or JWT, then read `locals.user` in any `load` function or action downstream — one validation point, zero repetition.\n\nThe golden rule: never do auth inside individual `load` functions. Centralize it in `handle`, guard routes by checking `locals` early, and redirect in the hook itself using `error(401)` or `redirect(303)`.",
      "code_snippet": "// hooks.server.ts\nimport { redirect } from '@sveltejs/kit';\nimport { validateSession } from '$lib/server/auth';\n\nexport const handle: Handle = async ({ event, resolve }) => {\n  const session = event.cookies.get('session');\n  event.locals.user = session ? await validateSession(session) : null;\n\n  const protectedRoutes = ['/dashboard', '/settings'];\n  const isProtected = protectedRoutes.some(r => event.url.pathname.startsWith(r));\n\n  if (isProtected && !event.locals.user) {\n    redirect(303, '/login');\n  }\n\n  return resolve(event);\n};",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Animation — page transition patterns, View Transitions API, layout persistence across navigations",
      "status": "done",
      "title": "SvelteKit Page Transitions — View Transitions API + Layout Persistence",
      "body": "SvelteKit's `onNavigate` lifecycle hook is the native integration point for the View Transitions API. Call `document.startViewTransition()` inside it and return a promise — SvelteKit holds the navigation until your promise resolves, giving the browser time to capture the old and new DOM states.\n\nFor layout persistence across navigations, the key is understanding that `+layout.svelte` components are never unmounted when child routes change — only the `<slot />` region re-renders. This means elements like sidebars and navbars animate naturally. To persist a specific element's visual identity across routes (e.g., a shared card expanding into a detail view), assign matching `view-transition-name` CSS properties on both pages.\n\nCSS `::view-transition-old` and `::view-transition-new` pseudo-elements let you override the default cross-fade per named element. Pair this with SvelteKit's `preloadData` on hover to ensure data is ready before the transition fires, preventing a flash of loading state mid-animation. For fallback, wrap in `if (document.startViewTransition)` — `onNavigate` still fires in unsupported browsers.",
      "code_snippet": "// src/hooks.client.ts\nimport { onNavigate } from '$app/navigation';\n\nonNavigate((navigation) => {\n  if (!document.startViewTransition) return;\n\n  return new Promise((resolve) => {\n    document.startViewTransition(async () => {\n      resolve();\n      await navigation.complete;\n    });\n  });\n});",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Global UI Patterns — layout-level toast/modal, shared loading UI in layout segment",
      "status": "done",
      "title": "SvelteKit Layout-Level Toast, Modal & Shared Loading UI — Global UI Patterns Done Right",
      "body": "SvelteKit's nested layout system makes it trivial to wire up global UI without prop-drilling or a state management library. Drop a `+layout.svelte` at the root and use a writable store to drive toasts and modals from anywhere in the app.\n\nDefine a `toastStore` and `modalStore` in `$lib/stores/ui.ts`. Your root layout subscribes to both and renders the overlay components once — they're always mounted, always reactive. Child routes trigger them by importing the store and calling `toastStore.show(...)` or `modalStore.open(...)`.\n\nFor shared loading UI, SvelteKit exposes the `$navigating` store from `$app/stores`. It's non-null during any client-side navigation. Bind a progress bar or skeleton overlay directly to `$navigating` in `+layout.svelte` — no lifecycle hooks needed. Pair it with `+loading.svelte` (introduced via route-level `+page.svelte` async `load`) for streamed data skeletons scoped to a layout segment. Use `+layout.ts` with `await parent()` to sequence data dependencies, keeping the root layout's loading state decoupled from nested route fetches.",
      "code_snippet": "<!-- src/routes/+layout.svelte -->\n<script lang=\"ts\">\n  import { navigating } from '$app/stores';\n  import { toastStore } from '$lib/stores/ui';\n  import Toast from '$lib/components/Toast.svelte';\n  import Modal from '$lib/components/Modal.svelte';\n</script>\n\n{#if $navigating}\n  <div class=\"progress-bar\" />\n{/if}\n\n<Toast messages={$toastStore.messages} />\n<Modal config={$modalStore} />\n\n<slot />",
      "language": "javascript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Bundle & Asset Optimization — automatic code splitting per route, dynamic imports, bundle analysis",
      "status": "done",
      "title": "SvelteKit's Automatic Code Splitting — Route Chunks, Dynamic Imports, and Bundle Analysis",
      "body": "SvelteKit splits your bundle automatically at route boundaries. Every `+page.svelte`, `+layout.svelte`, and `+page.server.ts` becomes its own chunk — Vite handles this without configuration. The client only downloads JavaScript for the routes it actually visits.\n\nFor heavy components inside a route, reach for dynamic imports via `{#await import(...)}` or wrap with `svelte:component` after a lazy load. This creates a secondary async chunk, keeping the initial route payload lean.\n\nServer-side modules (`+page.server.ts`, `+server.ts`) are never bundled into the client chunk at all — SvelteKit's module graph analysis strips them entirely, so you get zero leakage of server-only code.\n\nTo inspect what ends up in each chunk, run `vite build --mode production` then open `.svelte-kit/output/client/_app/immutable/` — the filenames include content hashes and you can cross-reference with `vite-bundle-visualizer`:\n\n```\nnpx vite-bundle-visualizer\n```\n\nFor granular control, use `$app/environment`'s `browser` guard combined with dynamic import to defer third-party libraries (e.g. chart renderers) until after hydration completes, preventing them from blocking the critical path.",
      "code_snippet": "<!-- +page.svelte: lazy-load a heavy chart component after mount -->\n<script lang=\"ts\">\n  import { browser } from '$app/environment';\n  import { onMount } from 'svelte';\n\n  let ChartComponent: typeof import('$lib/Chart.svelte').default | null = null;\n\n  onMount(async () => {\n    if (browser) {\n      const mod = await import('$lib/Chart.svelte');\n      ChartComponent = mod.default;\n    }\n  });\n</script>\n\n{#if ChartComponent}\n  <svelte:component this={ChartComponent} data={chartData} />\n{:else}\n  <p>Loading chart...</p>\n{/if}",
      "language": "typescript",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Dev Tooling & Quality Gates — framework CLI, preview deployments, type-check route params",
      "status": "done",
      "title": "SvelteKit Quality Gates — Type-Safe Route Params, CLI Sync, and Adapter-Aware Previews",
      "body": "SvelteKit's CLI ships a `sync` command that generates `.svelte-kit/types/` — the source of the `./$types` virtual module every route imports. Run `svelte-kit sync` before `svelte-check` in CI, or let both fire automatically via `npm run check` (scaffolded by `create-svelte` by default).\n\nRoute params get strict types through `./$types`: import `PageServerLoad` or `PageLoad` in your `+page.server.ts` / `+page.ts`, and `params` becomes typed against your file-system segments automatically. A route at `src/routes/posts/[slug]/+page.server.ts` surfaces `params.slug: string` — no manual casting. For optional or rest segments (`[[optional]]`, `[...rest]`), the generated `RouteParams` type reflects correct optionality.\n\nFor preview deployments, `vite preview` serves the Vite-built output locally but skips adapter-specific runtime behavior. For Cloudflare, use `wrangler pages dev .svelte-kit/cloudflare`; for Vercel, use `vercel dev`. These surface edge cases — missing platform bindings, broken `PLATFORM` env vars — that `vite preview` silently swallows.\n\nGate your CI pipeline with `svelte-kit sync && svelte-check --tsconfig ./tsconfig.json` to catch broken param types and `.svelte` type errors before any branch merges.",
      "code_snippet": "// src/routes/posts/[slug]/+page.server.ts\nimport type { PageServerLoad } from './$types';\nimport { error } from '@sveltejs/kit';\n\nexport const load: PageServerLoad = async ({ params, fetch }) => {\n  // params.slug: string — typed by svelte-kit sync, not hand-written\n  const res = await fetch(`/api/posts/${params.slug}`);\n  if (!res.ok) error(404, 'Post not found');\n  return { post: await res.json() as App.Post };\n};",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Monitoring in Production — edge function logs, server-side Sentry, Web Vitals from RUM",
      "status": "done",
      "title": "SvelteKit Production Monitoring — Edge Logs, Sentry in hooks.server.ts, and RUM Web Vitals",
      "body": "SvelteKit gives you three distinct integration points for production observability, and each requires a different approach.\n\nFor server-side Sentry, `hooks.server.ts` is the right seam — wire `handleError` through `Sentry.handleErrorWithSentry()`. This captures errors from both SSR renders and `+server.ts` API routes with full request context via the `RequestEvent`.\n\nEdge functions (routes exporting `export const config = { runtime: 'edge' }`) cannot use the Node.js Sentry SDK. Swap to `@sentry/cloudflare` or `@sentry/vercel-edge` depending on your adapter. `console.log` inside edge routes bypasses Node stdout entirely — output streams to your adapter's log drain (Vercel Log Drains, Cloudflare Logpush), so structure those logs as JSON for queryability.\n\nWeb Vitals via RUM require care: the `web-vitals` library only fires on hard navigations by default. In `+layout.svelte`, call your observers inside `afterNavigate` so LCP, CLS, and INP re-register after each client-side route transition. Tag each beacon with `page.route.id` from `$app/state` for per-route dashboards.",
      "code_snippet": "// src/hooks.server.ts\nimport * as Sentry from '@sentry/sveltekit';\n\nSentry.init({ dsn: process.env.SENTRY_DSN, tracesSampleRate: 0.1 });\n\nexport const handleError = Sentry.handleErrorWithSentry(({ error, event }) => {\n  console.error(JSON.stringify({ route: event.route.id, url: event.url.pathname, error }));\n});\n\n// src/routes/api/data/+server.ts — edge runtime\nexport const config = { runtime: 'edge' };\n\nexport const GET = async ({ request }) => {\n  console.log(JSON.stringify({ ray: request.headers.get('cf-ray'), ts: Date.now() }));\n  return new Response('ok');\n};",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Middleware — run at the edge before render, auth guards, A/B testing, geolocation, rate limiting",
      "status": "done",
      "title": "SvelteKit Hooks — Edge Middleware for Auth Guards, A/B Testing & Geolocation",
      "body": "SvelteKit's `handle` hook in `src/hooks.server.ts` is your edge middleware layer — it intercepts every request before any `+page.server.ts` or `+server.ts` runs. Use it to enforce auth guards by inspecting cookies and redirecting unauthenticated users, inject A/B variant flags into `event.locals`, or gate routes by geolocation from request headers.\n\nFor auth, read the session cookie via `event.cookies.get()`, validate it, and either `redirect(303, '/login')` or populate `event.locals.user`. Downstream `load` functions and form actions then access `locals.user` without re-validating.\n\nFor A/B testing, hash `event.request.headers.get('x-forwarded-for')` or a stable cookie into a bucket, store the variant in `locals.variant`, and render different UI branches in your `+page.svelte`.\n\nRate limiting fits here too — maintain a `Map` keyed by IP in module scope (or use a KV store like Cloudflare's), check request count, and return a `new Response('Too Many Requests', { status: 429 })` before `resolve(event)` is ever called. Chain multiple concerns with `sequence()` from `@sveltejs/kit/hooks`.",
      "code_snippet": "// src/hooks.server.ts\nimport { redirect } from '@sveltejs/kit';\nimport { sequence } from '@sveltejs/kit/hooks';\n\nconst auth: Handle = async ({ event, resolve }) => {\n  const session = event.cookies.get('session');\n  event.locals.user = session ? await validateSession(session) : null;\n  if (!event.locals.user && event.url.pathname.startsWith('/dashboard')) {\n    redirect(303, '/login');\n  }\n  return resolve(event);\n};\n\nconst abTest: Handle = async ({ event, resolve }) => {\n  event.locals.variant = (event.cookies.get('uid') ?? '').charCodeAt(0) % 2 === 0 ? 'A' : 'B';\n  return resolve(event);\n};\n\nexport const handle = sequence(auth, abTest);",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    }
  ]
}