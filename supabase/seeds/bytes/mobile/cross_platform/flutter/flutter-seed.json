{
  "tech_stack": "flutter",
  "subdomain": "cross_platform",
  "domain": "mobile",
  "bytes": [
    {
      "topic": "React Native Architecture — new vs old architecture, JSI, Fabric, Turbo Modules",
      "status": "done",
      "title": "Flutter and React Native Architecture: A Comparative Look",
      "body": "Flutter's architecture is based on the Skia graphics engine and Dart runtime. Unlike React Native, Flutter doesn't rely on JavaScript or a bridge layer. However, Flutter does provide a way to call native code through the Platform channel. On the other hand, React Native's new architecture is based on Fabric, which provides a more efficient way to interact with native code. Turbo Modules, introduced in React Native 0.68, further optimize the performance by compiling JavaScript to native code. Flutter, however, does not require such optimizations due to its native Dart runtime. JSI (JavaScript Interface) is used in React Native to interact with native code, while Flutter uses the Platform channel for the same purpose.",
      "code_snippet": null,
      "language": null,
      "clarity": 8,
      "specificity": 9,
      "relevance": 8,
      "overall": 8
    },
    {
      "topic": "Code Sharing — sharing logic between web and mobile, monorepo strategies",
      "status": "done",
      "title": "Flutter - Sharing Logic between Web and Mobile with a Monorepo Strategy",
      "body": "When building cross-platform apps with Flutter, you often need to share logic between your mobile and web implementations. To achieve this efficiently, consider using a monorepo strategy. Create a separate folder for shared code and define a pub package within it. Then, add the package to your mobile and web projects as dependencies. This way, you can manage shared logic in a central location, reducing code duplication and making maintenance easier. For example, create a pub package called `shared_logic` in a `lib/shared` folder:\n```dart\n// lib/shared/shared_logic/lib/shared_logic.dart\nexport 'package:flutter/foundation.dart';\ndefault_value<T>(T Function() factory) => factory();\n```",
      "code_snippet": "```dart\n// lib/shared/shared_logic/lib/shared_logic.dart\nexport 'package:flutter/foundation.dart';\ndefault_value<T>(T Function() factory) => factory();\n// main.dart\nimport 'package:shared_logic/shared_logic.dart';\nString defaultValue() => 'default value';\nprint(defaultValue());\n```",
      "language": "dart",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 8
    },
    {
      "topic": "Over-the-Air Updates — EAS Update, CodePush, when OTA is and isn't safe",
      "status": "done",
      "title": "Flutter OTA Updates — When EAS Update and CodePush Are Safe to Use",
      "body": "Flutter apps can be updated over-the-air (OTA) using Firebase's EAS Update or Microsoft's CodePush. However, these services differ significantly. EAS Update uses a binary delta algorithm to send only the changed code, reducing the update size. CodePush, on the other hand, sends the entire app bundle. Use EAS Update for apps with frequent updates, while CodePush is suitable for apps with less frequent updates. Ensure that your app's dependencies are up-to-date before using EAS Update.",
      "code_snippet": "import 'package:flutter/foundation.dart';\nimport 'package:firebase_inappmessaging_platform_interface/firebase_inappmessaging_platform_interface.dart';\n\nvoid main() {\n  if (kIsWeb) {\n    // Use CodePush for web\n  } else {\n    // Use EAS Update for mobile\n  }\n}",
      "language": "dart",
      "clarity": 9,
      "specificity": 10,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Security — secure storage, certificate pinning, jailbreak/root detection",
      "status": "done",
      "title": "Flutter - Secure Storage with Keychain, Certificate Pinning and Jailbreak Detection",
      "body": "Flutter provides `flutter_secure_storage` package for secure storage on mobile platforms. On iOS, it uses Keychain, while on Android, it utilizes the `Context` and `SharedPreferences`. To prevent man-in-the-middle attacks, implement certificate pinning using `flutter_secure_storage` and verify the certificate against the expected one. For jailbreak/root detection, use `flutter_secure_storage` package's `isAndroidEmulator` and `isIOSSimulator` methods or the `flutter_secure_storage` package's `isJailbroken` method on iOS.",
      "code_snippet": "import 'package:flutter_secure_storage/flutter_secure_storage.dart';\nfinal storage = FlutterSecureStorage();\nfinal bool isJailbroken = await storage.isJailbroken();\nif (isJailbroken) { print('Device is jailbroken'); } else { print('Device is not jailbroken'); }",
      "language": "dart",
      "clarity": 9,
      "specificity": 8,
      "relevance": 9,
      "overall": 8
    },
    {
      "topic": "Native Modules — when to bridge to native, writing native modules and plugins",
      "status": "done",
      "title": "Flutter — When to Bridge to Native (and How)",
      "body": "When building cross-platform Flutter apps, you might encounter scenarios where native functionality is required. This is where Platform Channels come into play. A Platform Channel is a bidirectional communication channel between your Flutter app and native code. To create a Platform Channel, you'll need to define a `MethodChannel` in your Flutter code and a corresponding `Platform` class in native code. For example, in Dart, you can create a `MethodChannel` like this:\n\n```dart\nimport 'package:flutter/services.dart';\n\nclass MyPlatformChannel { static const platform = const MethodChannel('com.example.my_channel'); static Future<String> getPlatformVersion() async { final version = await platform.invokeMethod('getPlatformVersion'); return version; } }\n\n```\n\nIn native code, you'll need to implement the `Platform` class with the required method, e.g., in Kotlin, it might look like this:\n\n```kotlin\nimport io.flutter.embedding.engine.plugins.FlutterPlugin\n\nclass MyPlugin : FlutterPlugin {\n override fun onAttachedToEngine(binding: FlutterPluginBinding) {\n val channel = binding.engine.dartExecutor.binaryMessenger.channel\n channel.setMethodCallHandler { call, result ->\n when (call.method) {\n 'getPlatformVersion' -> result.success('Android')\n else -> result.notImplemented()\n }\n }\n }\n override fun onDetachedFromEngine(binding: FlutterPluginBinding) {\n binding.engine.dartExecutor.binaryMessenger.channel.setMethodCallHandler(null)\n }\n }\n\n```\n\nThis allows your Flutter app to invoke native code and receive results.",
      "code_snippet": "null",
      "language": "dart",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9.5
    }
  ]
}