{
  "tech_stack": "wagmi",
  "subdomain": "web3_tools",
  "domain": "blockchain",
  "bytes": [
    {
      "topic": "viem — TypeScript-first, lightweight, low-level, replacing ethers in modern stacks",
      "status": "done",
      "title": "wagmi v2 — Built on viem, Not ethers",
      "body": "wagmi v2 dropped ethers.js entirely and rebuilt its internals on viem. This isn't just a dependency swap — it changes how you interact with low-level primitives throughout your app.\n\nEvery wagmi hook now returns viem-native types. `useBalance` returns a `bigint` balance, not an ethers `BigNumber`. `useReadContract` expects viem's `Abi` type for the `abi` param. Error types are viem's `ContractFunctionRevertedError`, `UserRejectedRequestError`, etc. — structured, typed, and narrowable without string matching.\n\nThe `config` object you pass to `WagmiProvider` accepts a `client` built with viem's `createPublicClient` and `createWalletClient` under the hood via `createConfig`. You can extract those clients directly with `getPublicClient(config)` and `getWalletClient(config)` for raw viem calls outside React.\n\nThis matters because you can now mix wagmi hooks with direct viem calls sharing the same transport and chain config — no duplicate RPC connections, no type impedance mismatch between layers. Passing ABIs as `const` assertions gives you fully inferred return types end-to-end, from contract ABI to your component's JSX.",
      "code_snippet": "import { createConfig, http, getPublicClient } from 'wagmi'\nimport { mainnet } from 'wagmi/chains'\nimport { createPublicClient } from 'viem'\n\nconst config = createConfig({\n  chains: [mainnet],\n  transports: { [mainnet.id]: http() },\n})\n\n// Extract the underlying viem client — same transport, no extra RPC\nconst client = getPublicClient(config, { chainId: mainnet.id })\n\nconst block = await client.getBlockNumber()\nconst balance = await client.getBalance({ address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045' })\n\nconsole.log(block, balance) // bigint, bigint — viem-native, no BigNumber wrapper",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "wagmi — React hooks for Ethereum, connector system, TanStack Query integration",
      "status": "done",
      "title": "wagmi's TanStack Query Integration — Cache, Invalidate, and Prefetch Ethereum Data",
      "body": "wagmi v2 is built on TanStack Query, which means every hook like `useBalance`, `useReadContract`, and `useBlockNumber` is a query under the hood. You get automatic caching, background refetching, and deduplication for free — no custom state management needed.\n\nEach wagmi hook accepts `query` config to control caching behavior. Set `staleTime: Infinity` for static contract data like token decimals, or `refetchInterval: 4_000` to poll every block. You can also call `queryClient.invalidateQueries({ queryKey })` after a write to immediately refresh dependent reads.\n\nwagmi exports `useQueryClient` and query key factories via `hashFn` and `readContractQueryOptions`, letting you construct precise query keys for targeted invalidation. After a `useWriteContract` mutation confirms, invalidate just the affected balance or allowance query rather than nuking the entire cache.\n\nConnectors (MetaMask, WalletConnect, Coinbase) are first-class in this model too — `useAccount` and `useChainId` sync via wagmi's internal store, while on-chain reads flow through TanStack Query, giving you a clean separation between wallet state and contract data.",
      "code_snippet": "import { useReadContract, useWriteContract, useQueryClient } from 'wagmi'\nimport { readContractQueryOptions } from 'wagmi/query'\nimport { erc20Abi } from 'viem'\n\nconst queryClient = useQueryClient()\nconst { writeContractAsync } = useWriteContract()\n\nconst { data: balance } = useReadContract({\n  address: '0xTokenAddress',\n  abi: erc20Abi,\n  functionName: 'balanceOf',\n  args: ['0xUserAddress'],\n  query: { staleTime: 10_000 },\n})\n\nasync function approveAndInvalidate() {\n  await writeContractAsync({ address: '0xTokenAddress', abi: erc20Abi, functionName: 'approve', args: ['0xSpender', 1000n] })\n  const opts = readContractQueryOptions(config, { address: '0xTokenAddress', abi: erc20Abi, functionName: 'balanceOf', args: ['0xUserAddress'] })\n  queryClient.invalidateQueries({ queryKey: opts.queryKey })\n}",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "ethers.js — providers, signers, contract interaction, event listening",
      "status": "done",
      "title": "wagmi v2 — Mapping ethers.js Providers, Signers, and Events to Hooks",
      "body": "wagmi v2 replaces the ethers.js mental model entirely with viem-backed hooks. Forget manually instantiating providers or signers — wagmi surfaces them as `usePublicClient` (read-only, replaces `JsonRpcProvider`) and `useWalletClient` (write-capable, replaces `Signer`). Contract reads use `useReadContract`, which caches results and re-fetches on block updates automatically. Writes go through `useWriteContract`, which returns a `writeContract` function and exposes `isPending`/`isSuccess` state — no manual gas estimation needed. For event subscriptions, `useWatchContractEvent` sets up a filter and fires a callback on each match, cleaning up the listener on unmount so you never leak subscriptions. One sharp edge: `useWalletClient` returns `data` that is async-resolved, so always guard with `if (!walletClient) return` before passing it to a viem write call. The underlying `PublicClient` and `WalletClient` are fully accessible if you need to drop down to raw viem calls for anything wagmi doesn't cover.",
      "code_snippet": "import { usePublicClient, useWalletClient, useReadContract, useWriteContract, useWatchContractEvent } from 'wagmi'\nimport { erc20Abi } from 'viem'\n\nconst CONTRACT = '0xTokenAddress'\n\nexport function TokenPanel() {\n  const publicClient = usePublicClient()\n  const { data: walletClient } = useWalletClient()\n\n  const { data: balance } = useReadContract({\n    address: CONTRACT, abi: erc20Abi, functionName: 'balanceOf',\n    args: [walletClient?.account.address ?? '0x0'],\n  })\n\n  const { writeContract } = useWriteContract()\n\n  useWatchContractEvent({\n    address: CONTRACT, abi: erc20Abi, eventName: 'Transfer',\n    onLogs: (logs) => console.log('Transfer:', logs),\n  })\n\n  return <button onClick={() => writeContract({ address: CONTRACT, abi: erc20Abi, functionName: 'approve', args: ['0xSpender', 1000n] })}>Approve</button>\n}",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "The Graph — subgraphs, indexing blockchain events, GraphQL queries, hosted vs decentralized",
      "status": "done",
      "title": "wagmi + The Graph — Query Indexed Blockchain History Instead of Scanning Events",
      "body": "wagmi's `useWatchContractEvent` catches live events, but it can't reach historical data. That's The Graph's job. A subgraph indexes your contract's emitted events into a PostgreSQL-backed GraphQL API — you define the schema in a manifest, deploy it, and query history instantly.\n\nwagmi v2 is built on TanStack Query, so pairing it with `graphql-request` is idiomatic: use `useQuery` directly with The Graph endpoint as the fetcher. Pull `address` from `useAccount()` and pass it as a variable — no custom hooks required.\n\nHosted Service (`api.thegraph.com`) is free and centralized — fine for dev/staging but deprecated for new subgraphs. The decentralized network (`gateway.thegraph.com`) requires GRT and an API key, but gives you SLA-backed indexing with redundancy. For production wagmi apps, migrate to the decentralized endpoint and gate your key server-side or via an env-prefixed Next.js route handler to avoid leaking it client-side.\n\nSubgraphs eliminate the need for `getLogs` pagination or a custom indexer for most read-heavy use cases.",
      "code_snippet": "import { useAccount } from 'wagmi'\nimport { useQuery } from '@tanstack/react-query'\nimport { request, gql } from 'graphql-request'\n\nconst SUBGRAPH = 'https://api.thegraph.com/subgraphs/name/uniswap/uniswap-v3'\n\nconst SWAPS = gql`\n  query GetSwaps($user: String!) {\n    swaps(where: { origin: $user }, first: 10, orderBy: timestamp, orderDirection: desc) {\n      amountUSD\n      timestamp\n      pool { token0 { symbol } token1 { symbol } }\n    }\n  }\n`\n\nexport function useUserSwaps() {\n  const { address } = useAccount()\n  return useQuery({\n    queryKey: ['swaps', address],\n    queryFn: () => request(SUBGRAPH, SWAPS, { user: address?.toLowerCase() }),\n    enabled: !!address,\n  })\n}",
      "language": "typescript",
      "clarity": 9,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Reading On-chain Data — eth_call, getLogs, event filtering, batch requests",
      "status": "done",
      "title": "wagmi — Reading On-chain Data with useReadContract, getLogs, and Multicall",
      "body": "wagmi's `useReadContract` maps directly to `eth_call` under the hood — it reads contract state without sending a transaction. For a single slot, pass `address`, `abi`, `functionName`, and `args`. Results are typed via viem's ABI inference, so you get autocomplete on return values.\n\nFor event history, use `useWatchContractEvent` for live streaming or viem's `getLogs` via `usePublicClient` for historical queries. Pass `fromBlock`, `toBlock`, and an `event` ABI entry to filter precisely — no topic encoding needed.\n\nBatch multiple reads with `useReadContracts`, which batches calls into a single JSON-RPC `eth_call` using multicall3 by default. Each contract in the `contracts` array returns `{ data, error, status }` independently, so partial failures don't break the batch.\n\nAvoid `useContractRead` (deprecated). Prefer `useReadContract` for single reads and `useReadContracts` for batches. Set `watch: true` on any hook to revalidate on new blocks without manual polling.",
      "code_snippet": "import { useReadContracts, usePublicClient } from 'wagmi'\nimport { erc20Abi, parseAbiItem } from 'viem'\n\nconst { data } = useReadContracts({\n  contracts: [\n    { address: '0xA0b...', abi: erc20Abi, functionName: 'balanceOf', args: ['0xUser...'] },\n    { address: '0xA0b...', abi: erc20Abi, functionName: 'totalSupply' },\n  ],\n})\n\nconst client = usePublicClient()\nconst logs = await client.getLogs({\n  address: '0xA0b...',\n  event: parseAbiItem('event Transfer(address indexed from, address indexed to, uint256 value)'),\n  fromBlock: 20_000_000n,\n  toBlock: 'latest',\n})",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Wallet Connection — WalletConnect, MetaMask, Coinbase Wallet, wallet abstraction",
      "status": "done",
      "title": "wagmi — Multi-Wallet Support with WalletConnect, MetaMask, and Coinbase Wallet",
      "body": "wagmi's `createConfig` accepts a `connectors` array where you wire up every wallet you want to support. Each connector is a first-class object from `wagmi/connectors` — no third-party adapter layer needed. `metaMask()`, `coinbaseWallet()`, and `walletConnect()` all implement the same `Connector` interface, so `useConnect` handles them identically: call `connect({ connector })` and wagmi manages the provider switch, chain negotiation, and account exposure automatically.\n\n`walletConnect()` requires a `projectId` from cloud.walletconnect.com and optionally accepts `showQrModal: false` if you're rendering your own modal via `useConnections`. `coinbaseWallet()` supports a `preference` field — set `'smartWalletOnly'` to force EIP-4337 smart accounts, or `'all'` to let users choose.\n\nFor wallet abstraction, reach for `useConnectors()` to get the live connector list at runtime and render a dynamic wallet picker. Combined with `useAccount()` checking `isConnected`, you get a fully reactive connection state machine with zero manual event wiring.",
      "code_snippet": "import { createConfig, http } from 'wagmi'\nimport { mainnet } from 'wagmi/chains'\nimport { metaMask, coinbaseWallet, walletConnect } from 'wagmi/connectors'\n\nexport const config = createConfig({\n  chains: [mainnet],\n  connectors: [\n    metaMask(),\n    coinbaseWallet({ appName: 'ByteAI', preference: 'all' }),\n    walletConnect({ projectId: process.env.NEXT_PUBLIC_WC_PROJECT_ID! }),\n  ],\n  transports: { [mainnet.id]: http() },\n})\n\n// In your component:\n// const { connectors, connect } = useConnect()\n// connectors.map(c => <button onClick={() => connect({ connector: c })}>{c.name}</button>)",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "IPFS — content addressing, DHT, pinning, IPFS vs Filecoin, CIDs",
      "status": "done",
      "title": "wagmi + IPFS — Resolving On-Chain CIDs Without Breaking NFT Metadata",
      "body": "IPFS uses content addressing: a CID is a cryptographic hash of the content itself, so `ipfs://bafkreigh2akiscaild...` always resolves to identical bytes regardless of which peer serves them. When wagmi's `useReadContract` reads `tokenURI` from an ERC-721, it typically returns an IPFS URI — either CIDv0 (`Qm...`, base58 SHA2-256) or CIDv1 (`baf...`, base32 multibase). Browsers don't speak the `ipfs://` scheme natively, so you must rewrite to an HTTP gateway before passing to `fetch` or an `<img>` tag.\n\nThe DHT (Distributed Hash Table) is how IPFS nodes discover which peers hold a given CID — it's a lookup layer, not a guarantee of availability. Pinning is that guarantee: without an explicit pin, nodes garbage-collect unpinned content. For production, use Pinata or Web3.Storage to pin NFT metadata CIDs.\n\nIPFS ≠ Filecoin: IPFS is the content-routing protocol; Filecoin is a separate incentive layer that pays storage providers to hold pinned CIDs under cryptographic proof-of-storage deals. An unpinned CID on IPFS can vanish; a Filecoin deal gives you verifiable persistence. Always validate the CID prefix in wagmi hooks before resolving — a malformed URI silently returns `undefined` from `useReadContract`.",
      "code_snippet": "import { useReadContract } from 'wagmi'\nimport { erc721Abi } from 'viem'\n\nconst toGatewayUrl = (uri: string): string =>\n  uri.startsWith('ipfs://')\n    ? `https://ipfs.io/ipfs/${uri.slice(7)}`\n    : uri\n\nexport function useNFTMetadataUrl(\n  address: `0x${string}`,\n  tokenId: bigint\n) {\n  const { data: tokenUri } = useReadContract({\n    address,\n    abi: erc721Abi,\n    functionName: 'tokenURI',\n    args: [tokenId],\n  })\n  return tokenUri ? toGatewayUrl(tokenUri) : null\n}",
      "language": "typescript",
      "clarity": 8,
      "specificity": 7,
      "relevance": 9,
      "overall": 8
    },
    {
      "topic": "Writing Transactions — signing, gas estimation, nonce management, transaction monitoring",
      "status": "done",
      "title": "wagmi Transaction Writing — Simulate, Send, and Monitor in One Flow",
      "body": "wagmi v2 splits transaction writing into three distinct hooks that mirror the actual on-chain lifecycle. Use `useSimulateContract` first — it dry-runs the call via `eth_call` and returns a typed `request` object, catching reverts before spending gas. Feed that `request` directly into `useWriteContract` to get a `writeContract` function and the resulting `hash`. Critically, `useSimulateContract` also handles gas estimation internally; override with `gas` only when you need a ceiling. Nonce management is handled by the connected wallet by default, but you can pass `nonce` explicitly for account abstraction or batching scenarios. Once you have a `hash`, hand it to `useWaitForTransactionReceipt` to poll for confirmation — it exposes `status` (`'pending' | 'success' | 'error'`), `confirmations`, and the full `TransactionReceipt`. Chain the enabled flag on `useWaitForTransactionReceipt` so it only activates post-submission. This three-hook pattern gives you granular error surfaces: simulation errors ≠ send errors ≠ receipt errors.",
      "code_snippet": "import { useSimulateContract, useWriteContract, useWaitForTransactionReceipt } from 'wagmi'\nimport { parseEther } from 'viem'\n\nconst { data: simData } = useSimulateContract({\n  address: '0xContractAddress',\n  abi,\n  functionName: 'deposit',\n  value: parseEther('0.1'),\n})\n\nconst { writeContract, data: hash } = useWriteContract()\n\nconst { status, data: receipt } = useWaitForTransactionReceipt({\n  hash,\n  confirmations: 2,\n})\n\n// simData.request is typed — pass directly, no manual gas/nonce needed\nreturn <button onClick={() => writeContract(simData!.request)}>Deposit</button>",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "ENS — name resolution, avatar, records, reverse resolution",
      "status": "done",
      "title": "wagmi ENS — Name Resolution, Avatars, and Reverse Lookup in One Hook",
      "body": "wagmi ships first-class ENS support via `useEnsAddress`, `useEnsName`, `useEnsAvatar`, and `useEnsText` — no ethers.js manual calls needed. Forward resolution maps a name like `vitalik.eth` to an address via `useEnsAddress({ name: normalize('vitalik.eth') })`. Always wrap inputs with `normalize` from `viem/ens` to prevent ENS normalization errors at runtime. Reverse resolution flips it: pass a raw address to `useEnsName({ address })` to retrieve the primary name set by the user. For avatars, `useEnsAvatar({ name })` resolves the `avatar` text record and handles IPFS, Arweave, and HTTP URIs transparently. Arbitrary records — Twitter handle, email, URL — are read with `useEnsText({ name, key: 'com.twitter' })`. All four hooks accept a `chainId` prop so you can target ENS on mainnet even when the user's wallet is on a different chain. Combine `useEnsName` + `useEnsAvatar` on the connected account to build a fully resolved identity header with two lines of hook usage.",
      "code_snippet": "import { useEnsAddress, useEnsName, useEnsAvatar, useEnsText } from 'wagmi'\nimport { normalize } from 'viem/ens'\nimport { useAccount } from 'wagmi'\n\nfunction UserIdentity() {\n  const { address } = useAccount()\n\n  const { data: ensName } = useEnsName({ address, chainId: 1 })\n  const { data: avatar } = useEnsAvatar({ name: ensName ?? undefined, chainId: 1 })\n  const { data: twitter } = useEnsText({ name: normalize('vitalik.eth'), key: 'com.twitter', chainId: 1 })\n  const { data: resolved } = useEnsAddress({ name: normalize('vitalik.eth'), chainId: 1 })\n\n  return <div>{avatar && <img src={avatar} />} {ensName ?? address}</div>\n}",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Multicall — batching contract reads, reducing RPC calls, gas savings",
      "status": "done",
      "title": "wagmi Multicall — Batch Contract Reads with useReadContracts",
      "body": "Every separate `useReadContract` call fires its own `eth_call` RPC request. At scale — think displaying token balances, allowances, and staking positions simultaneously — this hammers your RPC provider and inflates latency. wagmi's `useReadContracts` solves this by batching all reads through the Multicall3 contract in a single `eth_call`.\n\nPass an array of `contracts` configs — each with `address`, `abi`, and `functionName` — and wagmi routes them through Multicall3 automatically when the chain supports it (virtually all EVM chains do). Results come back as a typed array of `{ result, status }` objects, so you handle individual failures without losing the entire batch.\n\nSet `allowFailure: false` if you need strict all-or-nothing semantics — wagmi will throw if any call reverts. Leave it `true` (the default) for resilient UIs that degrade gracefully per-call. Combine with `multicallAddress` override when targeting chains with a custom Multicall3 deployment. The batching is automatic — no manual ABI encoding needed.",
      "code_snippet": "import { useReadContracts } from 'wagmi'\nimport { erc20Abi } from 'viem'\n\nconst tokenContract = { address: '0xA0b8...', abi: erc20Abi } as const\n\nfunction TokenStats({ account }: { account: `0x${string}` }) {\n  const { data } = useReadContracts({\n    allowFailure: false,\n    contracts: [\n      { ...tokenContract, functionName: 'balanceOf', args: [account] },\n      { ...tokenContract, functionName: 'totalSupply' },\n      { ...tokenContract, functionName: 'decimals' },\n    ],\n  })\n\n  const [balance, totalSupply, decimals] = data ?? []\n  return <div>{balance?.toString()}</div>\n}",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Testing with Tools — forking mainnet, Hardhat/Foundry test environment, mocking contracts",
      "status": "done",
      "title": "wagmi — Testing with Forked Mainnet and Mocked Contracts",
      "body": "wagmi's `WagmiProvider` expects a real or simulated chain. For integration tests, the sharpest approach is pairing wagmi with `viem`'s `createTestClient` over a Hardhat or Foundry forked mainnet — giving your hooks access to live state without touching real funds.\n\nWrap your test tree with a `WagmiProvider` configured against a `hardhat` or `anvil` transport pointed at your local fork (`http://127.0.0.1:8545`). Use `createConfig` with `chains: [hardhat]` and `transports: { [hardhat.id]: http() }`. Now `useReadContract`, `useWriteContract`, and `useWaitForTransactionReceipt` all resolve against forked state.\n\nFor unit tests where you want deterministic responses without a running node, swap the transport for `mock()` from `@wagmi/core/test`. Feed it a `requests` handler array that intercepts specific RPC calls — perfect for simulating reverts, specific balances, or ERC-20 allowance responses. Combine `mock()` transport with `renderHook` from Testing Library to test hook behavior in isolation, asserting on `data`, `error`, and `isPending` transitions without any network dependency.",
      "code_snippet": "import { createConfig, http } from 'wagmi'\nimport { hardhat } from 'wagmi/chains'\nimport { mock } from '@wagmi/core/test'\nimport { renderHook, waitFor } from '@testing-library/react'\nimport { useReadContract } from 'wagmi'\nimport { wrapper } from './test-utils' // WagmiProvider wrapper\n\nconst mockConfig = createConfig({\n  chains: [hardhat],\n  transports: {\n    [hardhat.id]: mock({\n      requests: [\n        { method: 'eth_call', result: '0x000000000000000000000000000000000000000000000000002386f26fc10000' },\n      ],\n    }),\n  },\n})\n\ntest('useReadContract returns mocked balance', async () => {\n  const { result } = renderHook(\n    () => useReadContract({ address: '0xTokenAddress', abi: erc20Abi, functionName: 'balanceOf', args: ['0xUserAddress'] }),\n    { wrapper: wrapper(mockConfig) }\n  )\n  await waitFor(() => expect(result.current.data).toBeDefined())\n})",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Frontend Web3 UX — pending states, failed tx handling, gas estimation errors",
      "status": "done",
      "title": "wagmi — Handling Pending States, Failed Transactions, and Gas Errors Like a Pro",
      "body": "wagmi's `useWriteContract` and `useWaitForTransactionReceipt` give you fine-grained control over every UX state in a transaction lifecycle. `useWriteContract` exposes `isPending` (wallet prompt open) and `isError` with a typed `error` — check `error.cause` for `UserRejectedRequestError` to distinguish user cancellations from real failures. Once you have a `hash`, pass it to `useWaitForTransactionReceipt` to track `isLoading` (mining) vs `isSuccess` vs `isError` (reverted on-chain). For gas estimation failures, `useEstimateGas` throws a `ContractFunctionExecutionError` before the wallet even opens — catch this to show inline warnings instead of letting the wallet display a cryptic error. Combine `useSimulateContract` before calling `writeContract`: if simulation fails, the button stays disabled and you surface the revert reason directly from `error.cause.reason`. This pattern eliminates the three most common Web3 UX dead-ends: silent wallet rejections, confusing mining spinners, and surprise gas failures at signing time.",
      "code_snippet": "const { data: sim, error: simError } = useSimulateContract({ ...config });\nconst { writeContract, data: hash, isPending, error: writeError } = useWriteContract();\nconst { isLoading: isMining, isSuccess } = useWaitForTransactionReceipt({ hash });\n\nconst label = isPending ? 'Confirm in wallet…'\n  : isMining   ? 'Mining…'\n  : isSuccess  ? 'Done!'\n  : 'Submit';\n\nconst rejectMsg = writeError?.cause instanceof UserRejectedRequestError\n  ? 'You rejected the transaction.'\n  : writeError?.shortMessage;\n\nreturn (\n  <button disabled={!sim || isPending || isMining} onClick={() => writeContract(sim!.request)}>\n    {label}\n  </button>\n);\n// simError?.cause?.reason surfaces revert reason before wallet opens",
      "language": "typescript",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Security — private key never in frontend, signature phishing, approvals management",
      "status": "done",
      "title": "wagmi Security — Phishing Signatures, Approval Exploits, and Why Your Frontend Holds No Keys",
      "body": "wagmi never exposes private keys — all signing is delegated to the injected wallet via `useSignMessage`, `useSignTypedData`, or `useSendTransaction`. Your dApp only constructs the payload; the wallet owns execution. The danger is what you put in that payload.\n\nSignature phishing happens when a malicious site uses `useSignTypedData` with a crafted EIP-712 domain that mimics a trusted protocol. Always display the `domain.name` and `domain.verifyingContract` in your UI before calling `signTypedDataAsync` so users can verify the target contract.\n\nApproval exploits are the bigger silent risk. Calling `useWriteContract` with `approve(spender, MaxUint256)` grants unlimited allowance. Instead, pass the exact token amount needed for the transaction. After the operation, prompt users to revoke via a follow-up `approve(spender, 0)` write. Use `useReadContract` to query current allowances before any approval flow — if `allowance >= amount`, skip the approve call entirely to avoid redundant exposure.\n\nFor `useSimulateContract`, always check the `request` object before executing — simulation catches revert scenarios but also reveals the exact calldata your user will sign.",
      "code_snippet": "import { useReadContract, useWriteContract, useSimulateContract } from 'wagmi'\nimport { erc20Abi, MaxUint256 } from 'viem'\n\nconst { data: allowance } = useReadContract({\n  address: tokenAddress,\n  abi: erc20Abi,\n  functionName: 'allowance',\n  args: [userAddress, spenderAddress],\n})\n\nconst needsApproval = allowance !== undefined && allowance < requiredAmount\n\nconst { data: approveRequest } = useSimulateContract({\n  address: tokenAddress,\n  abi: erc20Abi,\n  functionName: 'approve',\n  args: [spenderAddress, requiredAmount], // never MaxUint256\n  query: { enabled: needsApproval },\n})\n\nconst { writeContract } = useWriteContract()\nconst handleApprove = () => approveRequest && writeContract(approveRequest.request)",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Choosing a Library — ethers.js vs viem vs web3.js, migration considerations",
      "status": "done",
      "title": "wagmi's Transport Layer — ethers.js vs viem vs web3.js Migration Considerations",
      "body": "wagmi v2 dropped ethers.js entirely and is built on viem as its sole peer dependency. This isn't cosmetic — viem is tree-shakable, ships typed ABI encoding via `parseAbi`/`encodeAbiParameters`, and returns native BigInt instead of ethers' `BigNumber`. If you're migrating a wagmi v1 project, the breakage surfaces immediately in `useContractRead` → `useReadContract`, `usePrepareContractWrite` → `useSimulateContract`, and anywhere you passed ethers `Contract` instances or `providers`. Replace ethers utility calls with viem equivalents: `ethers.utils.formatEther` → `formatEther` from viem, `ethers.utils.parseUnits` → `parseUnits`. web3.js has no first-class integration path — wagmi never supported it natively, so a migration means rewriting all contract interaction logic to viem's `readContract`/`writeContract` primitives, which the wagmi hooks wrap internally. The upside: viem's strict TypeScript inference means your ABI types flow end-to-end through `useReadContract`'s `data` return without manual casting.",
      "code_snippet": "import { useReadContract } from 'wagmi'\nimport { parseAbi, formatEther } from 'viem'\n\nconst abi = parseAbi(['function balanceOf(address) view returns (uint256)'])\n\nexport function TokenBalance({ address }: { address: `0x${string}` }) {\n  const { data: balance } = useReadContract({\n    abi,\n    address: '0x6B175474E89094C44Da98b954EedeAC495271d0f',\n    functionName: 'balanceOf',\n    args: [address],\n  })\n\n  // balance is typed as bigint — no BigNumber.from() needed\n  return <p>{balance ? formatEther(balance) : '0'} DAI</p>\n}",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Contract ABIs — what ABIs are, encoding/decoding, generating typed ABIs",
      "status": "done",
      "title": "wagmi Contract ABIs — Encoding, Decoding, and Typed Generation",
      "body": "ABIs (Application Binary Interfaces) define a contract's external surface — function names, argument types, return types, and event signatures. The EVM uses this schema to encode calldata (function selector + ABI-encoded args) and decode return data back into usable values.\n\nIn wagmi, every hook that touches a contract — `useReadContract`, `useWriteContract`, `useSimulateContract` — requires an ABI. Wagmi delegates the actual encoding and decoding to viem under the hood. The critical detail: always annotate your ABI `as const`. Without it, TypeScript widens the type to `string`, and wagmi can't infer `functionName` as a union of your contract's methods, narrow `args` to the correct tuple types, or type the return value — everything degrades to `unknown`.\n\nFor real projects, skip hand-authored ABIs entirely. Use `@wagmi/cli` with the `foundry` or `etherscan` plugin: `wagmi generate` outputs a `src/generated.ts` with `as const` ABIs and pre-built typed React hooks for every contract function. Your hooks stay in sync with deployments automatically, and you get zero-config type safety across the stack.\n\nFor manual encoding outside hooks — multicall payloads, raw transaction construction — reach for viem's `encodeFunctionData` and `decodeFunctionResult` directly.",
      "code_snippet": "import { useReadContract } from 'wagmi'\n\nconst erc20Abi = [\n  {\n    name: 'balanceOf',\n    type: 'function',\n    stateMutability: 'view',\n    inputs: [{ name: 'account', type: 'address' }],\n    outputs: [{ name: '', type: 'uint256' }],\n  },\n] as const // critical: enables full TypeScript inference in wagmi hooks\n\nexport function TokenBalance({ account }: { account: `0x${string}` }) {\n  const { data: balance } = useReadContract({\n    abi: erc20Abi,\n    address: '0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48',\n    functionName: 'balanceOf', // autocompleted from ABI union\n    args: [account],           // typed as [`0x${string}`], not unknown[]\n  })\n\n  return <span>{balance?.toString() ?? '0'}</span>\n}",
      "language": "typescript",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "RPC Management — rate limits, fallback providers, Alchemy vs Infura vs QuickNode",
      "status": "done",
      "title": "wagmi RPC Management — Fallback Providers, Rate Limits, and Picking Between Alchemy, Infura, and QuickNode",
      "body": "wagmi v2 delegates all transport logic to viem — so RPC management lives in your `createConfig` `transports` map, not in React hooks. The critical primitive is viem's `fallback()` transport, which chains multiple providers and automatically retries the next one on any network error or 429 rate limit response.\n\nAlchemy is the default choice for most dApps: generous free tier (300M compute units/month), fast archive access, and reliable `eth_getLogs` with deep block range support. Infura works for simple reads but throttles aggressively on free plans and imposes stricter `eth_getLogs` depth limits. QuickNode shines for latency-sensitive workloads like real-time mempool watching over WebSocket, but costs more at scale.\n\nPass `rank: true` to `fallback()` to enable latency-based provider ranking — wagmi will automatically prefer the fastest responding RPC across requests. For WebSocket subscriptions (used by `useWatchContractEvent` and `useWatchBlocks`), configure a separate `webSocket()` transport alongside your `http()` fallback; `fallback()` supports mixed transport types. Each `http()` instance accepts `retryCount` and `retryDelay` to tune per-provider retry behavior before escalating down the fallback chain.",
      "code_snippet": "import { createConfig, http, webSocket } from 'wagmi'\nimport { mainnet } from 'wagmi/chains'\nimport { fallback } from 'viem'\n\nexport const config = createConfig({\n  chains: [mainnet],\n  transports: {\n    [mainnet.id]: fallback(\n      [\n        http('https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY', { retryCount: 2 }),\n        http('https://mainnet.infura.io/v3/YOUR_KEY', { retryCount: 1 }),\n        webSocket('wss://rpc.quicknode.pro/YOUR_KEY'),\n      ],\n      { rank: true }\n    ),\n  },\n})",
      "language": "typescript",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "IPFS Pinning — Pinata, web3.storage, NFT.Storage, self-hosted nodes",
      "status": "done",
      "title": "wagmi + IPFS Pinning — Pinata, web3.storage, and On-Chain CIDs via useWriteContract",
      "body": "IPFS pinning services ensure your NFT metadata stays retrievable — the CID you write on-chain via wagmi's `useWriteContract` is worthless if the content isn't pinned. The workflow is always: pin first, then write.\n\n**Pinata** is the most battle-tested option. Use their REST API or `@pinata/sdk` to pin JSON metadata server-side (never expose your JWT client-side), then pass the returned `IpfsHash` as an `ipfs://` URI to your contract call.\n\n**web3.storage** (w3up) uses UCAN-based auth and stores to both IPFS and Filecoin. It's more decentralized but requires the `@web3-storage/w3up-client` setup flow — heavier onboarding than Pinata.\n\n**NFT.Storage** is largely legacy — its free tier depends on Protocol Labs funding and should not be used for new production builds.\n\n**Self-hosted Kubo nodes** give full control via the HTTP API at `localhost:5001/api/v0/add`, but you bear the ops burden of availability and garbage collection.\n\nIn your wagmi component, the CID flows in as a prop or state after the async pin step, keeping your `writeContract` call clean and side-effect-free.",
      "code_snippet": "import { useWriteContract } from 'wagmi'\nimport type { Abi } from 'viem'\n\nexport function MintNFT({ abi, contractAddress }: { abi: Abi; contractAddress: `0x${string}` }) {\n  const { writeContract, isPending } = useWriteContract()\n\n  async function pinAndMint(metadata: Record<string, unknown>) {\n    // Pin to Pinata server-side — JWT must never be exposed client-side\n    const { cid } = await fetch('/api/pin-ipfs', {\n      method: 'POST',\n      body: JSON.stringify(metadata),\n    }).then(r => r.json())\n\n    writeContract({ address: contractAddress, abi, functionName: 'safeMint', args: [`ipfs://${cid}`] })\n  }\n\n  return <button disabled={isPending} onClick={() => pinAndMint({ name: 'ByteNFT #1' })}>Mint</button>\n}",
      "language": "typescript",
      "clarity": 8,
      "specificity": 8,
      "relevance": 9,
      "overall": 8
    },
    {
      "topic": "Event Indexing — why you can't rely on full node getLogs for production, subgraph vs custom indexer",
      "status": "done",
      "title": "wagmi Event Indexing — Why getLogs Breaks in Production and What to Use Instead",
      "body": "wagmi's useWatchContractEvent is perfect for real-time event streaming, but it's a trap for historical data. Internally, it delegates to viem's watchContractEvent, which polls eth_getLogs — and every major RPC provider (Alchemy, Infura, QuickNode) caps that at 2,000–10,000 blocks per request. Try scanning a Uniswap V3 pool from deployment block and you'll exhaust rate limits or timeout before retrieving anything useful.\n\nFor production, pick one of two paths:\n\nThe Graph (subgraph): Write AssemblyScript mappings, deploy a subgraph, query via GraphQL. wagmi v2 ships TanStack Query as its data layer, so useQuery slots directly in — use it for historical state, useWatchContractEvent for live deltas only.\n\nCustom indexer (Ponder, Envio): TypeScript-native, faster local iteration, full control over storage. Ponder exposes a REST/GraphQL API you wire straight into your wagmi frontend.\n\nThe production pattern: index all historical events externally → hydrate UI via useQuery → stream new events with useWatchContractEvent and trigger a refetch. Never paginate getLogs client-side — it doesn't scale past a few thousand blocks.",
      "code_snippet": "import { useWatchContractEvent } from 'wagmi'\nimport { useQuery } from '@tanstack/react-query'\nimport { erc20Abi } from 'viem'\n\nfunction useTokenTransfers(address: `0x${string}`) {\n  // Historical transfers from The Graph — NOT eth_getLogs\n  const history = useQuery({\n    queryKey: ['transfers', address],\n    queryFn: () => fetchSubgraphTransfers(address),\n  })\n\n  // Only real-time deltas via wagmi\n  useWatchContractEvent({\n    address,\n    abi: erc20Abi,\n    eventName: 'Transfer',\n    onLogs: () => history.refetch(),\n  })\n\n  return history\n}",
      "language": "typescript",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    }
  ]
}