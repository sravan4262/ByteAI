# ML Frameworks — Seed Reference

## Seed Config
subdomain: ml_frameworks
domain: ai_ml
tech_stacks: [pytorch, tensorflow, jax, keras, scikit_learn, xgboost]
byte_type_default: article

## Topics to Seed

1. Framework Choice — PyTorch vs TensorFlow vs JAX, when each makes sense
2. Tensor Operations — fundamentals, broadcasting, device management (CPU/GPU)
3. Model Architecture — layers, activation functions, building custom modules
4. Training Loop — forward pass, loss computation, backpropagation, optimizer step
5. Optimizers — SGD, Adam, AdamW, learning rate schedulers
6. Loss Functions — choosing the right loss, custom loss functions
7. Data Loading — DataLoader, batching, shuffling, prefetching, augmentation
8. Regularization — dropout, weight decay, batch normalization, early stopping
9. Transfer Learning — pretrained models, fine-tuning vs feature extraction, frozen layers
10. GPU Acceleration — CUDA, moving tensors to device, mixed precision training
11. Debugging Models — gradient flow, NaN loss, exploding/vanishing gradients
12. Model Evaluation — metrics per task type, confusion matrix, cross-validation
13. scikit-learn — pipeline API, preprocessing, cross-validation, model selection
14. XGBoost — gradient boosting, hyperparameter tuning, feature importance
15. JAX — functional transformations (jit, grad, vmap), XLA compilation
16. Keras — high-level API, functional vs sequential, custom layers and training loops
17. Saving & Loading — checkpointing, model serialization, format choices
18. Production Readiness — model export, ONNX conversion, serving considerations
