{
  "tech_stack": "langchain",
  "subdomain": "nlp_llms",
  "domain": "ai_ml",
  "bytes": [
    {
      "topic": "LLM Fundamentals — tokens, context window, temperature, top-p, sampling strategies",
      "status": "done",
      "title": "LangChain Sampling Controls — Temperature, Top-P, and Context Window Management",
      "body": "LangChain exposes sampling parameters directly on model constructors. `temperature` (0–2 for OpenAI models) controls output randomness — set it near 0 for deterministic code generation, higher for creative tasks. `top_p` enables nucleus sampling: only tokens whose cumulative probability mass reaches that threshold are candidates. Don't set both high simultaneously — they compound stochasticity unpredictably.\n\nContext window management is where LangChain earns its keep. Use `llm.get_num_tokens(text)` or `llm.get_num_tokens_from_messages(messages)` to count tokens before dispatch. For long conversation chains, `trim_messages()` from `langchain_core.messages` trims to a token budget using `strategy=\"last\"` (preserve recent turns) or `\"first\"` (preserve system prompt and early context). Pass the model instance as `token_counter` — it invokes the model's actual tokenizer, not an approximation.\n\n`max_tokens` caps only the *response*, not total throughput. The hard ceiling is `context_window − input_tokens`. For `gpt-4o` that's 128K total; for `claude-3-5-sonnet` it's 200K. Exceeding it throws a `BadRequestError` at runtime — trim proactively using `trim_messages`, not reactively with a try/except.",
      "code_snippet": "from langchain_openai import ChatOpenAI\nfrom langchain_core.messages import HumanMessage, SystemMessage, trim_messages\n\nllm = ChatOpenAI(\n    model=\"gpt-4o\",\n    temperature=0.1,   # near-deterministic for code tasks\n    top_p=0.9,         # nucleus sampling\n    max_tokens=512,\n)\n\nmessages = [SystemMessage(\"You are a code reviewer.\"), *conversation_history]\n\n# Trim to 6000 tokens using the model's own tokenizer\ntrimmed = trim_messages(\n    messages,\n    max_tokens=6000,\n    strategy=\"last\",\n    token_counter=llm,\n    include_system=True,\n)\n\nprint(llm.get_num_tokens_from_messages(trimmed))  # verify before call\nresponse = llm.invoke(trimmed)",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Prompt Engineering — system prompts, few-shot examples, chain of thought, structured output",
      "status": "done",
      "title": "LangChain Prompt Engineering — System Prompts, Few-Shot, CoT, and Structured Output in One Chain",
      "body": "LangChain's `ChatPromptTemplate` is the control plane for model behavior. Compose it with `SystemMessage`, `FewShotChatMessagePromptTemplate`, and `with_structured_output()` to get deterministic, typed responses without post-processing hacks.\n\nFew-shot examples slot in via `FewShotChatMessagePromptTemplate`, which wraps an `example_prompt` and an `examples` list — LangChain serializes them as alternating human/AI turns before your actual input. This is cleaner than string interpolation and works with any chat model.\n\nChain-of-thought is just a system prompt instruction: `\"Think step by step before answering.\"` Pair it with `with_structured_output(MyPydanticModel)` and LangChain will bind a tool-calling schema to the model, parse the response, and raise `OutputParserException` on failure — no manual JSON parsing.\n\nThe key insight: `ChatPromptTemplate | llm.with_structured_output(schema)` is a `Runnable`, so you get `.invoke()`, `.stream()`, `.batch()`, and full LangSmith tracing for free. Structure your prompt once, reuse it across environments.",
      "code_snippet": "from langchain_core.prompts import ChatPromptTemplate, FewShotChatMessagePromptTemplate\nfrom langchain_openai import ChatOpenAI\nfrom pydantic import BaseModel\n\nclass Verdict(BaseModel):\n    reasoning: str\n    is_bug: bool\n\nexamples = [\n    {\"input\": \"NullPointerException on line 42\", \"output\": '{\"reasoning\": \"Unhandled null\", \"is_bug\": true}'},\n    {\"input\": \"Variable renamed for clarity\", \"output\": '{\"reasoning\": \"Style change only\", \"is_bug\": false}'},\n]\n\nfew_shot = FewShotChatMessagePromptTemplate(\n    example_prompt=ChatPromptTemplate.from_messages([(\"human\", \"{input}\"), (\"ai\", \"{output}\")]),\n    examples=examples,\n)\n\nprompt = ChatPromptTemplate.from_messages([\n    (\"system\", \"You are a senior code reviewer. Think step by step before deciding.\"),\n    few_shot,\n    (\"human\", \"{diff}\"),\n])\n\nllm = ChatOpenAI(model=\"gpt-4o\", temperature=0)\nchain = prompt | llm.with_structured_output(Verdict)\n\nresult = chain.invoke({\"diff\": \"users[idx] accessed without bounds check\"})\nprint(result.is_bug, result.reasoning)",
      "language": "python",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Hugging Face — model hub, transformers library, pipelines, tokenizers",
      "status": "done",
      "title": "LangChain + Hugging Face — Loading Hub Models as LLMs and Embeddings",
      "body": "LangChain integrates directly with Hugging Face via `HuggingFaceHub`, `HuggingFacePipeline`, and `HuggingFaceEmbeddings` — each targeting a different runtime context.\n\n`HuggingFaceHub` hits the Inference API remotely, requiring only an API token. `HuggingFacePipeline` wraps a local `transformers` pipeline, giving you full control over device placement (`device=0` for GPU), quantization, and tokenizer kwargs — critical when the Hub's free tier rate-limits you.\n\nFor RAG pipelines, `HuggingFaceEmbeddings` wraps `sentence-transformers` models and slots directly into any `VectorStore`. The default model is `sentence-transformers/all-mpnet-base-v2`, but you can swap to `BAAI/bge-small-en-v1.5` for a faster, smaller footprint with competitive MTEB scores.\n\nKey gotcha: `HuggingFacePipeline` expects `task` to match the model's architecture — use `\"text-generation\"` for decoder-only models and `\"text2text-generation\"` for encoder-decoder (T5, FLAN). Mismatching silently produces garbage output. Always set `max_new_tokens` in `pipeline_kwargs`; the default truncates aggressively.",
      "code_snippet": "from transformers import pipeline\nfrom langchain_community.llms import HuggingFacePipeline\nfrom langchain_huggingface import HuggingFaceEmbeddings\nfrom langchain_core.prompts import PromptTemplate\nfrom langchain_core.output_parsers import StrOutputParser\n\n# Local LLM via transformers pipeline\nhf_pipeline = pipeline(\n    \"text-generation\",\n    model=\"mistralai/Mistral-7B-Instruct-v0.2\",\n    device=0,\n    max_new_tokens=256,\n)\nllm = HuggingFacePipeline(pipeline=hf_pipeline)\n\n# Embeddings for RAG\nembeddings = HuggingFaceEmbeddings(\n    model_name=\"BAAI/bge-small-en-v1.5\",\n    model_kwargs={\"device\": \"cuda\"},\n    encode_kwargs={\"normalize_embeddings\": True},\n)\n\n# Chain\nprompt = PromptTemplate.from_template(\"Explain {topic} in one sentence.\")\nchain = prompt | llm | StrOutputParser()\nprint(chain.invoke({\"topic\": \"tokenizer padding strategies\"}))",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "LangChain — chains, agents, tools, memory, LCEL",
      "status": "done",
      "title": "LangChain LCEL — Stateful Chains with RunnableWithMessageHistory",
      "body": "LangChain Expression Language (LCEL) composes `Runnable` objects using the `|` pipe operator, producing lazy, streamable pipelines. The modern way to add conversational memory isn't a standalone `ConversationChain` — it's wrapping any LCEL chain in `RunnableWithMessageHistory`.\n\nThis wrapper intercepts each invocation, fetches the relevant `BaseChatMessageHistory` by `session_id`, injects prior messages into the `MessagesPlaceholder`, and persists new turns automatically. You declare which keys carry the human input and history via `input_messages_key` and `history_messages_key`.\n\nUnder the hood, LangChain serializes the pipeline via `.get_graph()`, so you can visualize exactly where history insertion occurs. Pair this with `ChatPromptTemplate.from_messages([MessagesPlaceholder('history'), ('human', '{input}')])` and you get multi-turn stateful conversations with zero manual append logic.\n\nFor production, swap in-memory `ChatMessageHistory` for `RedisChatMessageHistory` or `PostgresChatMessageHistory` — they share the same `BaseChatMessageHistory` interface, so the chain itself never changes. This is LCEL's key advantage: composability without coupling to infrastructure.",
      "code_snippet": "from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder\nfrom langchain_core.runnables.history import RunnableWithMessageHistory\nfrom langchain_community.chat_message_histories import ChatMessageHistory\nfrom langchain_groq import ChatGroq\n\nllm = ChatGroq(model=\"llama-3.3-70b-versatile\")\nprompt = ChatPromptTemplate.from_messages([\n    MessagesPlaceholder(\"history\"),\n    (\"human\", \"{input}\"),\n])\nstore = {}\nchain_with_memory = RunnableWithMessageHistory(\n    prompt | llm,\n    lambda sid: store.setdefault(sid, ChatMessageHistory()),\n    input_messages_key=\"input\",\n    history_messages_key=\"history\",\n)\nresponse = chain_with_memory.invoke(\n    {\"input\": \"What is LCEL?\"},\n    config={\"configurable\": {\"session_id\": \"user-42\"}},\n)",
      "language": "python",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "LlamaIndex — document loading, indexing strategies, query engines, retrieval",
      "status": "done",
      "title": "LangChain + LlamaIndex — Using LlamaIndex Query Engines as Native LangChain Retrievers",
      "body": "LangChain and LlamaIndex solve different problems — LangChain orchestrates LLM workflows; LlamaIndex specializes in document indexing and structured retrieval. `langchain_community.retrievers.LlamaIndexRetriever` bridges them cleanly: you get LlamaIndex's indexing strategies as drop-in LangChain retrievers.\n\n`SimpleDirectoryReader` handles multi-format ingestion (PDF, DOCX, HTML, Markdown) automatically. `VectorStoreIndex` defaults to in-memory storage but swaps to Pinecone or Weaviate via `StorageContext.from_defaults(vector_store=...)`. For hybrid retrieval, `RouterQueryEngine` dispatches queries across a `VectorStoreIndex` for semantic search and a `KeywordTableIndex` for exact-match lookups — choosing the right engine based on query intent.\n\nWrapping any LlamaIndex query engine with `LlamaIndexRetriever` makes it fully compatible with `RetrievalQA`, `ConversationalRetrievalChain`, or LCEL's pipe syntax. This lets you use `SummaryIndex` for condensed document answers and `VectorStoreIndex` for chunk-level retrieval — both within a single LangChain pipeline without duplicating embedding logic.\n\nThe practical split: delegate index architecture and retrieval granularity to LlamaIndex; keep prompt chaining, memory, and output parsing in LangChain.",
      "code_snippet": "from llama_index.core import VectorStoreIndex, SimpleDirectoryReader\nfrom llama_index.core.indices.keyword_table import KeywordTableIndex\nfrom llama_index.core.query_engine import RouterQueryEngine\nfrom llama_index.core.selectors import LLMSingleSelector\nfrom langchain_community.retrievers import LlamaIndexRetriever\nfrom langchain.chains import RetrievalQA\nfrom langchain_groq import ChatGroq\n\ndocs = SimpleDirectoryReader(\"./docs\").load_data()\nvector_index = VectorStoreIndex.from_documents(docs)\nkeyword_index = KeywordTableIndex.from_documents(docs)\n\nrouter_engine = RouterQueryEngine(\n    selector=LLMSingleSelector.from_defaults(),\n    query_engine_tools=[vector_index.as_query_engine(), keyword_index.as_query_engine()],\n)\n\nretriever = LlamaIndexRetriever(index=router_engine)\nllm = ChatGroq(model=\"llama-3.3-70b-versatile\")\nchain = RetrievalQA.from_chain_type(llm=llm, retriever=retriever)\nprint(chain.invoke({\"query\": \"What are the system requirements?\"}))",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 9,
      "overall": 8
    },
    {
      "topic": "RAG — chunking strategy, embedding, retrieval, reranking",
      "status": "done",
      "title": "LangChain RAG — Chunking, MMR Retrieval, and Reranking That Actually Works",
      "body": "Chunking strategy is the most undertuned part of LangChain RAG pipelines. The default `RecursiveCharacterTextSplitter` splits on `[\"\\n\\n\", \"\\n\", \" \"]` with `chunk_size=1000, chunk_overlap=200` — reasonable, but semantically blind. For better coherence, swap to `SemanticChunker` from `langchain_experimental.text_splitter`, which uses embedding cosine similarity to detect topic boundaries before splitting.\n\nFor retrieval, drop plain similarity search. Use `search_type=\"mmr\"` on `.as_retriever()` to apply Maximal Marginal Relevance — set `fetch_k` high (50+) and `k` low (10–20) so MMR has candidates to diversify over. This prevents the retriever from surfacing near-duplicate chunks that bloat context and confuse the LLM.\n\nReranking is the single highest-leverage postprocessing step. Wrap any retriever with `ContextualCompressionRetriever` + `CohereRerank` (or the free `FlashrankRerank` from `langchain_community.document_compressors`). A cross-encoder rescores the top-k candidates against the actual query, not just approximate vector distance. In practice this boosts answer quality 15–30% without touching your vector store, embeddings, or prompt.",
      "code_snippet": "from langchain.retrievers import ContextualCompressionRetriever\nfrom langchain.retrievers.document_compressors import CohereRerank\nfrom langchain_community.vectorstores import Chroma\nfrom langchain_openai import OpenAIEmbeddings\n\nvectorstore = Chroma(embedding_function=OpenAIEmbeddings())\nbase_retriever = vectorstore.as_retriever(\n    search_type=\"mmr\",\n    search_kwargs={\"k\": 10, \"fetch_k\": 50}\n)\ncompressor = CohereRerank(model=\"rerank-english-v3.0\", top_n=5)\nretriever = ContextualCompressionRetriever(\n    base_compressor=compressor, base_retriever=base_retriever\n)\ndocs = retriever.invoke(\"How does RLHF work?\")",
      "language": "python",
      "clarity": 9,
      "specificity": 10,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "OpenAI API — chat completions, function calling, structured outputs, embeddings",
      "status": "done",
      "title": "LangChain Structured Outputs — Binding Schemas Directly to ChatOpenAI",
      "body": "LangChain's `with_structured_output()` method on `ChatOpenAI` eliminates the boilerplate of manual function calling. Pass a Pydantic model or JSON schema and LangChain automatically selects the right OpenAI mechanism — `tool_choice` for newer models, `function_call` for legacy ones — and parses the response into your typed object.\n\nUnder the hood, `with_structured_output()` wraps `ChatOpenAI` in a `RunnableBinding` that injects the schema as a tool definition and pipes the raw output through a `PydanticOutputParser`. This means you chain it like any other Runnable: `prompt | llm.with_structured_output(MyModel)`.\n\nFor embeddings, `OpenAIEmbeddings` batches requests automatically using `embed_documents()`, respecting the `chunk_size` parameter (default 1000). Pair it with `FAISS.from_documents()` or `PGVector` for immediate vector store ingestion. Avoid calling `embed_query()` in a loop — always prefer `embed_documents()` for bulk operations to minimize API round-trips and stay within rate limits.",
      "code_snippet": "from langchain_openai import ChatOpenAI, OpenAIEmbeddings\nfrom pydantic import BaseModel\n\nclass Repo(BaseModel):\n    name: str\n    language: str\n    stars: int\n\nllm = ChatOpenAI(model=\"gpt-4o\", temperature=0)\nstructured_llm = llm.with_structured_output(Repo)\n\nresult: Repo = structured_llm.invoke(\n    \"Describe the LangChain GitHub repo as structured data.\"\n)\nprint(result.model_dump())\n\n# Embeddings — batch, not loop\nembedder = OpenAIEmbeddings(model=\"text-embedding-3-small\")\nvectors = embedder.embed_documents([\"RAG\", \"agents\", \"tools\"])",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 9,
      "overall": 9
    },
    {
      "topic": "Anthropic API — Claude models, system prompts, tool use, streaming",
      "status": "done",
      "title": "LangChain + Claude — System Prompts, Tool Use, and Streaming in One Chain",
      "body": "LangChain's `ChatAnthropic` integration exposes the full Claude API surface directly in your chains. System prompts are passed via `SystemMessage` — not as a constructor param — keeping them composable with `ChatPromptTemplate`. For tool use, bind tools with `.bindTools([...])` on the `ChatAnthropic` instance; LangChain handles the `tool_use` / `tool_result` message cycle automatically when you wire an `AgentExecutor` or use `createToolCallingAgent`. Streaming works out of the box with `.stream()` or `.streamEvents()` — the latter gives you granular chunk-level visibility including `on_llm_stream` events, useful for token-level UI updates. Model selection matters: `claude-opus-4-5` for complex reasoning chains, `claude-haiku-4-5-20251001` for high-throughput tool-heavy agents. Watch the `max_tokens` param — Claude requires it explicitly, unlike OpenAI models. Set it on the `ChatAnthropic` constructor or you'll hit a 400. For cost control in multi-turn agents, use `trim_messages` from `langchain_core.messages` to prune history before each invocation.",
      "code_snippet": "from langchain_anthropic import ChatAnthropic\nfrom langchain_core.messages import SystemMessage, HumanMessage\nfrom langchain_core.tools import tool\n\n@tool\ndef get_repo_stars(repo: str) -> int:\n    \"\"\"Returns star count for a GitHub repo.\"\"\"\n    return 42000\n\nllm = ChatAnthropic(model=\"claude-opus-4-5\", max_tokens=1024)\nllm_with_tools = llm.bindTools([get_repo_stars])\n\nmessages = [\n    SystemMessage(content=\"You are a concise developer assistant.\"),\n    HumanMessage(content=\"How many stars does langchain have?\")\n]\n\nfor chunk in llm_with_tools.stream(messages):\n    print(chunk.content, end=\"\", flush=True)",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Groq — inference speed, supported models, API compatibility",
      "status": "done",
      "title": "LangChain + Groq — Sub-Second LLM Inference Without Changing Your Chain Logic",
      "body": "Groq's LPU (Language Processing Unit) delivers 500–800 tokens/sec on models like `llama-3.3-70b-versatile` — roughly 10–25x faster than GPU-hosted equivalents. LangChain exposes this through `ChatGroq` in the `langchain-groq` package, which is a drop-in replacement for `ChatOpenAI`. Because `ChatGroq` implements the same `BaseChatModel` interface, any chain, agent, or LCEL expression that accepts a chat model works without modification.\n\nSupported models include `llama-3.3-70b-versatile`, `llama-3.1-8b-instant`, `mixtral-8x7b-32768`, and `gemma2-9b-it`. Groq's API is OpenAI-compatible at the wire level, but prefer `ChatGroq` over monkey-patching `ChatOpenAI` with a custom `base_url` — the dedicated class handles rate-limit retries and Groq-specific response fields correctly.\n\nFor latency-sensitive pipelines — streaming chat, real-time RAG, or tool-calling agents — swap the model reference and gain speed with zero chain refactoring. `ChatGroq` supports `streaming=True` and `bind_tools()`, so existing ReAct agents work as-is.",
      "code_snippet": "from langchain_groq import ChatGroq\nfrom langchain_core.prompts import ChatPromptTemplate\n\nllm = ChatGroq(\n    model=\"llama-3.3-70b-versatile\",\n    temperature=0,\n    streaming=True,\n    max_tokens=1024,\n)\n\nprompt = ChatPromptTemplate.from_messages([\n    (\"system\", \"You are a concise technical assistant.\"),\n    (\"human\", \"{question}\"),\n])\n\nchain = prompt | llm\n\nfor chunk in chain.stream({\"question\": \"Explain GGUF quantization in 3 sentences.\"}):\n    print(chunk.content, end=\"\", flush=True)",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Ollama — local model serving, model library, privacy benefits",
      "status": "done",
      "title": "LangChain + Ollama — Run Local LLMs Without Sending Data Anywhere",
      "body": "LangChain's `ChatOllama` and `OllamaEmbeddings` classes connect directly to a local Ollama server, letting you swap cloud models for on-device inference with a one-line change. Ollama serves models like Llama 3.2, Mistral, Gemma 2, and Phi-3 via a local REST API on `localhost:11434`. Because inference never leaves your machine, it's the go-to choice for RAG pipelines over sensitive codebases, private documents, or regulated data.\n\nSwap `ChatOpenAI` for `ChatOllama` in any existing chain — the interface is identical. For embeddings in a local vector store, `OllamaEmbeddings` replaces `OpenAIEmbeddings` with zero changes to your retriever or `VectorstoreIndexCreator`. LangChain's streaming support also works out of the box via `stream()`, so you get token-by-token output locally. Pull a model once with `ollama pull llama3.2`, and your entire LangChain app runs fully offline. No API keys, no rate limits, no egress costs — just a running Ollama daemon.",
      "code_snippet": "from langchain_ollama import ChatOllama, OllamaEmbeddings\nfrom langchain_community.vectorstores import FAISS\nfrom langchain.chains import RetrievalQA\n\nllm = ChatOllama(model=\"llama3.2\", temperature=0)\nembeddings = OllamaEmbeddings(model=\"nomic-embed-text\")\n\nvectorstore = FAISS.from_texts(\n    [\"All inference stays on-device.\", \"No API key required.\"],\n    embedding=embeddings,\n)\n\nchain = RetrievalQA.from_chain_type(llm=llm, retriever=vectorstore.as_retriever())\nprint(chain.invoke({\"query\": \"Where does inference run?\"}))",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Embeddings — vector representations, semantic similarity, embedding model comparison",
      "status": "done",
      "title": "LangChain Embeddings — Choosing the Right Model and Measuring Semantic Similarity",
      "body": "LangChain's `Embeddings` interface abstracts over providers — OpenAI, Cohere, HuggingFace, and local models via Ollama — all exposing `embedDocuments()` and `embedQuery()`. The split matters: `embedQuery` applies query-specific instruction tuning on models like `text-embedding-3-large`, while `embedDocuments` batches efficiently.\n\nFor semantic similarity, avoid raw dot product unless embeddings are L2-normalized. LangChain's `MemoryVectorStore` uses cosine similarity by default, which is safer across models with different magnitude distributions.\n\nModel comparison matters significantly. `text-embedding-3-small` (1536-dim) outperforms `text-embedding-ada-002` on MTEB benchmarks at lower cost. For local/offline use, `nomic-embed-text` via `OllamaEmbeddings` hits strong retrieval quality at 768 dims. Always match embedding model between indexing and query time — mixing models silently degrades recall without errors.\n\nUse `embedQuery` for user queries, `embedDocuments` for your corpus. For RAG pipelines, pass your `Embeddings` instance directly into `SupabaseVectorStore` or `PGVector` — LangChain handles the insert and similarity search SQL under the hood.",
      "code_snippet": "import { OpenAIEmbeddings } from \"@langchain/openai\";\nimport { OllamaEmbeddings } from \"@langchain/ollama\";\nimport { MemoryVectorStore } from \"langchain/vectorstores/memory\";\n\n// Swap models without changing downstream code\nconst embeddings = new OpenAIEmbeddings({ model: \"text-embedding-3-small\" });\n// const embeddings = new OllamaEmbeddings({ model: \"nomic-embed-text\" });\n\nconst store = await MemoryVectorStore.fromTexts(\n  [\"LangChain supports pgvector\", \"Redis can cache embeddings\", \"FAISS runs locally\"],\n  [{ id: 1 }, { id: 2 }, { id: 3 }],\n  embeddings\n);\n\nconst results = await store.similaritySearchWithScore(\"vector database options\", 2);\nconsole.log(results); // [ [Document, 0.91], [Document, 0.87] ]",
      "language": "typescript",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Fine-tuning — when to fine-tune vs prompt engineer, LoRA, QLoRA",
      "status": "done",
      "title": "LangChain Fine-Tuning — When to Stop Prompt Engineering and Reach for LoRA",
      "body": "Fine-tuning in LangChain makes sense when few-shot prompting with `FewShotChatMessagePromptTemplate` stops scaling — typically when you need consistent output schema enforcement, domain-specific token distributions, or latency reduction from bloated prompts. Before reaching for LoRA, exhaust `ChatPromptTemplate` with detailed system messages and 5–10 curated examples. If `FewShotChatMessagePromptTemplate` hits 90%+ quality on 10 examples but degrades unpredictably at scale or adds 500+ tokens of overhead per call, fine-tuning is warranted.\n\nWhen you do fine-tune, QLoRA (4-bit quantized LoRA) is the practical default — it fits a 7B model on a 24GB GPU with minimal accuracy loss. Load the result into LangChain via `HuggingFacePipeline`, wrapping a transformers pipeline with `BitsAndBytesConfig`. The fine-tuned model slots into any LCEL chain, `ConversationalRetrievalChain`, or tool-calling agent without API changes.\n\nChoose full LoRA over QLoRA only when you need fp16 precision and have the VRAM budget. The 4-bit quantization loss is negligible for most NLP classification and extraction tasks, but measurable for math reasoning.",
      "code_snippet": "from transformers import AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig, pipeline\nfrom langchain_community.llms import HuggingFacePipeline\nfrom langchain_core.output_parsers import StrOutputParser\nimport torch\n\nbnb_config = BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_compute_dtype=torch.float16)\nmodel = AutoModelForCausalLM.from_pretrained(\"your-org/qlora-finetuned\", quantization_config=bnb_config)\ntokenizer = AutoTokenizer.from_pretrained(\"your-org/qlora-finetuned\")\n\npipe = pipeline(\"text-generation\", model=model, tokenizer=tokenizer, max_new_tokens=256)\nllm = HuggingFacePipeline(pipeline=pipe)\n\n# Slots into LCEL unchanged — no API surface changes from swapping base model\nchain = prompt | llm | StrOutputParser()",
      "language": "python",
      "clarity": 8,
      "specificity": 8,
      "relevance": 9,
      "overall": 8
    },
    {
      "topic": "Agents & Tool Use — function calling, ReAct pattern, tool selection, agent loops",
      "status": "done",
      "title": "LangChain Agents — ReAct Loop with Custom Tools",
      "body": "LangChain's ReAct agent alternates between Thought → Action → Observation cycles until it reaches a final answer. Each cycle, the LLM reasons about which tool to invoke, the tool executes, and the result feeds back into context. Wire this up with `create_react_agent` from `langgraph` (the modern replacement for the deprecated `initialize_agent`). Define tools using the `@tool` decorator — LangChain auto-generates the JSON schema from the docstring and type hints, which the LLM uses for tool selection. Pass tools into `create_react_agent` alongside your model; the agent handles the loop internally via a compiled `StateGraph`. For multi-step tasks, the agent re-enters the reasoning node after each tool call until no more tool calls are emitted. Use `ToolNode` explicitly if you need to intercept tool execution — useful for adding retry logic, logging, or rate limiting. Set `recursion_limit` on your graph config to prevent runaway loops. Tool descriptions are your primary lever for selection accuracy; vague docstrings cause incorrect tool routing far more often than model capability issues.",
      "code_snippet": "from langchain_core.tools import tool\nfrom langchain_openai import ChatOpenAI\nfrom langgraph.prebuilt import create_react_agent\n\n@tool\ndef get_repo_stars(repo: str) -> int:\n    \"\"\"Fetch the current GitHub star count for a given repo (owner/repo format).\"\"\"\n    import httpx\n    r = httpx.get(f\"https://api.github.com/repos/{repo}\")\n    return r.json()[\"stargazers_count\"]\n\nllm = ChatOpenAI(model=\"gpt-4o\", temperature=0)\nagent = create_react_agent(llm, tools=[get_repo_stars])\n\nresult = agent.invoke({\"messages\": [(\"user\", \"How many stars does langchain-ai/langchain have?\")]})\nprint(result[\"messages\"][-1].content)",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Streaming — streaming responses, token-by-token output, UX patterns",
      "status": "done",
      "title": "LangChain Streaming — Token-by-Token Output and UX Patterns That Actually Work",
      "body": "LangChain's LCEL makes streaming first-class — every `Runnable` exposes `.stream()` and `.astream()` out of the box, yielding `AIMessageChunk` objects as tokens arrive. Passing `streaming=True` to `ChatOpenAI` (or any chat model) enables true token-level output; without it, `.stream()` still works but buffers until the full response completes.\n\nFor granular control in complex chains — RAG pipelines, agents — use `astream_events(version=\"v2\")`. It emits structured events (`on_chat_model_stream`, `on_retriever_end`, `on_tool_end`) so you can differentiate token chunks from retrieval results, letting you update distinct UI regions independently.\n\nKey UX pattern: pipe streamed chunks into a `ReadableStream` on the server, then consume with `fetch()` and `response.body.getReader()` on the client. Never buffer the full response before rendering — it kills perceived latency. For Next.js, pair LangChain's `HttpResponseOutputParser` with the Vercel AI SDK's `StreamingTextResponse` for encoding handled automatically.\n\nOne gotcha: intermediate `RunnableLambda` steps returning non-string types silently break streaming unless you yield chunks explicitly or wrap with `RunnablePassthrough`.",
      "code_snippet": "from langchain_openai import ChatOpenAI\nfrom langchain_core.prompts import ChatPromptTemplate\n\nllm = ChatOpenAI(model=\"gpt-4o\", streaming=True)\nprompt = ChatPromptTemplate.from_template(\"Explain {topic} concisely.\")\nchain = prompt | llm\n\n# Sync: yields AIMessageChunk objects\nfor chunk in chain.stream({\"topic\": \"transformers\"}):\n    print(chunk.content, end=\"\", flush=True)\n\n# Async: granular events for complex chains\nasync for event in chain.astream_events({\"topic\": \"transformers\"}, version=\"v2\"):\n    if event[\"event\"] == \"on_chat_model_stream\":\n        print(event[\"data\"][\"chunk\"].content, end=\"\", flush=True)",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Cost Management — token counting, caching, batching, model selection for cost",
      "status": "done",
      "title": "LangChain Cost Management — Token Counting, Caching, and Smart Model Routing",
      "body": "LangChain exposes token usage directly via `AIMessage.usage_metadata` and `get_num_tokens()` on any chat model. Use `model.get_num_tokens_from_messages(messages)` before invoking to estimate cost and gate expensive calls. For caching, drop in `InMemoryCache` or `RedisSemanticCache` — semantic caching matches semantically similar prompts so identical-intent queries hit the cache even with different phrasing. Enable globally with `set_llm_cache(RedisSemanticCache(redis_url=..., embedding=embeddings))`. For batching, pass a list of inputs to `chain.batch([input1, input2], config={\"max_concurrency\": 5})` — this parallelizes requests while respecting rate limits. Model routing is cleanest with a `RunnableBranch` or a custom `RunnableLambda` that inspects token count: route short, simple prompts to `gpt-4o-mini` and reserve `gpt-4o` for long-context or complex reasoning tasks. Combine with `with_fallbacks()` to automatically downgrade on quota errors. Track spend per chain run using `LangSmith` callbacks, which log token usage per trace automatically.",
      "code_snippet": "from langchain.globals import set_llm_cache\nfrom langchain_community.cache import RedisSemanticCache\nfrom langchain_openai import ChatOpenAI, OpenAIEmbeddings\nfrom langchain_core.runnables import RunnableLambda, RunnablePassthrough\n\nset_llm_cache(RedisSemanticCache(redis_url=\"redis://localhost:6379\", embedding=OpenAIEmbeddings()))\n\nmini = ChatOpenAI(model=\"gpt-4o-mini\")\nfull = ChatOpenAI(model=\"gpt-4o\")\n\ndef route_by_tokens(input: dict):\n    tokens = mini.get_num_tokens(input[\"question\"])\n    return full if tokens > 300 else mini\n\nrouter = RunnableLambda(route_by_tokens)\nchain = RunnablePassthrough.assign(model=router) | (lambda x: x[\"model\"].invoke(x[\"question\"]))\n\nresults = chain.batch([{\"question\": q} for q in questions], config={\"max_concurrency\": 4})",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Evaluation — LLM-as-judge, RAGAS for RAG evaluation, benchmarks",
      "status": "done",
      "title": "LangChain Evaluation — LLM-as-Judge, RAGAS, and Benchmarks That Actually Catch Regressions",
      "body": "LangChain's `langchain.evaluation` module ships three evaluator families for production RAG pipelines. `load_evaluator(\"labeled_criteria\")` wires any ChatModel as a judge against reference answers — pass `criteria=\"correctness\"` for factual scoring or define custom rubrics via `EvaluatorType.LABELED_SCORE_STRING`. For RAG-specific signal, RAGAS integrates via `RagasEvaluatorChain`, which wraps `faithfulness`, `answer_relevancy`, and `context_precision` into LangChain-compatible callables — scoring retrieval and generation independently.\n\nThe real leverage comes from pairing both with LangSmith's `evaluate()` runner. Supply a `Dataset` of question/context/ground_truth triples plus a list of evaluators; LangSmith runs your chain, collects full traces, and aggregates scores into a single experiment view. Use `context_recall` to isolate retriever failures from generator failures before they reach users.\n\nFor regression tracking, `langchain_benchmarks` ships task suites like `ToolUsage` and `RetrievalTask` with pre-scored reference runs — no custom harness needed. Stack all three layers: RAGAS for component-level RAG metrics, LLM-as-judge for holistic output quality, and `langchain_benchmarks` for capability regression across model upgrades.",
      "code_snippet": "from langchain.evaluation import load_evaluator, EvaluatorType\nfrom langsmith.evaluation import evaluate, LangChainStringEvaluator\nfrom ragas.metrics import faithfulness, context_precision\nfrom ragas.langchain import RagasEvaluatorChain\n\n# LLM-as-judge: correctness scored against ground truth\ncriteria_eval = LangChainStringEvaluator(\n    \"labeled_criteria\",\n    config={\"criteria\": \"correctness\"},\n)\n\n# RAGAS faithfulness wrapped as a LangChain evaluator\nragas_faithfulness = RagasEvaluatorChain(metric=faithfulness)\n\nresults = evaluate(\n    \"your-langsmith-dataset\",\n    llm_or_chain_factory=your_rag_chain,\n    evaluators=[criteria_eval],\n    verbose=True,\n)\nprint(results.aggregate_feedback)",
      "language": "python",
      "clarity": 8,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Safety & Guardrails — output filtering, prompt injection, hallucination mitigation",
      "status": "done",
      "title": "LangChain Safety & Guardrails — Injection Prevention, Schema Enforcement, and Moderation",
      "body": "LangChain exposes three composable guardrail layers. First, prompt injection: keep user input strictly inside `HumanMessage` via `ChatPromptTemplate.from_messages()`. Never f-string user data into a `SystemMessage` — doing so lets adversaries overwrite your system constraints at runtime. Second, hallucination mitigation via `with_structured_output()`: binding a Pydantic model forces the LLM to emit structured JSON and lets you attach `@field_validator` rules (e.g., requiring `sources` to be non-empty). When the LLM violates the schema, LangChain automatically retries with the validation error appended. For persistent failures, `RetryOutputParser.from_llm()` re-prompts with the original parse error as context. Third, output filtering: `OpenAIModerationChain` wraps any LCEL chain and raises `ValueError` on policy violations before output surfaces to your app — drop it at the end of the pipe. Stack these as: isolation → schema enforcement → moderation. Each layer is independently composable via the `|` operator, so you can add or remove layers without restructuring the chain.",
      "code_snippet": "from langchain_openai import ChatOpenAI\nfrom langchain_core.prompts import ChatPromptTemplate\nfrom langchain.chains import OpenAIModerationChain\nfrom pydantic import BaseModel, field_validator\n\nclass CitedAnswer(BaseModel):\n    text: str\n    sources: list[str]  # enforced citation reduces hallucination\n    @field_validator(\"sources\")\n    def must_cite(cls, v):\n        if not v: raise ValueError(\"Cite at least one source\")\n        return v\n\nllm = ChatOpenAI(model=\"gpt-4o\")\n# User input isolated in HumanMessage — blocks system-prompt injection\nprompt = ChatPromptTemplate.from_messages([\n    (\"system\", \"Answer Python questions only. Refuse all else.\"),\n    (\"human\", \"{question}\"),\n])\nchain = prompt | llm.with_structured_output(CitedAnswer)\nmoderated_chain = chain | OpenAIModerationChain(llm=ChatOpenAI())",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    },
    {
      "topic": "Context Management — long context handling, chunking, sliding window, summarization",
      "status": "done",
      "title": "LangChain Context Management — Chunking, Sliding Windows, and Summarization for Long Docs",
      "body": "LangChain's default `ConversationBufferMemory` dumps the entire history into every prompt — fine for short sessions, catastrophic at scale. For long contexts, you have three practical strategies.\n\n**Chunking with RecursiveCharacterTextSplitter:** Split documents using `chunk_size=1000, chunk_overlap=200`. The overlap prevents losing context at boundaries — critical for coherent RAG retrieval.\n\n**Sliding window via ConversationBufferWindowMemory:** Pass `k=5` to retain only the last 5 exchanges. Zero summarization overhead, but older context is gone permanently.\n\n**Summarization with ConversationSummaryBufferMemory:** Set `max_token_limit=2000`. LangChain auto-summarizes older messages when the buffer exceeds the limit, feeding that summary as a compressed prefix. Uses an LLM call per compression — budget accordingly.\n\nFor RAG pipelines, combine chunking + `FAISS.from_documents()` with `MMR` search to retrieve diverse, non-redundant chunks. Pair with `ContextualCompressionRetriever` and `LLMChainExtractor` to strip irrelevant sentences before they hit your prompt window.",
      "code_snippet": "from langchain.memory import ConversationSummaryBufferMemory\nfrom langchain_openai import ChatOpenAI\nfrom langchain.chains import ConversationChain\n\nllm = ChatOpenAI(model=\"gpt-4o\")\n\nmemory = ConversationSummaryBufferMemory(\n    llm=llm,\n    max_token_limit=2000,\n    return_messages=True\n)\n\nchain = ConversationChain(llm=llm, memory=memory, verbose=True)\n\nchain.predict(input=\"Explain transformer attention in detail.\")\nchain.predict(input=\"Now compare it to state space models.\")\n# Older turns auto-summarized once buffer exceeds 2000 tokens",
      "language": "python",
      "clarity": 9,
      "specificity": 9,
      "relevance": 10,
      "overall": 9
    }
  ]
}